# Open questions — GUI-076

## Resolved

- [x] Which asset is used where? The supplied square `icon.png` is the app/installer icon source; the supplied wide `logo.png` is the visible Welcome/README brand.
- [x] Do marketplace manifests need an image update? No. Their current schemas contain plugin metadata only and expose no logo field.
- [x] Is a user decision needed before planning? No. The ticket body and the inspected asset roles provide a sufficiently specific direction.

## Parked (explicitly deferred)

- None.
