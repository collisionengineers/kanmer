Initial focused checks exposed a planned-file omission: the canonical managed AGENTS body is `scripts/agents-block-body.mjs`, while `scripts/agents-block.mjs` only imports it. The first prose regression run also failed three new assertions because their regexes did not tolerate intentional Markdown line wrapping; the contract prose itself was present. Expanded the bounded file map to include the canonical body, corrected the semantic regex whitespace handling, refreshed AGENTS.md, and preserved these failed attempts for the report.

Authoritative `npm run verify` reached the final `plugin:check` and failed because `plugins/kanmer/scripts/agents-block-body.mjs` had not been regenerated. All preceding build/test/typecheck/docs/smoke/skills/AGENTS checks passed. Ran `npm run plugin:build`, which updated only the packaged canonical body copy (the MCP bundle bytes were unchanged), and expanded the bounded file map from nine to ten paths.
