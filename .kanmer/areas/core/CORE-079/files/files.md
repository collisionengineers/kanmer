# Files

- `apps/gui/src/main/kanmerGit.test.ts` — normalize board-root and expected-path assertions with `pathIdentity`.
- No production behavior change is expected; this is an authoritative Windows test portability fix.
