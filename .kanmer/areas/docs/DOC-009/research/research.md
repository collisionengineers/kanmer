# Research — DOC-009

The stale map is outside the managed block (which ends before the contributor repo layout). The checked source confirms `plugins/kanmer/.mcp.json` is absent and the Codex plugin manifest is skills-only; the only shipped plugin MCP config is `mcp/claude.mcp.json` for Claude Code and grok. The exact two stale map lines are 149–150 of AGENTS.md.

No user decision is required. The correction is textual and must preserve the marker-delimited managed block unchanged.
