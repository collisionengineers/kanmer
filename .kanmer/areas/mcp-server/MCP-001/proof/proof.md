# Proof

Branch at `3144b04`.

- `tools/list` returns **29**; all five group tools present; `get_doc_gates`
  `readOnlyHint`; `delete_item` and `remove_column` the only destructive ones.
- `list_board` surfaces the six stages, all five profiles, proof types, group
  kinds, the doc vocabulary and the gate-exempt folders.
- `get_doc_gates` returns `profile`, `boundaries`, `reachable` and `blockedBy`.
- The full profile gate matrix passes **over stdio**, not just in core: chore's
  one-call jump, spike to Done on research, feature held at Backlog, the
  first-unmet-boundary refusal on a multi-stage jump, custom named requirements,
  and the proof:visual warning that does not block.
- Group behaviour end to end, including the byte-identical group file after a
  member move.
- `plugin:check`: 29 tools match, bundle bytes match.
- `smoke-protocol`: 26/26 across every protocol version the SDK supports.
- Both smoke scripts also run green against the **committed bundle**, not just
  the dev build.
