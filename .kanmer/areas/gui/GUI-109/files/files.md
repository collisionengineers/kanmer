# Files

## Production

| Path | Change |
|---|---|
| `apps/gui/src/renderer/src/App.tsx` | Load active groups for the open card menu; add the Add to group submenu; re-read the latest ticket and append membership through `client.updateItem`; surface errors via the existing card-action path. |
| `apps/gui/src/renderer/src/lib/groupMenu.ts` | Pure group-menu entry and membership helpers. Keeps labels, duplicate disabling, and append semantics deterministic and testable without another data model. |
| `apps/gui/src/renderer/src/lib/groupMenu.test.ts` | Focused tests for labels, empty state, disabled existing memberships, and preservation/no-duplicates. |
| `docs/manual/groups.md` | Replace the stale “no add-to-group control” text with the shipped card context-menu behavior. |
| `apps/gui/src/renderer/src/manual/chapters.generated.ts` | Generated by `npm run build:manual`; refreshes in-app group guidance from the manual source. |

## Existing contracts to reuse

| Surface | Existing contract |
|---|---|
| Group discovery | `ProjectClient.listGroups()` / `window.kanmer.listGroups` / `KanmerStore.listGroups`; active groups exclude archived groups. |
| Membership write | `ProjectClient.updateItem(id, { groups: string[] })`; core validates every id and stores membership on the ticket. |
| Menu behavior | `ContextMenu` nested submenu, disabled item and keyboard semantics. |
| Refresh | Existing `runCardAction` and `refresh` path plus filesystem watcher. |

## Explicit non-targets

No new IPC channels, MCP tools, core storage, group creation UI, provider/dispatch changes, context-menu redesign, group filter/detail changes, or edits to CORE-026/CORE-035.
