# Files

- `apps/gui/src/main/kanmerGit.ts` — preserve the known board root through rename-path failures.
- `apps/gui/src/main/kanmerGit.test.ts` — deterministic regression for rename success followed by ignore failure.
- `apps/gui/src/main/index.ts` — inspect only; confirm callers receive the retained path and paused state.
