2026-08-24 follow-up checks after `npm run build -w @kanmer/core` exit `0`: `node --test scripts/release-notes.test.mjs` exit `0` (1/1 passed); `git diff --check` exit `0`; working diff named only `apps/gui/release-notes.md`; direct wording assertion passed. The surrounding ad-hoc PowerShell wrapper returned `1` only because it indexed a scalar path as an array element; this did not alter the focused test/diff exit codes and is retained here rather than presented as a repository failure.
