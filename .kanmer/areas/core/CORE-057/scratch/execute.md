Implementation complete on `.worktrees/core-057` / `core-057-dns-bound-resolver`, based on CORE-044 cumulative head `142af2f3b105b38b00d659019d1cfe99f3b50844` (PR #165). `sources.ts` now validates DNS under the request AbortSignal, passes validated addresses to a production `https.request` lookup callback (preserving hostname/SNI while preventing a second native DNS resolution), and applies the same timeout to resolver, redirect, and body work. Injected request seam and resolver-timeout regressions were added; existing redirect/SSRF/cache coverage remains intact.

Rails and exact outcomes: focused MCP source tests 16/16 PASS (exit 0); core source/store tests 91/91 PASS (exit 0); `npm run test:http -w @kanmer/mcp-server` 84/84 PASS (exit 0); `npm run build:core` PASS (0); MCP typecheck PASS (0); MCP build/standalone PASS (0); `npm run test:scripts` 88/88 PASS (0); `npm run smoke:protocol` 46/46 PASS (0); `npm run verify:docs` PASS (0); `git diff --check` PASS (0). Root `npm run typecheck` preserved FAIL (1) in existing GUI dispatch/provider/core-export baseline (`dispatchDeliverableProven`, `verifyDeliverable`, `antigravity`); `npm run plugin:check` preserved refusal (1) because linked worktree resolution points to the main checkout core dist. No live DNS rebinding/private-network/Windows-host or packaged external evidence was available; those remain INCONCLUSIVE.
