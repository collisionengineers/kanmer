---
id: GUI-002
type: ticket
title: 1.1 codex -> configFile provider
status: done
area: gui
assignee: ''
profile: feature
labels:
  - v3-phase-1
links: []
refs:
  - docs/functional/frd/FRD-012-connect.md
  - docs/architecture/adr/ADR-0007-codex-project-config.md
archived: false
created: '2026-08-16T00:30:18.596Z'
updated: '2026-08-16T03:32:27.951Z'
---

Register codex as kind `configFile` writing `<root>/.codex/config.toml` (`[mcp_servers.kanmer]`) with pure TOML merge/unmerge preserving unknown tables and keys. Retain `codex mcp remove kanmer-<project>` as best-effort legacy cleanup on connect and disconnect. Surface the trust caveat in the Connect UI.

**Where:** `apps/gui/src/main/providers.ts`, `providers.test.ts`. New dep: `smol-toml` (first TOML dep in main).
**Plan:** `docs/plans/kanmer-v3/phase-1-connect/plan.md` § 1.1
**Governing docs:** FRD-012 R1, ADR-0007
**Depends:** nothing (Phase 1 is independent — ship early)

Today codex is `kind: "cli"` and name-mangles per project via `codexServerName()` (providers.ts:88-95); the `configFile` kind and its merge/unmerge shape already exist for opencode/grok/antigravity (providers.ts:114-133).

Verification: TOML merge idempotent, unknown-key-preserving, byte-stable re-merge; unmerge removes only kanmer; connect codex twice on a real project → one project entry, global drained.
