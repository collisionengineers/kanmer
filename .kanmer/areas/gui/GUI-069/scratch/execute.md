opened PR https://github.com/collisionengineers/kanmer/pull/38 — branch `gui-069-backlog-first-column`, worktree `.worktrees/gui-069`, commit `4936358` off `origin/main@5d0e0d7`.

Rail green before the PR: `npm test` 21 files / 210 tests; `npm run typecheck` all four workspaces named; `npm run build:ui`; `npm run build -w @kanmer/gui`; `npm run check:manual` → "manual: up to date (12 chapters)" (confirmed, not assumed); MCP `smoke.mjs` 120/120 and `smoke-protocol.mjs` 26/26; GUI smoke boot exit 0.

`packages/ui` is **tracked** — research's "untracked" note is stale; 21 files under `git ls-files`, `index.ts` present in `origin/main`. Barrel edit landed in the commit.
