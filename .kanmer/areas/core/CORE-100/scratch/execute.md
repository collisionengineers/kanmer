## Execution evidence — 2026-08-24

- Isolated worktree created from `origin/main` at `4c327f6c557541669b98fbb8e9981a984e0c91c4`: `.worktrees/core-100`, branch `core-100-deterministic-windows-assets`. Dependencies installed with `npm --prefix <worktree> ci --ignore-scripts` (exit 0).
- Baseline `node --test scripts/verify-release-assets.test.mjs`: exit 0, 44 passing tests.
- Read-only public v0.3.6 metadata and `latest.yml` confirmed the immutable mixed inventory: dot-form installer and blockmap, hyphen-form blockmap, MCPB, and a manifest naming absent `Kanmer-Setup-0.3.6.exe`. No public resource was changed.
- After the bounded config/test/AGENTS change, focused `node --test scripts/verify-release-assets.test.mjs`: exit 0, 46 passing tests. `node --test scripts/check-updater-package.test.mjs`: exit 0, 4 passing tests. `npm run verify:agents-block`: exit 0 (31/31). `npm run verify:docs`: exit 0.
- First `npm run test:scripts` attempt exited 1 because the fresh worktree lacked `packages/core/dist/index.js` for pre-existing `auto-run-state` and `release-notes` tests. `npm run build` then exited 0 (isolated core/server build); the single corrective `npm run test:scripts` rerun exited 0, 102/102.
- First direct root-level `electron-builder --win --publish never` invocation exited 1 before an installer because root `package.json` points at non-existent root `index.js`; it scheduled no publish. Correct GUI-workspace `npm run dist -w @kanmer/gui -- --publish never` exited 0. It produced `Kanmer-Setup-0.3.6.exe`, matching blockmap, and `latest.yml` with the same hyphenated name. `node scripts/check-updater-package.mjs` exited 0 (8 checks).
- Read-only `node scripts/verify-release-assets.mjs 0.3.6 --dir apps/gui/release` exited 1 as required. It reports the missing public `Kanmer-Setup-0.3.6.exe` plus byte mismatches against the newly generated local package; dot-form installer/blockmap remain informational extras. No release/tag/asset/workflow/credential/publisher action occurred.

## Correction and final verification — 2026-08-25

- Read-only tag-workflow run `32785754328` retained the full v0.3.6 verifier result after 10 bounded attempts: missing `Kanmer-Setup-0.3.6.exe`; expected space-origin blockmap mapped to the hyphen name but public size was `83041` vs local `83074`; public blockmap SHA-256 `9fc4…` vs local `83f9…`; public `latest.yml` SHA-256 `00ca…` vs local `3c37…`. Dot-form installer/blockmap and MCPB were informational extras. This is a test-fixture completeness correction, not a separate production source defect.
- Plan and checklist were amended through MCP. The regression now asserts exactly those four errors and passes (46/46).
- Commits pushed: `7e0683be876ff1e3921a999fc3d6f0257d937e43` (explicit Windows artifact name, documentation, initial regression) and `fb501a0487dc4314e432054c7ef01336b5d67f25` (complete historical tag-workflow integrity regression).
- Final fresh GitHub-origin normal clone at `fb501a0487dc4314e432054c7ef01336b5d67f25`: `npm ci --ignore-scripts` exit 0; authoritative `npm run verify` exit 0. It included core 310/310, GUI 468/468, MCP HTTP 102/102, scripts 102/102, typecheck, docs, MCP smokes, MCPB check, skill/AGENTS/plugin checks.
