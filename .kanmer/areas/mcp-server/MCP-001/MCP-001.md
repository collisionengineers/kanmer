---
id: MCP-001
type: ticket
title: 3.2 Tool surface v3
status: backlog
area: mcp-server
priority: medium
assignee: ''
labels:
  - v3-phase-3
links: []
blocks:
  - GUI-009
  - GUI-013
  - GUI-014
  - SKILL-001
refs:
  - docs/functional/frd/FRD-022-mcp-server-surface.md
  - docs/architecture/adr/ADR-0009-skills-are-not-the-contract.md
archived: false
created: '2026-08-16T00:30:18.734Z'
updated: '2026-08-16T02:24:35.734Z'
---

Add `create_group`, `get_group`, `list_groups`, `get_group_doc`, `set_group_doc`. `update_item` gains `groups`, `profile`, `requires`; priority params dropped; column kind narrows to area-only. `get_doc_gates` exposes the core resolver including warnings. `list_board`/`get_status` surface stages, kinds, profiles, proofTypes and the doc vocabulary. Every description rewritten as the ADR-0009 contract layer. Annotations audited.

**Where:** `packages/mcp-server/src/index.ts`
**Plan:** `docs/plans/kanmer-v3/phase-3-groups-mcp/plan.md` § 3.2
**Governing docs:** FRD-022 R1-R5, ADR-0009
**Depends:** 3.1

24 tools today -> 29. `ColumnKind` narrows from `status|area|priority`, touching `add_column`'s enum, `columnList()` and `Settings.tsx`.

**Release rail (hard):** tool-reference.md +5 rows, status/priority column prose removed, field semantics updated; `plugin:build` then `plugin:check` at the new count.

**Note:** `mcp-server` has no unit tests — `smoke.mjs` is the entire safety net, so extend it per FRD-022's acceptance list. Consider generating tool-reference.md from the zod shapes: `plugin:check` compares names only (check-plugin-sync.mjs:39-45) and everything below `## Field semantics` is invisible to it, so description drift is unguarded.
