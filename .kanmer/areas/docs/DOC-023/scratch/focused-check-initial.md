2026-08-24 initial focused check: `node --test scripts/release-notes.test.mjs` exited `1` in the new clean DOC-023 worktree before evaluating the note text because `scripts/release-notes.mjs` imports `packages/core/dist/index.js`, which does not exist in a fresh checkout. `git diff --check` exited `0`; the one-file diff and direct wording assertion passed. No source defect is inferred. The core build prerequisite will be run locally before one subsequent focused-test attempt; both outcomes are retained.
