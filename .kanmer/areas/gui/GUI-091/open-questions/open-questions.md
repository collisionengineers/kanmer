# Open questions

No user-only questions.

## Parked (explicitly deferred)

- [ ] GUI-068's native updater-refusal dialog still cannot be guaranteed by `webContents.capturePage()`, because Electron captures the web page rather than OS-owned dialog pixels. It is intentionally deferred to [[GUI-068]]'s real-release/human-verification scope.
