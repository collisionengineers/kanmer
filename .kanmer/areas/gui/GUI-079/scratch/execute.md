opened PR https://github.com/collisionengineers/kanmer/pull/47 (branch `gui-079-connect-ownership`, worktree `.worktrees/gui-079`, commit `754566c`).

Rebased onto `c81063e` mid-flight — origin/main had advanced by three PRs (#42, #43, #44) while this was being written. #44 (MCP-009) rewrote FRD-012's R2 matrix, R4 and R5, which conflicted; resolved by keeping upstream's rewrite wholesale and re-applying this ticket's R1/R1a/R1b/R4-clause/AC-6/AC-7/upgrade-note on top. Two consequential details from that merge: upstream's R5 is no longer "re-verify against host docs" but ADR-0009's method clause ("established against the installed binary… with the command and its output recorded"), so ADR-0012 and the grok test comment now cite it that way and record the actual commands; and FRD-012's open-work list named GUI-079 as an outstanding R4 violation, which this PR removes.
