# Files — CORE-076

- `apps/gui/src/main/kanmerGit.ts` — make orphan board commit/push and source `.kanmer/` cleanup retryable after partial completion.
- `apps/gui/src/main/kanmerGit.test.ts` — deterministic post-commit cleanup failure/retry regression, preserving existing orphan and Git rails.
