// GENERATED by scripts/build-manual.mjs — do not edit.
//
// Committed deliberately: the renderer CSP forbids fetching anything at
// runtime, and the packaged app does not ship /docs/. Regenerate with
// `npm run build:manual`; `--check` fails when this file is stale.

export interface ManualChapter {
  id: string;
  title: string;
  /** Markdown. */
  body: string;
}

export const MANUAL_CHAPTERS: readonly ManualChapter[] = Object.freeze([
  {
    "id": "getting-started",
    "title": "What Kanmer is",
    "body": "Kanmer is a kanban board that lives inside your repository as ordinary markdown\nfiles. There is no server, no account, and nothing to sign into. The board is a\nfolder called `.kanmer` in your project, and it works offline because there is\nnothing for it to be online with.\n\nThat one decision is what the rest of this manual is downstream of.\n\n## Why files\n\nBecause it means your coding agents and you are looking at the same board.\n\nNot a summary of it, not an export, not an integration that syncs every few\nminutes and disagrees in between — the same files. When an agent researches a\nticket, it writes the research into the ticket. When it moves a card, the card\nmoves on your screen. When you drop a mockup onto a ticket, the agent can open\nit.\n\nMost tools put an API between the human's view and the machine's. Kanmer puts a\nfolder there, which turns out to be enough, and has the useful property that\nyour work is diffable, greppable, reviewable and yours.\n\n## The shape of it\n\nA **ticket** is a folder with an id like `API-001`. It holds a few fields and up\nto seven **documents** — research, files, plan, checklist, open questions,\npost-implementation report, proof — each of which is a real piece of writing\nrather than a text field.\n\nTickets sit in one of six **stages**, and those six are the same on every Kanmer\nboard:\n\n**Backlog → Preparing → Implementing → Review → Verifying → Done**\n\nAll six are columns on one board, Backlog first — there is no second place to\nlook.\n\nAlongside the stages, **areas** say what part of the product a ticket belongs to\nand colour it accordingly, and **groups** collect tickets that ship together\n(an epic) or that matter right now (a horizon).\n\n## The one idea worth having up front\n\nBoards usually let you move anything anywhere, which means the column a ticket\nis in records what somebody clicked rather than what happened.\n\nKanmer attaches requirements to the **boundaries between stages**. How many\nrequirements depends on the ticket's **profile** — a two-line fix owes almost\nnothing, a feature owes research and a plan and proof — so the board can be\nstrict without being bureaucratic about small work.\n\nThe result is that a ticket in Verifying really has merged, and a ticket in Done\nreally does have evidence behind it. Not because anyone was disciplined, but\nbecause the board asked at the moment it mattered.\n\nIf a move is ever refused, **Why can't I move this?** explains both reasons it\ncan happen and what to do about each.\n\n## Where to go next\n\n- **Install and open a project** — if you have not yet.\n- **Connect an agent** — the step that makes the rest of this worth doing.\n- **Your first ticket, end to end** — one worked example, which is the fastest\n  way to understand everything above.\n- **The six stages** and **Profiles: what a ticket owes** — the two concepts the\n  rest of the manual builds on."
  },
  {
    "id": "install",
    "title": "Install and open a project",
    "body": "Kanmer is a desktop application. There is no account to make, no server to\npoint it at, and nothing to configure before it is useful.\n\n## Installing\n\nDownload the installer from Kanmer's releases page and run it.\n\nThe installer is not code-signed, so Windows SmartScreen will warn you the first\ntime. Choose **More info → Run anyway**. You pay that once: updates after the\nfirst install are downloaded and applied by Kanmer itself and do not trigger the\nwarning again.\n\nIt installs for your user rather than for the whole machine, so it does not ask\nfor administrator rights, and you can choose the location if you want to.\n\n## Opening a project\n\nKanmer works on a folder. **File → Open project folder…**, and pick the root of\nthe project you want a board for — the same folder you would open in an editor.\n\nYou can have several projects open at once; each gets a tab across the top.\nKanmer remembers what you had open and reopens it next time, and keeps a list of\nrecent projects.\n\n## Where the board lives\n\nYour board is a folder called `.kanmer` inside the project. Tickets, their\ndocuments, your areas and your board settings are all files in there — readable,\ndiffable, greppable, and yours.\n\nIf the project is a Git repository, there is one wrinkle worth knowing about\nimmediately: the board is kept on its own branch, in a second working copy at\n`.worktrees/kanmer`, so that ticket edits never show up in your code reviews.\nKanmer sets that up and finds it for you. See **Sharing a board over Git**.\n\nIf it is not a Git repository, `.kanmer` sits directly in the project folder and\neverything else works the same.\n\n## The first time\n\nAn empty board is not much to look at, so the fastest way to understand Kanmer\nis to put something on it. **Your first ticket, end to end** walks through\nexactly that.\n\nTwo things are worth doing early:\n\n- **Set up your areas** in **Settings → Board**. An area is a part of your work,\n  it colours and clusters your cards, and it decides the prefix on ticket ids.\n  A board ships with one; three or four is usually right.\n- **Connect an agent** in **Settings → Connect**, if you work with one. See\n  **Connect an agent**.\n\n## A note on when `.kanmer` appears\n\nThe folder is created when the board is first written to — when you create your\nfirst ticket, or when you change a board setting. Opening a project and looking\naround does not create anything, so pointing Kanmer at a folder to see what is\nin it leaves no trace."
  },
  {
    "id": "connect",
    "title": "Connect an agent",
    "body": "Kanmer is most useful when your coding agent can read and move tickets itself,\nrather than you relaying between a chat window and a board. Connecting an agent\ntakes one click per host.\n\n## Doing it\n\nOpen **Settings → Connect**. You will see a row per supported host, each with a\n**Connect** button:\n\n| Host | Notes |\n|---|---|\n| Codex | |\n| Claude Code | |\n| opencode | |\n| Grok CLI | |\n| Antigravity | User-scoped native plugin; background sessions use `--add-dir` — see below |\n\nPress **Connect** on the one you use. Two things happen: Kanmer registers this\nproject's board with that host's agent-tool client, and it installs Kanmer's\nskills for that host so the agent knows the working practices, not just the\ntools. The row reports what it wrote.\n\nGrok and Antigravity use native `kanmer` plugins installed in the user's host\nprofile, so each affects every workspace for that user. Connect warns before\nthis user-scoped change, preflights the CLI/runtime, validates the plugin,\nrequires a functional host proof before migration cleanup, and then retires\nonly legacy Kanmer project state. Other providers keep their project-scoped\nregistration or skill paths; connecting them does not change your global\nconfiguration.\n\n## Restart the agent afterwards\n\nKanmer will not remind you, so remember it here: an agent that was already\nrunning started before the registration existed and will not see it. Quit the\nagent and start it again in the same project.\n\nThe same is true after Kanmer updates itself. An agent holding a connection is\nrunning the version it started with; restart it or it keeps reading your board\nwith the old code.\n\n## Connect a private board to ChatGPT\n\nOpenAI Secure MCP Tunnel can let a ChatGPT developer-mode app reach Kanmer\nwithout a public MCP endpoint or an inbound firewall rule. The separate\n`tunnel-client` process connects\noutbound to OpenAI and starts Kanmer's existing stdio server as its private MCP\ntarget. Kanmer never stores the API key. The GUI has a separate **Settings →\nOpenAI tunnel** surface that stores non-secret profile metadata and manages the\nsame long-lived runtime alias used by the manual commands below.\n\nYou need an OpenAI tunnel associated with the intended Platform organization\nand ChatGPT workspace, a runtime API key whose principal has **Tunnels Read +\nUse**, ChatGPT developer-mode access, and outbound HTTPS access to\n`api.openai.com:443`. Download `tunnel-client` from the link in Platform tunnel\nsettings or OpenAI's current release; keep the API key out of project files.\n\nCreate the runtime key at **OpenAI Platform → Organization settings → API\nkeys**, in the same organization as the tunnel. This is a normal organization\nAPI key whose principal has **Tunnels Read + Use**, not an Admin API key. Admin\nkeys are needed only for programmatic tunnel creation, editing and deletion.\nThe Platform shows a new key once: put it in the process environment, never in\nthe profile, a command committed to source control, or a chat message. Revoke\nand replace a key immediately if it is exposed.\n\nThe installed app already contains a stable launcher at\n`%LOCALAPPDATA%\\Kanmer\\bin\\kanmer-mcp.cmd`. Point the tunnel at that launcher,\nnot at a versioned `Kanmer.exe` or bundle path: the installer keeps the launcher\nstable while replacing the runtime behind it. Because tunnel-client 0.0.11\nparses a Windows command string before it starts MCP, use a small\noperator-private PowerShell wrapper to preserve the project working directory.\nFor example, save this outside the project as `kanmer-tunnel-mcp.ps1`:\n\n```powershell\n$env:KANMER_PROVIDER_CWD = \"C:/path/to/project\"\n$env:KANMER_BOARD_BRANCH = \"<saved-board-branch>\"\n& \"$env:LOCALAPPDATA\\Kanmer\\bin\\kanmer-mcp.cmd\"\nexit $LASTEXITCODE\n```\n\nSubstitute the project path and the exact board branch saved in **Settings →\nGit** (the default is `kanmer-board`). Then substitute your paths, profile,\nalias, environment-variable name and tunnel id:\n\n```powershell\n$env:CONTROL_PLANE_API_KEY = \"<runtime-api-key>\"\n\n$tunnelClient = \"C:/path/to/tunnel-client.exe\"\n$mcpCommand = 'powershell.exe -NoProfile -NonInteractive -File \"C:/private/path/kanmer-tunnel-mcp.ps1\"'\n\n& $tunnelClient runtimes connect `\n  --alias kanmer-board `\n  --profile kanmer-local `\n  --tunnel-id <tunnel-id> `\n  --runtime-api-key env:CONTROL_PLANE_API_KEY `\n  --mcp-command $mcpCommand `\n  --tunnel-client-bin $tunnelClient\n\n& $tunnelClient doctor --profile kanmer-local --explain\n& $tunnelClient runtimes status kanmer-board --json\n```\n\n`runtimes connect` is the supported long-lived local supervisor. Do not report\nthe tunnel ready from `doctor` alone: status must say the process is running,\nhealthy and ready. Use the corresponding runtime stop/remove command before\nretiring an alias; do not kill an unrelated process or delete the remote tunnel.\n\nIn the GUI, open **Settings → OpenAI tunnel**, enter the runtime alias, profile name, tunnel id,\n`tunnel-client` path, credential environment-variable name, and loopback health\naddress, then save. The address is a validated, non-secret expectation; the GUI\ndoes not rewrite `tunnel-client`'s profile file or claim a live listener, so\ndistinct ports must still be configured in the client profile by the operator.\nThe GUI uses `runtimes connect`, `runtimes status`, `runtimes stop`, and\n`runtimes rm`. The runtime alias and client profile may have different names.\n**Connect runtime** creates or reuses the alias and **Check\nstatus** reports ready only when the managed process is running, healthy, and\nready. The credential is passed as `--runtime-api-key env:<NAME>`, so its value\nis never persisted or rendered by Kanmer. A downloaded app update marks a ready\nruntime for reconnect; quitting Kanmer leaves the long-lived managed runtime\nrunning. Stop and remove affect only the local alias and never delete the remote\ntunnel. In ChatGPT select the discovered Tunnel app. Do not use Custom\nConnector: that screen requests OAuth endpoints Kanmer does not implement.\nCloudflare settings are a different provider path and are not used here.\n\nDisabling a saved profile stops and confirms its managed runtime first. Project\nidentity reconciliation removes the old local alias before transferring the\nprofile, after which Connect binds a new runtime to the current board and source\nroots. Check status can inspect an existing runtime without the runtime API key;\nthe key is required when connecting or reconnecting.\n\nUse forward slashes inside `$mcpCommand`, including on Windows. Version 0.0.11's\ncommand parser treats backslashes as escapes; a normal Windows path such as\n`C:\\Users\\...` becomes invalid during its executable preflight. The profile\nit creates under `%APPDATA%\\tunnel-client` refers to the API key as an\n`env:<NAME>` reference; it does not need the key written into YAML.\n\nKeep the managed runtime connected while creating the ChatGPT app and whenever\nthe app uses Kanmer. In ChatGPT's developer-mode app settings, choose **Tunnel** and\nselect the tunnel associated with that workspace. Keep the local operator UI\non its default loopback address. Restart the tunnel after a Kanmer update,\nbecause the installed MCP process is replaced during the update.\n\nThis path was exercised successfully on Windows with `tunnel-client` 0.0.11.\nYour own tunnel identifier, workspace, and credentials are private operational\nstate and must not be committed. The packaged MCP smoke separately verifies all\n41 tools and their file mutations, including the policy-bound dispatch/list/cancel surface when an operator explicitly enables it.\n\n### More than one project\n\nUse one OpenAI tunnel, one local profile, one private wrapper, one runtime alias,\nand one ChatGPT app per Kanmer project. Reuse the installed stable launcher and\nruntime API key; change the tunnel id, profile name, wrapper project path and\nalias:\n\n```powershell\n$profile = \"another-project\"\n$alias = \"another-project\"\n$mcpCommand = 'powershell.exe -NoProfile -NonInteractive -File \"C:/private/path/another-project-kanmer-mcp.ps1\"'\n\n& $tunnelClient runtimes connect `\n  --alias $alias `\n  --profile $profile `\n  --tunnel-id <another-project-tunnel-id> `\n  --runtime-api-key env:CONTROL_PLANE_API_KEY `\n  --mcp-command $mcpCommand `\n  --tunnel-client-bin $tunnelClient\n```\n\nEach wrapper sets its own `KANMER_PROVIDER_CWD` and `KANMER_BOARD_BRANCH`. The stable launcher discovers\neither a direct `.kanmer` folder or the GUI-managed board worktree from that\nproject root. List configured profiles with `tunnel-client profiles list` and\nmanaged aliases with `tunnel-client runtimes list --json`.\nProfiles default their local health/admin surface to `127.0.0.1:8080`, so run\none at a time or assign each profile a distinct `health.listen_addr` before\nrunning them concurrently.\n\n#### Named endpoint registry\n\nEvery Kanmer MCP process serves exactly one project — the board it was started\nwith — and no request can point it at another path. To keep an eye on several\nprojects at once, name their endpoints in a small registry file and any of\nthose servers will report all of them through the read-only `list_projects`\ntool:\n\n```json\n{\n  \"schema\": 1,\n  \"endpoints\": {\n    \"kanmer\": { \"boardRoot\": \"C:/path/to/kanmer/.worktrees/kanmer\", \"repoRoot\": \"C:/path/to/kanmer\", \"boardBranch\": \"kanmer-board\", \"policy\": \"main-only\" },\n    \"another-project\": { \"boardRoot\": \"C:/path/to/another/.worktrees/kanmer\" }\n  }\n}\n```\n\nThe file lives at `~/.kanmer/endpoints.json`, or wherever\n`KANMER_ENDPOINT_REGISTRY` (an absolute path in the server's environment) says\n— an operator or the Kanmer app decides that when the server is started, never\nan agent request. Names are lowercase (`a-z`, `0-9`, `.`, `_`, `-`); paths must\nbe absolute. For each name the tool reports the project's logical identity,\nwhere it physically is, its board sync state, the declared `policy`, health\n(`ok`, `unassigned` for a board that has not received its identity yet,\n`missing-board`, or `invalid` for a malformed entry — never silently dropped),\nand the controllers and workspaces currently holding tickets there. The\nanswering server marks which entry is its own. Everything across projects is\nobservational: writing to another project means connecting to that project's\nendpoint and passing its `project_id` as `expected_project`; sending another\nproject's id to this one is refused with `WRONG_PROJECT`. The Kanmer app reads\nand edits the same file from **Settings → Projects** (see **Settings**), so a\nname you add there is what `list_projects` reports. Combining boards behind one tunnel is discouraged:\neach exposes the same Kanmer tool names, leaving the remote agent without a\nclear board-selection boundary.\n\n### Instructions for the remote agent\n\nChatGPT discovers Kanmer's tool names, schemas and descriptions through MCP,\nbut it does not receive the Kanmer skills installed into local coding agents.\nGive the app this compact operating instruction:\n\n> Start with `get_status`, `list_board`, and `list_items`. Keep each piece of\n> work in a ticket. Before every move call `get_doc_gates`, cross at most one\n> stage boundary, and write required ticket documents with `set_ticket_doc`.\n> Use `append_scratch` for working notes, concurrency tokens from reads when\n> updating shared state, and `archived: true` instead of permanent deletion.\n\nRemote calls use the same tools as a local agent. `create_item` exclusively\ncreates the ticket's Markdown/frontmatter file; `update_item` atomically\nrewrites it; `move_item` checks the configured gates before changing its\nfrontmatter; and `set_ticket_doc` atomically writes Markdown inside the ticket\nfolder. Each mutation also appends an activity entry. The GUI sees those file\nchanges through its existing watcher—there is no second remote database or\nsync layer.\n\n### Cloudflare does not make this tunnel provider-neutral\n\nThe OpenAI Windows client package may include `cloudflared.exe`. In the tested\n0.0.11 package it is a pinned transport companion managed by `tunnel-client`:\nthe outer client still authenticates to OpenAI's `/v1/tunnel/*` control plane,\nuses an OpenAI tunnel id, and generates the token file required by its\nCloudflare companion. It does not produce a stable MCP URL that can be pasted\ninto another provider.\n\nKanmer itself remains provider-neutral over stdio. Another provider can use the\nsame server if it can launch stdio MCP locally or supplies its own private-MCP\nbridge. Reusing Cloudflare independently would be a separate deployment: it\nwould require an HTTP MCP transport, a hostname, TLS and authentication, and\nmust not be inferred from the presence of the bundled executable.\n\n## Antigravity: bind the folder\n\nConnect installs the user-scoped native Kanmer plugin (`agy plugin install`),\nwhich owns its skills and root `mcp_config.json`. Older `.agents/mcp_config.json`\nand `.agents/skills/` files are migration residue; Connect removes only Kanmer's\nowned entries after a functional plugin proof.\n\nAn interactive `agy` session reads the plugin's MCP and skills only when bound to\nthe workspace folder:\n\n```sh\nagy --add-dir /path/to/your/project     # binds for this session, stores nothing\nagy --new-project                        # binds by creating a project for the folder\n```\n\nA plain `agy` started inside the project does **not** bind it. For background\ndispatch Kanmer therefore always passes `--add-dir <project> -p <prompt>`; it\nnever relies on cwd, creates a persistent project id, or uses `--new-project`.\n\nTwo ways to tell it worked, both worth knowing because the obvious check is\nmisleading: ask the agent to use a Kanmer tool and see whether it comes back with\nreal board data. Do not go looking for a tool named after Kanmer in a tool list —\na connected workspace MCP server does not appear under its own name there.\n\nThe read-only local CLI probe used `agy` 1.1.14: `agy plugin validate\nplugins/kanmer` reported 12 skills and 1 MCP. A real install and bound\n`get_status` tool call require an authorized disposable host and remain\nINCONCLUSIVE when that evidence is unavailable. The Antigravity IDE has not\nbeen tested, so nothing here is a claim about it.\n\n## When it does not work\n\nIf Kanmer cannot complete the registration it says so and shows you what it was\ntrying to do, with a **Copy** button. For hosts driven by a command line, that\nis the exact command — paste it into a terminal in your project and you are\nconnected. For hosts configured through a file, it names the file to edit.\n\nTwo host-specific things worth knowing:\n\n- **Codex** only loads a project's configuration for folders you have told it to\n  trust. If Codex ignores the registration, trust the folder explicitly; Kanmer\n  warns you when it can tell this applies.\n- **Skills are replaced, not merged.** Connecting, or pressing **Update skills**\n  when Kanmer offers it, overwrites the Kanmer-owned skill folders in your\n  project. If you have edited one by hand, that edit is discarded — and Kanmer\n  names the folders it replaced so you can see what went.\n\n## Keeping the skills current\n\nWhen the copy of the skills in your project is older than the one your Kanmer\nships, the row shows both versions and an **Update skills** button. Nothing\nbreaks if you leave it; the newer skills simply describe the newer app.\n\n## Disconnecting\n\n**Disconnect** removes the registration for that host from this project. It does\nnot touch your board, your tickets, or anything outside the project."
  },
  {
    "id": "first-ticket",
    "title": "Your first ticket, end to end",
    "body": "One worked example, start to finish. It is worth doing once on something real\nbut small, because the five chapters after this one will make considerably more\nsense afterwards.\n\n## 1. Create it\n\nPress **N**, or use the quick-add row at the top of a column.\n\nGive it a title that says what will be true when it is done. Pick an **area** if\nyou have set some up. Leave the **profile** alone for now — you will get `fix`,\nwhich is the right default for most work.\n\nThe ticket appears in Backlog with an id like `API-001`.\n\n## 2. Decide what it owes\n\nOpen it. At the top of the Ticket tab is the readiness panel: the profile, and a\nrow for each upcoming boundary showing what is required and whether it exists\nyet.\n\nA `fix` wants a files document and a plan before it can leave Preparing, and\nproof before it can reach Done. That is the whole contract, visible up front.\n\nIf this is a bigger piece of work than a `fix`, change the profile now — the\npanel updates immediately.\n\n## 3. Let an agent prepare it\n\nWith an agent connected, you have two ways to do this.\n\n**Ask it.** \"Have a look at API-001 and work out what it touches.\" The agent\nreads the ticket from your board, does the work, and writes into the ticket's\ndocuments. You watch the board change while it does.\n\n**Or dispatch it.** Right-click the card → **Dispatch to agent** → your agent →\n**Map files**. That starts a background agent whose only job is to produce the\nfiles document, and it stops when the document exists. The card shows an\n**⏳ agent** chip while it works.\n\nThen dispatch **Write plan + checklist**, or ask for it.\n\nRead what comes back. This is the part that matters: the plan is a document you\nare going to act on, and if it is wrong it is much cheaper to fix now.\n\n## 4. Move it to Preparing\n\nDrag the card. If it refuses, the message says what is missing — see **Why can't\nI move this?**\n\nNote what just happened: you did not move it because you had finished thinking\nabout it, you moved it and Kanmer checked whether you had.\n\n## 5. Implement\n\nDispatch **Execute checklist**, or work it yourself.\n\nAn agent doing this takes the ticket first — the card grows a **⛏** badge with\nthe branch name, so the board shows the work is live and no second agent picks\nit up. It works through the checklist, ticking boxes as it goes, and finishes by\nwriting a post-implementation report and opening a pull request.\n\nYou can watch the boxes tick in the checklist tab.\n\n## 6. Review, merge, verify\n\nMove to Review, read the change against what the plan said it would be, merge.\n\nThen move to **Verifying** — and this is the stage most boards do not have.\nThe change is merged; the question is whether it works. Dispatch **Verify +\nwrite proof**, or do it yourself: run the thing, capture what actually happened,\nand write it into the proof tab.\n\n## 7. Done\n\nMove to Done. It will refuse if there is no proof.\n\n## What you should take from that\n\nThe board did not manage you. At each boundary it asked one question — *have you\ngot the thing this kind of work owes?* — and it asked before letting the ticket\nclaim it had moved on.\n\nAnd every artefact along the way is a file in your project, which is why the\nagent could do most of the work: you and it were reading and writing the same\nboard, not exchanging descriptions of one."
  },
  {
    "id": "stages",
    "title": "The six stages",
    "body": "Every Kanmer board has the same six columns, in the same order, and you cannot\nadd, rename or remove one.\n\n**Backlog → Preparing → Implementing → Review → Verifying → Done**\n\n## What each one means\n\n**Backlog** — captured, not started. Everything you have decided is real but\nhave not begun. A ticket can sit here indefinitely and that is fine; a backlog\nis a record of intent, not a queue of failures.\n\n**Preparing** — everything before the code changes. Finding out how the thing\nworks today, working out which files the change lands in, deciding on an\napproach, writing it down. Research and planning share one stage on purpose:\nthey interleave in practice, and splitting them produced a lot of moves that\ntold nobody anything.\n\n**Implementing** — the code changes, in the ticket's own branch and working\ncopy. This stage ends when the pull request is open, not when the work \"feels\ndone\".\n\n**Review** — before the merge. The change is checked against what the plan said\nit would be. Feedback that is worth doing becomes its own ticket rather than an\nunbounded review thread. Ends at the merge.\n\n**Verifying** — after the merge. The shipped result is checked in the merged\nproduct, and the evidence is written down. This is the column most boards do\nnot have, and it is the one that pays: *merged* and *works* are different facts,\nand a board without a stage between them quietly treats a merge as proof.\n\n**Done** — verified. Records and cleanup happen after the ticket lands here,\nnot before it.\n\n## Why they are fixed\n\nConfigurable stages sound like flexibility and behave like drift. Once each\nboard has its own columns, nothing generalises: an agent's working practices\nhave to be re-taught per project, \"what stage is it in\" stops meaning the same\nthing between two repositories, and every board slowly grows a column that means\n\"we are not sure about this one\".\n\nFixing the six means a ticket in Preparing means the same thing everywhere, so\nyour agents already know how to work your board the first time they see it,\nand so the requirements attached to the boundaries between stages can be shared\nrather than reinvented.\n\nWhat is *not* fixed is what a ticket owes to cross a boundary. That is the\nprofile's job, it is per board and per area, and it is where all the real\nflexibility lives — see **Profiles: what a ticket owes**.\n\n## Backlog is a column like the others\n\nThe first one, always present, with drag and drop and quick-add — and there is\nno second place to look.\n\nKanmer did once give Backlog its own list view as well, on the argument that\nscanning a long queue is a different activity from watching work in flight.\nIt was withdrawn: keeping the same tickets in two places cost more than the\nlist was worth. Filter and search on the board instead.\n\nArchiving is how you retire a backlogged ticket you are not going to do. It\nkeeps the ticket and its documents on disk and searchable in the **Archived**\nview, and takes it off the board. A backlog that is never pruned stops being\nread.\n\n## Moving tickets\n\nDrag a card, use the arrow keys, use the card's right-click menu, or change the\nstage in the editor.\n\nTwo rules apply, and both are covered in **Why can't I move this?**: a ticket\nmay owe documents before it can leave a stage, and a move may cross only one\nguarded boundary at a time. Moving *backwards* is always allowed and never\nasks for anything — you can always undo a move.\n\nThere is no priority field. Order within a column is whatever order you drag\ncards into, and \"what matters now\" is expressed with a horizon group rather than\nwith a number that everyone sets to High."
  },
  {
    "id": "profiles",
    "title": "Profiles: what a ticket owes",
    "body": "The six stages are the same on every board. What differs is how much evidence a\nticket has to produce to move through them — and that is a property of the\nticket, not of the board.\n\nA **profile** is the answer to \"how much does this one owe?\"\n\n## The five that ship\n\n| Profile | Owes |\n|---|---|\n| **fix** | A files document and a plan before leaving Preparing; proof before Done. |\n| **chore** | A plan before leaving Preparing; proof before Done. |\n| **feature** | A linked governing document before leaving Backlog; research, a files document, a plan and a checklist before leaving Preparing; a post-implementation report before Review; proof before Done. |\n| **spike** | Research before Done. Nothing else — a spike's output *is* the finding. |\n| **capture** | Nothing. A recorded observation, not yet work — see below. |\n\nThere is also **custom**, which asks for nothing until you tell it what to ask\nfor, ticket by ticket.\n\nEvery profile except `spike`, `capture` and `custom` additionally requires that\nthe ticket's open questions are resolved at each of its boundaries. That is not\na document — see below.\n\n## Quick capture\n\n`capture` is the profile for the thing you noticed but cannot honestly size\nyet. It needs a title and a body — the observation — and nothing else. You can\nattach evidence, and you can leave it in Backlog forever; nothing will nag you\nabout it, because there is nothing it owes.\n\nWhat makes it more than \"a ticket with no documents\" is the other half: a\ncapture **cannot** leave Backlog, cannot be taken, and is never picked up by an\nautonomous run. It leaves that state only when someone records a decision about\nit — it was a duplicate, it was already fixed, it joins a small batch of fixes,\nit is real work with a real profile now, it stays a capture for now, or it no\nlonger matters. Whichever it is, the decision is written on the ticket.\n\nThat is the point. The failure mode captures prevent is the half-filed ticket:\na one-line observation that looks like planned work, gets picked up by whoever\nis clearing the backlog, and turns out to be nobody's idea of a specification.\nA capture is honest about being an observation until a human says otherwise.\n\nTwo limits worth knowing. The observation lives in the ticket body, so it is\nsearchable like any other ticket — but a capture with an empty title or an empty\nbody is refused, because those two are the whole record. And `capture` is not\nusable as an **area** or board default profile: it is a choice you make about\none observation, not a weight you set for a whole area.\n\n## Choosing one\n\n**`fix` is the default.** A ticket you create without thinking about it is a\n`fix`, and that is the right default: most work is small, and a board whose\ndefault is `feature` teaches people to route around it.\n\nPick the profile on the New ticket dialog, or change it any time in the editor.\nChanging it re-evaluates the gates immediately — a ticket that could not move a\nmoment ago may be free now, and that is a legitimate thing to do when the work\nturns out smaller than you filed it as.\n\nYou can also set a **default profile per area** in **Settings → Profiles**, so\ntickets filed in an area start with the right weight without anyone choosing.\nA ticket's own profile wins over its area's, which wins over the board's.\n\n## The cost of over-filing\n\nThis is the failure mode worth naming. Filing a two-line change as a `feature`\nmeans it owes six documents, and what actually happens is not six good\ndocuments — it is five empty ones written to unblock a move, plus everyone\nlearning that the gates are theatre.\n\nProfiles are load-bearing only if a heavy one is a real signal. Use `fix` freely,\nuse `feature` when the change genuinely deserves research and a plan somebody\nwill read, and change the profile when you were wrong rather than writing\ndocuments nobody needs.\n\nUnder-filing is the cheaper mistake: raise the profile and the missing documents\nare asked for at the next boundary.\n\n## \"questions-resolved\" is not a document\n\nIt is the one requirement that cannot be satisfied by writing a file. It asks\nthat the ticket's `open-questions` document has no unanswered questions left in\nit — specifically, no unticked `- [ ]` lines.\n\nYou satisfy it in one of two ways: **answer the question and tick the box**, or\n**move it under a `## Parked` heading with a reason for deferring**. Anything\nbelow a Parked heading is not counted.\n\nA ticket with no open-questions document at all satisfies it trivially, which is\nthe common case.\n\nThe point is that an unanswered question should block work rather than be\nsilently assumed away at the moment somebody starts coding. Parking is a real\nanswer — \"we decided not to decide, here is why\" is a decision — it just has to\nbe written down as one.\n\n## Changing what profiles ask for\n\n**Settings → Profiles** shows a grid: one row per profile, one column per stage\nboundary, each cell a comma-separated list of what that profile requires there.\nEdit a cell to change what your team owes.\n\nYou can also set each area's default profile here, and the list of proof types\nyour board recognises.\n\nInvalid entries are flagged as you type and Save stays disabled until they are\nfixed. Before saving, Kanmer tells you how many tickets the change will re-gate,\nbecause loosening or tightening a profile takes effect on every existing ticket\nat once.\n\nAdding a whole new profile, or changing which profile is the board's default,\nis not done here — those live in the board's configuration file."
  },
  {
    "id": "gates",
    "title": "Why can't I move this?",
    "body": "You dragged a card and it bounced back with a red banner. There are exactly two\nreasons that happens, and the message tells you which.\n\n## \"…requires …\"\n\nThe ticket owes a document it has not got.\n\n> `API-003 cannot move from \"verifying\" to \"done\": entering Done requires proof\n> (profile \"fix\").`\n\nRead it as: *this profile, at this boundary, wants this*. The fix is to write\nthe thing, or to decide the ticket is not the size you filed it as and change\nits profile.\n\n**Where to look:** open the ticket. At the top of the Ticket tab is a readiness\npanel — the profile, then a row per upcoming boundary with a tick or a circle\nagainst each requirement. Click a requirement and it opens the tab where that\ndocument goes. That panel is the same information as the refusal, except you can\nsee it before you try.\n\nYou can also see it during a drag: columns you cannot drop into are tinted, and\nhovering one tells you what it is waiting for. The card's right-click **Move\nto** menu greys out the stages that would be refused, with the reason.\n\n### If it says \"questions-resolved\"\n\nThat one is not a document, and the message says so:\n\n> `\"questions-resolved\" is not a document: open-questions/ still has unticked\n> \"- [ ]\" lines. Answer them and tick the box, or move them under \"## Parked\n> (explicitly deferred)\" with a reason for deferring.`\n\nDo exactly that. Answer the question and tick it, or park it with a reason.\nParking is a legitimate answer.\n\n### If it says \"governing-doc\"\n\nThe ticket has not been tied to any of your project's own documents — the\nproduct or design writing the work is meant to serve. Link one, or mark that\none still needs writing. Only `feature` asks for this, and only on the way out\nof Backlog.\n\n## \"…crosses N document gates\"\n\nThe second refusal is not about what is missing. It is about how far you tried\nto jump.\n\n> `API-003 cannot move from \"backlog\" to \"done\" in one step: that crosses 3\n> document gates (leaving Backlog, leaving Preparing, entering Review). A single\n> move may cross one. Move one stage at a time — the next is \"preparing\".`\n\nEvery document can exist and this still refuses. That is not a bug, and it is\nthe refusal people find most surprising, so it is worth being plain about why:\nthe stages are meant to be **walked**. Dragging a ticket from Backlog to Done\nrecords that it passed through Preparing, Implementing, Review and Verifying —\nwhich is a claim about work nobody did. A board where that is possible is a\nboard where the columns are decoration.\n\nThe message names the next legal stage. Move there, then move again.\n\n## Two things that never refuse\n\n- **Moving backwards.** Any move to an earlier stage is always allowed and\n  never asks for anything. You can always undo.\n- **Creating a ticket.** A new ticket can be created directly into any stage.\n  Gates guard *movement*, on the assumption that if you file something as\n  already in Review you know what you are claiming.\n\n## A note on the wording\n\nThe GUI banner translates agent-only recovery instructions into the controls you\ncan use: open the ticket's document tabs to add what is missing, then use the\nreadiness panel at the top of the Ticket tab for the complete picture. A\nmulti-stage jump also points you to that panel for the next legal stage.\n\n## When the gate is wrong\n\nSometimes the ticket really is smaller than its profile assumes. Changing the\nprofile is a normal, honest thing to do, and the gates re-evaluate the instant\nyou save it.\n\nWhat is not worth doing is writing an empty document to get past a boundary. The\ngate cannot tell the difference and you will not gain anything — the next person\nto open that ticket, quite possibly you, will find a research document with a\nheading and no research in it."
  },
  {
    "id": "documents",
    "title": "Ticket documents",
    "body": "A ticket is not just a card with a title. Each one carries a set of documents,\nand those documents are the actual work product of thinking about the change —\nwhat you found out, what you decided, and what you can show for it afterwards.\n\nOpen a ticket and you will see a row of tabs. The first is **Ticket** — the\nfields you would expect, title, stage, area, profile, assignee. The rest are\ndocuments.\n\n## The seven\n\n| Tab | What goes in it |\n|---|---|\n| `research` | What you **learned**. How the thing works today, where the surprises are, what the options were. |\n| `files` | What the change **touches**. The surface area: which files change, which files you need to read first, what ripples out. Not findings — locations. |\n| `plan` | The **reasoning**. The approach you chose and why it beat the others, the ordered steps, the risks. |\n| `checklist` | The **executable** distillation of the plan. One tickable box per step, each independently checkable. |\n| `open-questions` | The things nobody has decided yet. These genuinely block the ticket, which is the point of writing them down. |\n| `post-implementation-report` | The author's **claim**, written before the change merges. What was built, what changed, what a reviewer should look at. |\n| `proof` | **Evidence**, gathered after the change lands. Not a description of what was built. |\n\nThe tabs show a dot when a document exists, so you can see at a glance what a\nticket has. The checklist tab additionally shows how many of its boxes are\nticked.\n\n## The distinctions that matter\n\nThree pairs get confused constantly, and each pair exists because the confusion\nis expensive:\n\n- **research vs files.** Research is what you learned; files is where the change\n  lands. A files document full of findings is a second research document, and\n  nobody reads the same thing twice.\n- **plan vs checklist.** The plan is the argument; the checklist is the list of\n  moves. If a checklist line needs a paragraph to explain it, that paragraph\n  belongs in the plan.\n- **report vs proof.** The report is a claim made before merging. Proof is\n  evidence from after. A report that says \"tests pass\" is a claim; proof is the\n  output of the run.\n\n## Which ones a ticket owes\n\nNot all seven, and not the same set for every ticket. That is the profile's job\n— see **Profiles: what a ticket owes**. The readiness panel at the top of the\nTicket tab shows exactly which documents each upcoming boundary is waiting for,\nand clicking a requirement opens the tab where it goes.\n\nA tab you do not need is not a failure. A `chore` that never grows a research\ndocument is a `chore` that did not need one.\n\n## Writing them\n\nClick a tab. If the document does not exist yet you get a **Create** button;\nafter that it is a plain markdown editor that saves as you go. Checkboxes in a\nchecklist are live — tick them in the editor and the file changes underneath.\n\nAgents write into the same documents through the same files. That is the whole\npoint: you are not reading a summary of your agent's work, you are reading the\nwork.\n\n## When two people write at once\n\nBecause you and your agents share the files, you can both be editing when one\nof you saves. Kanmer notices instead of silently losing the loser.\n\nFor a document, you will see a banner saying the file changed since you opened\nit, with **Reload from disk** and **Overwrite anyway**. Your typed text is not\nthrown away either way — reloading shows you what arrived so you can re-apply\nyour change, and overwriting wins deliberately rather than by accident.\n\nFor the ticket's own fields, the banner names which fields collided and offers\n**Keep mine** or **Take theirs**. Fields you did not touch quietly take the\nnewer value, so an agent moving a ticket while you retitle it is not a conflict\nat all."
  },
  {
    "id": "references",
    "title": "Reference files and scratch",
    "body": "Not everything attached to a ticket is a document. Two other things live beside\nthem, and neither can ever tick a box.\n\n## Attachments\n\nScroll down the Ticket tab and you will find a section headed **Attachments**,\nwith the hint *inputs to the work — never satisfy a document gate*.\n\nThis is where you put what you are handing the work: a mockup, an exported\ndesign, a log from the failure you are chasing, a schema, a screenshot of the\nbug, a spreadsheet somebody sent you. Anything, any file type.\n\nAdd one by **dragging it onto the section**, or with the **Add files…** button\nand a normal file picker. Click a listed file to open it in whatever your\nsystem uses for that type. Removing takes two clicks — Kanmer asks before\ndeleting, because these are often the only copy.\n\nIf you add a file whose name is already there, Kanmer keeps both and numbers the\nnew one. Nothing is overwritten silently.\n\nAttachments live in the ticket's own folder, so they travel with it, they are\nversioned with your board, and **your agents can read them**. That last one is\nthe real reason the section exists: \"here is the mockup, build this\" is a\nsentence you can now actually mean.\n\n## Scratch\n\nScratch is the agent's notepad. When an agent is part-way through something and\nwants to leave itself a note — a half-finished trace, a list of files it has\nalready checked, a hypothesis it wants to come back to — it appends to scratch\nrather than putting it in a document that somebody will later read as a finding.\n\nScratch does not appear as a tab in the editor. It is deliberately quiet: notes\nin progress are not deliverables, and a notepad promoted to a document is just\nnoise with a filename. You will see scratch in the ticket's folder if you go\nlooking, and agents can read each other's.\n\n## Neither one satisfies a gate\n\nThis is enforced, not merely discouraged. A ticket that owes a research document\nstill owes it after you attach forty files; a ticket that owes a plan is not\nunblocked by an agent writing a very long scratch note.\n\nThat is deliberate, and it is worth being clear about why. An attachment is an\n**input** — it is what you were given. Scratch is **working**, not conclusions.\nA gate is asking whether somebody has done the thinking and written it down, and\nneither of those things is evidence that they have. If gates accepted them, the\neasiest way to pass would be to attach the thing you were sent, which is exactly\nthe behaviour the gates exist to prevent.\n\nIf you find yourself wanting an attachment to count, the honest move is usually\nto change the profile — see **Profiles: what a ticket owes** — rather than to\nfind a way around it."
  },
  {
    "id": "proof",
    "title": "Proof",
    "body": "Proof is the last thing a ticket owes, and the one requirement that does not\nbend. Everything else on a ticket is somebody's account of the work. Proof is\nwhat you can show.\n\n## Evidence, not description\n\nThe distinction is the whole idea. A post-implementation report says *what was\nbuilt*; it is written by the author, before the change merges, and it is a\nclaim. Proof is gathered **afterwards, from the merged result**, and it is\nevidence.\n\n\"The retry path is covered by tests\" is a claim. The output of the test run,\npasted in, is proof. \"It looks right in dark mode\" is a claim; the screenshot is\nproof. The difference is whether a reader has to trust you.\n\nCompiling is not evidence. Neither is a green tick on a branch that has not\nlanded.\n\n## The three kinds\n\nKanmer ships three proof types, and a ticket's profile can ask for one\nspecifically:\n\n| Type | What it wants |\n|---|---|\n| `visual` | A screenshot or recording of the actual result. Something a reader can look at without running anything. |\n| `test-output` | The output of the run that actually happened — the command, and what it printed. Not a summary of it. |\n| `command-log` | What you ran and what came back, in order. The transcript, for changes whose result is a behaviour rather than a picture. |\n\nA profile can ask for proof in general (`proof`) or for a kind\n(`proof:visual`), and on boards that track deployments it can ask for proof from\na particular environment.\n\nIf your team wants different kinds, the list is editable in **Settings →\nProfiles**.\n\n## What Kanmer checks\n\nThe gate itself is simple: the ticket must have something written under proof\nbefore it can enter Done.\n\nOn top of that, a ticket asked for **visual** proof gets a nudge. If no image\nhas been added under proof, Kanmer says so — *expects a screenshot, but no image\nfiles were found* — and then **lets the move through anyway**, telling you to\ncheck it yourself.\n\nThat is not a loophole; it is honest about what a tool can know. Kanmer can tell\nwhether an image exists. It cannot tell whether the image shows the thing you\nclaimed, and pretending otherwise would train you to satisfy the checker instead\nof the reader. The other two kinds have no automatic check at all, for the same\nreason.\n\n## Why the last gate is the strict one\n\nA ticket in Verifying has merged. The change is in the product. The only\nquestion left is whether it does what it was supposed to do, and Verifying\nexists because \"merged\" and \"works\" are different facts that get conflated the\nmoment there is no column between them.\n\nSkipping proof does not save the work — it just moves the discovery of a problem\nfrom you to whoever hits it next.\n\n## Doing it\n\nMove the ticket to Verifying when the change has merged, gather the evidence\nfrom the merged result, write it into the proof tab, then move to Done. If you\nare dispatching this to an agent, the verify task is deliberately unavailable\nuntil something has merged, and it will tell you so."
  },
  {
    "id": "groups",
    "title": "Areas, epics and horizons",
    "body": "Two ways of clustering tickets, answering two different questions. **Areas**\nanswer *what part of the product is this?* **Groups** answer *what does this\nbelong with?*\n\n## Areas\n\nAn area is a part of your work: the API, the GUI, the docs, the release\nmachinery. Every board starts with one, and you make the rest.\n\nAn area does three things:\n\n- **It colours and clusters cards.** Inside every stage column, cards group\n  under a sub-header for their area, each with its colour. Tickets with no area\n  fall into a bucket at the bottom.\n- **It gives a ticket its id.** A ticket created in an area with the prefix\n  `API` is `API-001`, then `API-002`. That id is a birth certificate, not an\n  address: move the ticket to another area later and the id does not change, so\n  every link to it keeps working.\n- **It can set a default profile**, so tickets filed in an area start owing the\n  right amount without anyone remembering to choose.\n\nEdit them in **Settings → Board**. Each row has a colour swatch, an editable\nname, arrows to reorder, and a delete — which warns you first if tickets are\nstill using it. Add one by typing a name.\n\nThe id and its prefix are shown but not editable, because changing a prefix\nwould orphan every id already issued from it.\n\n## Groups\n\nA group is a set of tickets that belong together for a reason that is not\n\"same part of the codebase\". Two kinds ship:\n\n- An **epic** means *these ship together*. It is a deliverable made of several\n  tickets, and it is done when they are.\n- A **horizon** means *this is what matters now*. It is a time box, not a\n  feature: what this month is about, what the next release contains.\n\nA ticket can be in several groups — typically one epic and one horizon, since\nthose answer different questions.\n\n## What a group holds\n\nMore than a name. A group is a folder with a goal written in it, and any other\ndocuments you add sit beside that goal as **shared context for every ticket in\nthe group** — the brief, the constraint everyone keeps forgetting, the decision\nthat applies to all of it. An agent working any member ticket can read them.\n\nThat is the thing a label cannot do. A label is a word; a group is a place to\nput the paragraph that would otherwise be repeated in nine tickets or, more\nlikely, in none.\n\nProgress is worked out live from the members, so a group always shows the true\nstate of its tickets — including how they are spread across the stages — without\nanyone maintaining a count.\n\n## Working with them\n\nGroup chips appear on cards. Click one and every view filters to that group;\nthe filter bar has an **All groups** dropdown and a button to open the group\nitself, which shows its goal, its shared context, its members and a progress\nbar.\n\nCreating a group is still an agent's job today. Ask your connected agent —\n\"make an epic for the checkout rewrite\" — and the group appears on your board.\nTo put an existing ticket into one, open that card's context menu and choose\n**Add to group**, then select an active group. Existing memberships are kept,\nand a ticket already in a group is shown as unavailable rather than duplicated.\n\nYou can see, filter by, and assign tickets to groups in the app; creating a\ngroup remains an agent action. Open a group’s detail view to archive or\nunarchive it — its ticket memberships are kept when it is archived.\n\nGroups are archived rather than deleted, so a finished epic stops cluttering the\nboard without taking its history with it."
  },
  {
    "id": "dispatch",
    "title": "Dispatching agents",
    "body": "A connected agent can work your board while you talk to it. **Dispatch** is the\nother direction: you point Kanmer at a ticket, choose one job, and it starts a\nbackground agent to do that job and nothing else.\n\n## Choosing a model and adding operator instructions\n\nOpen **Settings → Dispatch** to configure background dispatch on this machine.\nEach dispatchable provider has a default model, optional per-task override, and\nan optional suffix of additional instructions. The task's built-in prompt is\nalways retained; the suffix is appended and cannot replace the ticket workflow.\nLeave the model blank to use the provider CLI default. Model names are opaque,\nlimited to 200 characters, and are not checked against a catalogue. The suffix\nis limited to 4,000 characters. These settings are stored as plaintext in your\nElectron user data, so never enter credentials or secrets.\n\nIf a provider is not shown, Kanmer does not support starting it in the\nbackground. A failed configured model is reported as a failed dispatch; Kanmer\ndoes not retry without the model. Live provider authentication and model\navailability remain host-specific.\n\n## Doing it\n\nRight-click a card → **Dispatch to agent** → pick the agent → pick the task.\n\nFive hosts can be dispatched to: Codex, Claude Code, opencode, Grok CLI and\nAntigravity. Antigravity is launched with `agy --add-dir <sourceRoot> -p\n<prompt>` so each session is bound to the project; cwd alone is never treated as\nbinding. A real Kanmer tool call/deliverable is required for host acceptance;\nprocess start, PONG and tool listings are not sufficient.\n\n## One deliverable, not \"work on this\"\n\nThe task menu is deliberately narrow. Each entry names what it will produce, and\nthe agent stops when that thing exists:\n\n| Task | Produces |\n|---|---|\n| **Research (quick)** | At least one research document |\n| **Deep research** | Several research documents plus a summary |\n| **Map files** | At least one files document |\n| **Write plan + checklist** | A plan and a checklist |\n| **Execute checklist** | The checklist worked through, a post-implementation report, and a pull request open |\n| **Verify + write proof** | At least one proof document |\n\nThere is also **Whole ticket**, which hands the agent the ticket and lets it use\nits own judgement. It is the right choice less often than it looks: a background\nagent with an unbounded goal is a background agent you have to supervise, and\nthe entire benefit of dispatch is not having to.\n\nA narrow task has a done-condition you can check in three seconds. That is what\nmakes it safe to walk away from.\n\n## What the menu tells you before you commit\n\nTwo entries turn themselves off when they cannot work:\n\n- **Execute checklist** is disabled with no plan — *\"no plan yet — dispatch\n  Write plan + checklist first\"*.\n- **Verify + write proof** is disabled until something has merged — *\"nothing is\n  merged yet — verify runs on merged main\"*.\n\nOthers stay available but warn: dispatching a plan with no research says the\nplan will be less grounded; deep research on a ticket that already has research\nsays it will add to it rather than start over. The warning is advice, not a\nrefusal — sometimes you know something the board does not.\n\n## Watching it\n\nA card with a dispatch in flight shows an **⏳ agent** chip.\n\nFor the detail, open the command palette and choose **Show background\ndispatches**. The drawer lists this session's dispatches — the ticket, the\nstate, which agent, which task, and the last few lines of output. **Cancel**\nstops one.\n\nYou get a toast when a dispatch finishes, whichever way it finished: done,\nfailed, cancelled, or timed out. Dispatches time out after thirty minutes, on\nthe grounds that an agent still going after half an hour on one deliverable is\nstuck rather than thorough.\n\nWhen a dispatch ends, a summary — how it exited, how long it took, and the tail\nof its output — is appended to the ticket's scratch notes, so the record\noutlives the session.\n\n## One at a time, and not on a taken ticket\n\nKanmer will not start a second dispatch on a ticket that already has one\nrunning, and it will not dispatch a ticket that somebody has taken. Release it\nfirst. Both rules exist to stop two agents writing the same document.\n\n## Taken tickets and worktrees\n\nAn agent doing real work takes the ticket first: that stamps it with who has it\nand which branch the work is on, and moves it to Implementing. The card shows a\n**⛏** badge with the branch name, so the board tells you what is live.\n\nAgents doing implementation work in their own git worktree, one per ticket, is\nthe working practice Kanmer's skills follow — it is how parallel work avoids\ncolliding. Dispatch itself does not create the worktree; the agent does, as part\nof the execute task.\n\nYou can take and release a ticket yourself from the command palette.\n\n## What your agent will say it is doing\n\nIf you dispatch to an agent that has Kanmer's skills installed, its output will\nname them: `kanmer-research`, `kanmer-plan`, `kanmer-execute`, `kanmer-review`,\n`kanmer-verify`, `kanmer-closeout`, and a few more for board housekeeping.\n\nThey are the working practices behind the stages in this manual — one per job.\nYou do not need to learn them; they are listed here only so that seeing\n`kanmer-plan` scroll past reads as *\"it is writing the plan\"* rather than as\nsomething going wrong.\n\n## Remote MCP dispatch is opt-in\n\nThe MCP server exposes the same bounded contract as three tools:\n`dispatch_task`, `list_dispatches`, and `cancel_dispatch`. It is **disabled by\ndefault**. An operator who deliberately wants a host to launch local agents\nmust configure all of the following in its process environment:\n\n```text\nKANMER_DISPATCH_ENABLED=true\nKANMER_DISPATCH_PROVIDERS=codex,claude\nKANMER_DISPATCH_TASKS=research-quick,files\nKANMER_DISPATCH_MAX_ACTIVE=1\nKANMER_DISPATCH_TIMEOUT_MS=1800000\nKANMER_DISPATCH_APPROVAL=elicit\n```\n\nProvider and task ids are closed allowlists from Kanmer's core registry. The\ncaller cannot provide a command, prompt, working directory, environment, pid,\nor log path. The server checks the project fingerprint, ticket state,\nfeasibility, duplicate/concurrency limits and approval before it creates a\nchild or local log. `elicit` refuses when the host cannot complete approval;\n`preapproved` is an explicit operator decision, not a fallback. Bearer-token\nauthentication proves access to an endpoint but does not authorize process\nlaunch.\n\nRemote list/cancel responses contain only sanitized lifecycle metadata. Raw\noutput and local log paths remain on the host; one bounded terminal summary is\nwritten to ticket scratch, and a scratch failure remains visible as\n`recordingError`. Credentials or a live provider/remote host are not implied by\nthese repository checks."
  },
  {
    "id": "board-sync",
    "title": "Sharing a board over Git",
    "body": "Your board is files, so sharing it is a matter of getting those files onto\nsomeone else's machine. In a Git repository Kanmer does that for you, and it\ndoes it without putting ticket edits in front of your code reviewers.\n\n## The board lives on its own branch\n\nOpen a Git project and Kanmer checks out a second working copy beside your\ncode, in `.worktrees/kanmer`, on a branch of its own. The default name for that\nbranch is `kanmer-board`.\n\nYour project tab still shows your source checkout — you are not working in the\nboard copy and you will rarely look at it. What the separate branch buys you is\nthat moving eight tickets and writing four documents produces commits on\n`kanmer-board`, not on your feature branch. A pull request stays a diff of the\nwork. The board is versioned, shared and reviewable, and it is never noise.\n\nThe worktree path does not change when you rename the branch, so nothing you\nhave open moves under you.\n\n## Turning sync on\n\nSync is **off** until you ask for it. Open **Settings → Git** and tick\n**Automatic sync**; a **Minutes** box appears, and Kanmer syncs on that interval\nfrom then on. Ticking the box starts you at one minute, and any whole number\nfrom one upwards is allowed — there is no fixed list to pick from.\n\nThere is a **Sync now** button for when you would rather not wait, and the hint\nunder it shows where the board worktree is and when it last synced.\n\n## What a sync actually does\n\nIn order: stage the board files, commit them if anything changed, fetch, and\nrebase onto the remote branch. Kanmer never force-pushes and never touches any\nbranch but the board's.\n\nRebasing rather than merging is what keeps the board's history readable — a\nboard that merged every two minutes would be nothing but merge commits.\n\n## When a sync conflicts\n\nTwo people edited the same ticket and Git cannot reconcile it. Kanmer stops:\nit aborts the rebase, keeps your local commits exactly as they were, and pauses\nautomatic sync rather than quietly retrying into the same wall.\n\nYou will see the error Git itself reported, and the **Sync now** button becomes\n**Retry**. Nothing is lost while it is paused — your work is committed locally\non the board branch. Resolve the conflict the way you would resolve any other\none, in `.worktrees/kanmer`, then press **Retry**.\n\nSync stays paused until a sync succeeds, which is deliberate: a paused sync is\nvisible, and a silently failing one is not.\n\nDuring a board-branch handoff, Kanmer also pauses the automatic timer while the\nopen worktree is on the wrong branch. It does not push using the cached branch\nin the background. **Retry** re-inspects the live worktree before it can commit,\nrebase, or push; if the branch is still mismatched, it remains paused and makes\nno Git mutation. Once the worktree reaches the exact requested destination,\nKanmer clears only the handoff-generated pause and message; a real conflict or\npush error remains visible, and **Retry** is still available.\n\n## Renaming the board branch\n\nFor a non-protected board branch, do it from **Settings → Git**, not from the\ncommand line. Type the new name and press **Rename branch**. Kanmer renames the\nbranch in place, so the board keeps its whole history and the worktree path stays\nthe same. It pushes the new name before any cleanup. For a custom-to-custom\nrename, Kanmer cannot update the hosted repository's `KANMER_BOARD_BRANCH`\nvariable, so it retains the old remote ref and shows a warning. Update that\nvariable to the new name first; only then is it safe to delete the old ref.\nThis applies to every custom-to-custom rename, not just the first handoff. The\nold ref is an intentional handoff record, not a failed cleanup; the Actions\nvariable must be updated before an administrator removes it.\n\nThe default `kanmer-board` branch is protected by the repository's merge gate.\nKanmer cannot edit GitHub protection, so it refuses to rename away from that\nbranch automatically. An authorized repository administrator must first push the\ndestination, set the repository Actions variable `KANMER_BOARD_BRANCH` to that\nsame destination, retarget protection and required checks to it, confirm the old\nrule is removed, and rename every local `.worktrees/kanmer` worktree. The bundled\n`.github/workflows/pr.yml` uses `kanmer-board` only when that variable is absent,\nso update the variable before removing the old branch or protection rule. Then\nchange the Kanmer branch setting. Until that handoff is complete, the existing\nboard branch and setting are left untouched.\n\nProjects that were closed when you renamed are migrated the next time you open\nthem.\n\n## Picking the board up elsewhere\n\nThere is nothing to export. Clone the repository on the second machine, open it\nin Kanmer, and the board branch is fetched into its own worktree the same way.\nTurn sync on there too and the two stay level.\n\nOne thing does not travel: the activity log is local to each machine, because\nit is a record of what happened in front of *you*. Tickets, documents,\nreference files and board settings all sync.\n\n## Projects that are not Git repositories\n\nEverything still works — the board is just files in a folder. Kanmer says\nso in **Settings → Git** rather than offering controls that cannot do anything,\nand sharing that board is up to you."
  },
  {
    "id": "sync",
    "title": "Staying in sync",
    "body": "You are not the only one touching this board. An agent researching a ticket in\nthe background writes documents; a dispatched task moves cards. Kanmer's job is\nto make that legible rather than startling.\n\n## The board is live\n\nKanmer watches the board's files. When something changes on disk — an agent\nwriting a plan, a colleague's sync arriving, you editing a file in another\nprogram — the board updates in place. There is no refresh button and there is\nnothing to reload.\n\nUpdates are surgical. A changed ticket repaints that card, not the board. A\ndocument you have open updates without losing what you are typing.\n\n## Your own edits do not echo\n\nWhen *you* move a card, Kanmer does not then notify you that a card moved. It\nknows which changes it just made and ignores the file events they cause.\n\nThis sounds obvious and is the difference between a live board and an\nirritating one: every notification you get is about something you did not do.\n\n## Toasts, when you are watching\n\nWhile Kanmer has focus, a change made by an agent slides in as a small toast —\n*Agent updated TICKET-ID*, or a note that the board's configuration changed.\nClick one to jump straight to the card. They clear themselves after a few\nseconds.\n\nIf you have several projects open, a change in a background project tab puts a\ndot on that tab instead of interrupting you in this one.\n\n## Desktop notifications, when you are not\n\nWhen the Kanmer window is not focused, changes surface as system notifications\ninstead — so you can leave an agent working and go and do something else.\n\nThey are batched: a burst of changes over a second or two arrives as one\nnotification rather than eleven, and a large burst collapses into a count. A\nnotification names the ticket and the stage it moved to; clicking it brings\nKanmer to the front and selects the card.\n\nThis is on by default. Turn it off in **Settings → Appearance**, under\n**Notifications** — the checkbox reads *Toast when an agent changes the board\nwhile the window is unfocused*.\n\n## The activity feed\n\nThe bell in the header opens **Activity**: a running log of what happened to\nthis board, newest first, with a dot when there is something you have not seen.\n\nEach entry says what changed, who changed it, and how long ago — a ticket\ncreated, a stage move with its from and to, a document written or appended, a\nticket taken on a branch or released, a field edited. \"Who\" is the name of the\nagent that did it, or the app itself when it was you.\n\nClicking an entry reveals the ticket it refers to.\n\nThe feed answers the question you actually have when you come back to a machine\nthat has been working without you: not *what does the board look like now*,\nwhich you can see, but *what happened while I was gone*.\n\nTwo things worth knowing about it. It is local to this machine — it does not\ntravel between machines the way tickets do, because it is a record of what\nhappened in front of you. And it is derived: it is safe to delete, and it trims\nitself rather than growing forever."
  },
  {
    "id": "settings",
    "title": "Settings, tab by tab",
    "body": "Settings has several tabs. This is a tour of what is in each, and where to look\nwhen you know what you want to change but not where it lives.\n\n## Board\n\n**Areas.** The parts your work divides into. One row per area, each with a\ncolour swatch, an editable name, arrows to reorder them, and a delete that warns\nyou if tickets still use it. Type a name to add one.\n\nEach area shows its id and the prefix that ticket ids in it are built from.\nNeither is editable, because changing a prefix would orphan every id already\nissued from it.\n\nThis tab also holds a couple of preferences for new tickets, such as which area\nthey default to.\n\nStages are not here, and there is nothing missing: the six stages are the same\non every board and cannot be changed. Priorities are not here either — Kanmer\ndoes not have them. See **The six stages**.\n\n## Profiles\n\nWhere you decide what a ticket owes.\n\nA grid: one row per profile, one column per stage boundary, each cell a\ncomma-separated list of what that profile requires there. Below it, a default\nprofile per area, and the list of proof types your board recognises.\n\nMistakes are flagged as you type and Save stays disabled until they are fixed.\nBefore you save, Kanmer tells you how many existing tickets the change will\nre-gate — profile changes are retroactive, and it is better to know that before\nthan after.\n\nSee **Profiles: what a ticket owes**.\n\n## Appearance\n\nTheme — light, dark, or follow the system.\n\n**Notifications** also live here, which is slightly counter-intuitive if you\nwent looking in Connect. The checkbox reads *Toast when an agent changes the\nboard while the window is unfocused*, and it is on by default. See **Staying in\nsync**.\n\n## Git\n\nOnly useful when the project is a Git repository; Kanmer says so plainly when it\nis not, rather than showing controls that cannot do anything.\n\n- **Kanmer branch** — the name of the branch your board lives on, with a\n  **Rename branch** button. Non-protected branch renames happen in place and\n  keep the board's history. A custom-to-custom rename retains the old remote\n  ref until an administrator updates the repository Actions variable\n  `KANMER_BOARD_BRANCH` to the new branch; remove the old ref only after that\n  handoff. The protected default `kanmer-board` requires an\n  administrator to set the repository Actions variable `KANMER_BOARD_BRANCH`\n  to the destination, retarget GitHub protection and required checks, and\n  rename local board worktrees before the setting can change; Kanmer refuses\n  that automatic rename.\n- **Automatic sync** — off until you turn it on, then every N **minutes**.\n- **Sync now** — sync immediately. It becomes **Retry** if a sync has paused on\n  a conflict.\n\nUnder those, Kanmer shows where the board worktree is and when it last synced,\nand any error from the last attempt. A retained board worktree with an error\nmeans reconciliation failed (not that the project is non-Git); finish the\nhosted handoff and press **Retry**. See **Sharing a board over Git**.\n\n## Connect\n\nOne row per supported agent host, each with a **Connect** button that registers\nthis project's board with that host and installs Kanmer's skills for it.\n\nRows show **Disconnect** once connected, and an **Update skills** button when\nthe copy in your project is older than the one this Kanmer ships. If a\nconnection cannot be made automatically, the row shows you what to run or which\nfile to edit, with a **Copy** button.\n\nSee **Connect an agent**.\n\n## Projects\n\nOne card per project named in the endpoint registry — the same\n`~/.kanmer/endpoints.json` (or `KANMER_ENDPOINT_REGISTRY`) that every Kanmer\nMCP server reports through `list_projects`. Each card shows the project's\nlogical identity, where its board and repository are, which branch the board\nis on and how far ahead of or behind the remote it is, the declared policy\nlabel, how many tickets it holds, and who is working there right now: every\ncontroller holding a live claim with the tickets it holds (an expired claim\nis listed as a workspace but is not an active controller), and each\nworkspace's branch, worktree, claim state and — when the board records one —\nits lease, flagged when its heartbeat has gone stale.\n\nHealth is one of **Healthy**, **No identity yet** (a board that has not had\nits one-time identity migration), **Board missing**, **Invalid entry** (the\nregistry line is malformed; it is shown, never dropped) or **Error**.\n\nOnly the **selected project** — the tab this Settings dialog belongs to — has\ncontrols: rename its entry, set or clear its policy label, or remove it from\nthe registry. If it is not registered yet, name it and press **Add this\nproject**; its locations come from the open tab, never from a typed path, a\nname already in the registry is refused rather than replaced, and the board\nbranch is recorded only when the board is actually under Git. The app\nrefuses a rename, policy change or removal aimed at any endpoint other than\nthe selected project, whatever the window asks for. Every other card is\nobservation only. Its single button, **Open project**, opens that project as\na tab and makes it the selected project — the same path as opening a folder,\nincluding its confirm when a ticket editor has unsaved changes. Unsaved\nchanges on the Board tab block the switch until you discard them, and\nSettings starts afresh for the newly selected project so a draft taken from\none board is never saved into another. **Open project** is disabled for an\nendpoint whose board could not be observed (missing, invalid or in error):\nopening a folder without a board would create a fresh one there, which a\nstale registry entry must never do. Nothing here writes to another project's\nboard or its board branch.\n\nLooking at a project here never changes it: the app reads each board with a\nthrow-away read-only view, and writes to the registry file are serialised and\nrefused if the file was edited by hand in the meantime.\n\n## Things that are not in Settings\n\nWorth knowing so you stop looking:\n\n- **Stages** — fixed at six, by design.\n- **Priority** — does not exist. Use a horizon group and the order of cards in a\n  column.\n- **Adding or removing a profile**, and **which profile is the board's\n  default** — these live in the board's own configuration file rather than in\n  the app.\n- **Creating a group** — ask a connected agent; there is no button for it yet."
  },
  {
    "id": "shortcuts",
    "title": "Keyboard shortcuts",
    "body": "Generated from the app's own binding table, so it cannot drift from what\nthe keys actually do.\n\n| Keys | Does | Where |\n|---|---|---|\n| `Ctrl+K` | Command palette | Anywhere |\n| `Ctrl+N` | New ticket | Anywhere |\n| `Ctrl+F  /  /` | Focus search | Anywhere |\n| `Ctrl+,` | Settings | Anywhere |\n| `F1` | Manual | Anywhere |\n| `Ctrl+1…3` | Switch view (Board, Standup, Archived) | Anywhere |\n| `Ctrl+Tab` | Next project tab (Shift for previous) | Anywhere |\n| `Escape` | Close the topmost panel, else deselect | Anywhere |\n| `← / →` | Move the selected ticket a stage | Board |"
  },
  {
    "id": "remote-access",
    "title": "Remote access",
    "body": "Remote access lets one Kanmer project be reached through an HTTPS MCP endpoint when you are away from the desktop. It is opt-in: the normal local board remains the source of truth, and remote requests still use the same ticket, document, dependency, review, and proof rules. The endpoint is not a general web server and it does not turn Kanmer into a shared multi-user service.\n\n## Remote access\n\nThe endpoint is a Streamable HTTP MCP endpoint ending in `/mcp`. Every request needs a Kanmer bearer token in the `Authorization` header. Use HTTPS for any public endpoint. A running endpoint belongs to one project identity and one process; do not infer the project from a hostname. The desktop and a headless process are separate owners, so stop one before asking the other to take over the same local resources.\n\n## Security model\n\nThe bearer is an application credential in addition to any controls provided by a tunnel service. It is created and stored through protected operating-system storage or a protected token file, shown once, and never placed in a project file, URL, command argument, or diagnostic. Rotation invalidates active sessions. There is no per-user identity in this release: anyone holding the token has the same Kanmer permissions, subject to the board's normal workflow gates.\n\n## Prerequisites\n\nHave a project open in Kanmer, a stable HTTPS hostname, an operator-managed named tunnel, its protected credential reference, and a supported tunnel executable installed separately. The executable and tunnel resources are outside Kanmer's ownership. The project must have a valid board and repo identity. A public route is not considered verified merely because the local listener started.\n\nKanmer does not download an executable, create a provider account, create DNS, create a tunnel, or mint a public hostname. Install and provision those prerequisites through the provider's operator workflow, then enter references—not credential contents—in Kanmer.\n\n## Architecture and terms\n\nThe local HTTP host authenticates and speaks MCP. A tunnel adapter forwards one exact loopback origin to a provider endpoint. The public endpoint is the HTTPS URL a client uses. “Connected” means the local host and adapter are healthy; “verified” means a public doctor run has confirmed DNS, TLS, route, authentication, project identity, tool policy, and session close. A failed or old public result is shown as stale. The project fingerprint is the stable identity used to prevent cross-project use.\n\n## GUI setup\n\nOpen Settings → Connect and select the project. In the Cloudflare Tunnel section, use the fields “cloudflared executable”, “Tunnel id”, “Credentials file”, and “Public hostname”. Check “Enable this project’s Cloudflare remote access” and, if desired, “Start this project automatically when Kanmer opens”; choose “Save configuration”. Registered projects show State, Action, Severity, Board, Listener, Auth, Tunnel, and Remote health. The selected project exposes “Create token”, “Rotate token”, “Start”, “Stop”, “Run doctor”, “Reconcile identity”, and “Remove”.\n\nCreate a token only after saving a valid executable. The one-time dialog is masked by default; “Reveal” and “Copy and dismiss” are available, and the token expires after a short delivery window. Copy it into the external MCP client's secret store. It is not displayed again. “Rotate token” is disabled while the tunnel is active and invalidates older sessions. “Reconcile identity” is the safe recovery when a project folder moved; “Remove” asks for confirmation and removes the project's remote configuration and encrypted bearer.\n\n## Headless setup\n\nThe installed package provides `kanmer-mcp-token <token-file>` for exclusive protected-file creation and `kanmer-doctor config|local|public [--json]`. `kanmer-mcp-remote` takes no positional arguments. It reads protected references from `KANMER_TUNNEL_PROVIDER=cloudflared`, `KANMER_HTTP_TOKEN_FILE`, `KANMER_TUNNEL_HOSTNAME`, `KANMER_CLOUDFLARED_EXECUTABLE`, `KANMER_CLOUDFLARED_TUNNEL_ID`, and `KANMER_CLOUDFLARED_CREDENTIALS_FILE`. It starts the authenticated loopback host, performs local verification, then starts the adapter and emits redacted readiness/status lines. Stop it with the normal process signal and confirm the child and listener have closed.\n\nFor a disposable setup, keep the token file and package under an operator-controlled directory (a path containing spaces is supported), use a placeholder file name, and remove the directory after the run:\n\n```text\nkanmer-mcp-token <protected-token-file>\nkanmer-doctor config --json\nkanmer-mcp-remote\n<normal process signal>\n```\n\nThe token command refuses an existing target rather than overwriting it. The doctor command accepts only `config`, `local`, or `public`, with optional `--json`; invalid flags and raw-token or URL arguments exit with code 2. Record only the mode, exit code, and redacted status. Do not put a real token, hostname, or machine path in a command transcript.\n\n## Configure a remote MCP client\n\nGive the client the HTTPS endpoint ending in `/mcp` and store the bearer separately. Send `Authorization: Bearer <token>` on every request. Never put the token in a query string, URL fragment, cookie, source file, or shell history. Use the client’s ordinary Streamable HTTP MCP transport; client-specific configuration syntax varies and is intentionally not prescribed here.\n\n## Start, stop and auto-start\n\nUse Start only after a token exists. The status surface distinguishes disabled, starting, ready, degraded, and stopped states, with local, tunnel, and public dimensions. Stop closes the authenticated listener before stopping the owned tunnel process. Auto-start is per project and is bounded and deterministic; one project's failure does not prevent other registered projects from being shown. A true Kanmer quit releases desktop ownership. Headless ownership remains separate and must be stopped explicitly.\n\nFor safe cleanup, stop the process first, wait for its redacted stopped status, confirm the child and loopback listener are closed, then remove only the disposable token/package directory. Never kill an unrelated owner or delete a provider credential as cleanup.\n\n## Run connector doctor\n\nRun doctor in `config`, `local`, or `public` mode. The report is schema version 1 and contains ordered checks with pass, warn, fail, or skipped status, severity, safe details, and an optional repair. A prerequisite failure causes dependent checks to be skipped rather than run blindly. Exit 0 means the selected checks are healthy, exit 1 means a check found a problem, and exit 2 means the check could not complete. JSON and human output carry the same safe facts.\n\nUse the [troubleshooting matrix](remote-access-troubleshooting.md#doctor-checks) to interpret each check. Provider-specific setup is isolated in the [Cloudflare named-tunnel appendix](providers/cloudflared.md#supported-named-tunnel-mode).\n\n## Rotate or recover a token\n\nIf a token may have leaked, rotate it immediately and replace it in the client. Existing sessions are invalidated, so reconnect after rotation. If the one-time delivery expires before copying, create or rotate again. Do not search project files or logs for a token. If the desktop no longer recognises a moved project, use Reconcile identity and verify the displayed fingerprint before starting it.\n\n## Move or remove a project\n\nProject registration is keyed by the project fingerprint, not by a hostname. Move the project through the normal project picker, inspect the fingerprint, and reconcile the registration. Remove only when the project should no longer expose remote access; the confirmation removes its encrypted bearer and saved Cloudflare references. Removing a registration does not delete provider accounts, DNS, tunnels, credentials, or executables.\n\n## Security and limitations\n\nThe current provider path is a locally managed named Cloudflare Tunnel plus mandatory Kanmer bearer authentication. Kanmer does not create accounts or DNS, provision tunnel resources, download executables, use Access as a replacement for the bearer, support Quick Tunnels as production infrastructure, accept remote-managed tunnel tokens, host Kanmer in a Worker, offer OAuth or per-user identity, or provide an always-on service. Background dispatch is a separate opt-in policy boundary: bearer possession alone never authorizes it, and malformed/missing policy refuses before launch. A disposable Worker is an external client proof only; public success is downstream evidence and must not be inferred from local readiness. Never bypass TLS validation, bind broadly, share raw logs, force ownership, or retry an unsafe configuration blindly."
  },
  {
    "id": "remote-access-troubleshooting",
    "title": "Remote-access troubleshooting",
    "body": "Use the connector doctor before changing configuration. Run the narrowest mode that answers the question: `config` checks inputs, `local` checks the authenticated loopback path, and `public` adds DNS, TLS, and route checks. A skipped check means a prerequisite failed; it is not a pass. Repairs are bounded guidance, not automatic mutation. Never publish bearer values, provider credentials, session identifiers, raw child output, or machine-specific paths. See [remote access](remote-access.md#run-connector-doctor) for invocation and [the Cloudflare appendix](providers/cloudflared.md#public-verification-and-rollback) for provider-only repairs.\n\n## Doctor checks\n\nThe matrix records the safe facts an operator may compare. `Observed` means the redacted field or state to capture; `Expected` means the contract value. Status and logs are the doctor report, GUI health panel, or bounded child-status line—not raw process output.\n\n| Check | Mode / layer | Pass condition | Safe observed / expected | Likely causes | Ordered repair | Rerun | Stop or escalate |\n|---|---|---|---|---|---|---|---|\n| `PROJECT_CONFIG_VALID` | config, local, public / project | Selected project identity and board configuration are readable. | Observed: project id, board-read status. Expected: one selected project and readable board. | Wrong project, missing board, stale registration. | 1. Open intended project. 2. Repair board through normal Kanmer flow. 3. Reconcile identity. | `config` | Stop if identity is ambiguous or board reads mutate; escalate with ids only. |\n| `REMOTE_CONFIG_VALID` | config, local, public / remote | One supported provider and complete remote fields are present. | Observed: provider name, field-presence booleans. Expected: `cloudflared` and all required fields present. | Missing field, unsupported provider, malformed hostname. | 1. Correct the profile. 2. Save. 3. Re-run config. | `config` | Stop on an unknown provider or arbitrary URL; escalate the profile name. |\n| `SECRET_REFERENCE_VALID` | config, local, public / secret | Token reference is protected, regular, non-symlinked, and readable by the owner. | Observed: reference kind, protection result. Expected: protected regular file or OS secret; never token text. | Missing file, weak permissions, symlink, wrong owner. | 1. Recreate with `kanmer-mcp-token`. 2. Restrict permissions. 3. Re-run config. | `config` | Stop if secret contents appear in output; rotate and escalate. |\n| `TUNNEL_EXECUTABLE_VALID` | config, local, public / provider | Configured executable is absolute, present, and reports a supported version. | Observed: basename, version accepted/rejected. Expected: operator-installed supported executable. | Not installed, wrong path, unsupported version, shell wrapper. | 1. Install from the provider channel. 2. Select the executable. 3. Re-run config. | `config` | Stop on an executable that requires shell interpolation or self-updates. |\n| `TUNNEL_CONFIG_VALID` | config, local, public / provider | Tunnel id, credential reference, hostname, and loopback origin form one exact route. | Observed: UUID-shape result, field-presence, origin host/port. Expected: one id, protected credential, exact hostname, loopback `/mcp`. | Mismatched tunnel, broad origin, missing credential. | 1. Correct named-tunnel fields. 2. Validate the loopback origin. 3. Re-run config. | `config` | Stop on wildcard binds, redirects, or a credential copied into project data. |\n| `LOCAL_STATUS_READY` | local, public / local | Local host reports ready within its bounded deadline. | Observed: state and elapsed bucket. Expected: `ready` before deadline. | Host not started, stale child, port conflict. | 1. Inspect bounded status. 2. Stop stale children. 3. Start once. | `local` | Escalate after the bounded deadline or repeated conflicting owners. |\n| `LOCAL_BIND_LOOPBACK` | local, public / local | Listener is bound only to loopback. | Observed: normalized bind host. Expected: `127.0.0.1` or `::1`. | Broad bind, inherited unsafe configuration. | 1. Remove broad bind. 2. Restart the local host. | `local` | Stop immediately if a non-loopback address is observed. |\n| `AUTH_MISSING_REJECTED` | local, public / auth | Request without bearer is rejected before MCP work. | Observed: HTTP status and challenge presence. Expected: protected rejection, no tool result. | Client omitted header, route stripped auth. | 1. Check client header injection. 2. Check tunnel forwarding. | `local` or `public` | Stop if missing auth reaches MCP or returns a redirect. |\n| `AUTH_WRONG_REJECTED` | local, public / auth | Incorrect bearer is rejected before MCP work. | Observed: HTTP status only. Expected: same protected rejection as missing auth. | Stale client token, verifier misconfiguration. | 1. Replace client secret from protected store. 2. Rotate if uncertain. | `local` or `public` | Rotate and escalate if a wrong token is accepted. |\n| `AUTH_VALID_ACCEPTED` | local, public / auth | Correct bearer reaches the authenticated host. | Observed: accepted status and redacted token id. Expected: authenticated request, no token value. | Token file unreadable, expired generation, header loss. | 1. Validate reference. 2. Recreate or rotate. 3. Retry once. | `local` or `public` | Stop after an unexpected acceptance or repeated generation mismatch. |\n| `MCP_INITIALIZE_LOCAL` | local, public / MCP | Normal MCP initialize succeeds locally. | Observed: protocol result and server name/version. Expected: initialize response from selected project. | Wrong endpoint, unsupported transport, local host unhealthy. | 1. Use Streamable HTTP `/mcp`. 2. Confirm bearer. 3. Re-run local. | `local` | Escalate protocol errors with version and status, never raw payloads. |\n| `PROJECT_FINGERPRINT_LOCAL` | local, public / identity | Local response matches selected project fingerprint. | Observed: redacted fingerprint prefix. Expected: selected project fingerprint. | Moved project, stale registration, wrong root. | 1. Reconcile identity. 2. Verify displayed fingerprint. 3. Restart. | `local` | Stop if fingerprints differ after reconciliation. |\n| `REMOTE_TOOL_POLICY_LOCAL` | local, public / policy | Local tool surface and workflow gates are intact. | Observed: tool-count and policy status. Expected: canonical read/write surface and normal gates. | Wrong server build, stale artifact, bypass attempt. | 1. Confirm server identity. 2. Refresh the shipped artifact. 3. Re-run local. | `local` | Escalate any unexpected tool or gate change; do not bypass it. |\n| `SESSION_CLOSE_LOCAL` | local, public / session | Local MCP session closes cleanly. | Observed: close status and active-session count bucket. Expected: closed and no leaked session. | Client crash, cleanup race, stale process. | 1. Stop stale client. 2. Re-run local once. | `local` | Escalate repeated leaks with session ids omitted. |\n| `TUNNEL_PROCESS_READY` | local, public / provider | Owned tunnel child reaches bounded readiness. | Observed: provider state and attempt number. Expected: `connected` or ready within bound. | Executable, credential, hostname, or local origin failure. | 1. Run config. 2. Run local. 3. Inspect bounded provider status. | `local` | Stop on terminal provider failure; do not blind-retry. |\n| `PUBLIC_DNS_RESOLVES` | public / DNS | Configured hostname resolves without changing records. | Observed: address-family counts. Expected: at least one address for exact hostname. | Missing DNS, stale record, wrong hostname. | 1. Repair DNS through provider operator. 2. Wait for propagation. 3. Re-run public. | `public` | Escalate with hostname redacted if records are unexpected. |\n| `PUBLIC_TLS_VALID` | public / TLS | Certificate is valid for the exact hostname. | Observed: protocol, issuer name, expiry bucket. Expected: valid certificate and hostname match. | Wrong cert, expired cert, hostname mismatch. | 1. Repair provider certificate/hostname chain. 2. Re-run public. | `public` | Never disable verification; stop and escalate certificate errors. |\n| `PUBLIC_ROUTE_NO_REDIRECT` | public / route | HTTPS reaches `/mcp` without an identity redirect. | Observed: status and redirect-present boolean. Expected: no redirect to a login or unrelated host. | Access layer, wrong ingress, path rewrite. | 1. Remove unrelated redirect. 2. Keep exact `/mcp` route. 3. Re-run public. | `public` | Stop on an identity redirect or broad public route. |\n| `AUTH_MISSING_PUBLIC_REJECTED` | public / auth | Public route rejects a missing bearer. | Observed: status and challenge-present boolean. Expected: protected rejection before MCP. | Tunnel strips header, route bypasses local auth. | 1. Check ingress forwarding. 2. Confirm bearer enforcement. 3. Re-run public. | `public` | Stop if public requests reach MCP without bearer. |\n| `MCP_INITIALIZE_PUBLIC` | public / MCP | Public MCP initialize succeeds with bearer. | Observed: protocol status and server version. Expected: same protocol as local. | Route mismatch, client transport, provider readiness. | 1. Fix route/client. 2. Re-run local. 3. Re-run public. | `public` | Escalate after local passes but public initialize fails. |\n| `PROJECT_FINGERPRINT_PUBLIC` | public / identity | Public identity equals local selected project. | Observed: redacted public and local prefixes. Expected: exact equality. | Wrong tunnel target, stale project, cross-project route. | 1. Stop route. 2. Correct project selection. 3. Reconcile and rerun. | `public` | Stop immediately on a mismatch; never continue serving. |\n| `REMOTE_TOOL_POLICY_PUBLIC` | public / policy | Public tools retain local policy and gates. | Observed: tool-count and policy status. Expected: exact local parity. | Different server artifact, proxy mutation, wrong origin. | 1. Compare server identity. 2. Correct origin. 3. Re-run public. | `public` | Stop and escalate any mismatch; do not grant extra access. |\n| `SESSION_CLOSE_PUBLIC` | public / session | Public MCP session closes cleanly. | Observed: close status and count bucket. Expected: closed with no leaked public session. | Proxy timeout, client crash, provider cleanup. | 1. Stop stale client. 2. Verify local close. 3. Re-run public. | `public` | Escalate repeated leaks without sharing session identifiers. |\n| `LOCAL_PUBLIC_CONSISTENT` | public / consistency | Local and public health describe one endpoint and generation. | Observed: redacted generation/fingerprint prefixes and state tuple. Expected: equal project and compatible states. | Stale verification, replacement in progress, split ownership. | 1. Stop. 2. Reconcile identity. 3. Restart and verify local then public. | `public` | Keep endpoint disabled until consistency passes. |\n| `DIAGNOSTIC_REDACTION` | config, local, public / safety | Report details contain no secret, credential, session, or unsafe path material. | Observed: allowlisted detail keys and redaction status. Expected: safe fields only. | Raw child output, token leakage, path interpolation. | 1. Remove exposed diagnostic. 2. Rotate bearer. 3. Preserve only redacted summary. | same mode | Stop distribution immediately and escalate as a security incident. |\n| `NO_BOARD_MUTATION` | config, local, public / safety | Doctor creates, moves, edits, or archives no board content. | Observed: before/after board fingerprint and mutation flag. Expected: unchanged. | Wrong root, write-capable diagnostic, unexpected server behavior. | 1. Stop doctor and endpoint. 2. Preserve redacted evidence. 3. Escalate. | same mode | Never rerun blindly after a mutation. |\n\nThe order is deliberate: later checks depend on earlier checks. A `skipped` result names the prerequisite that failed. Human output is convenient for an operator; `--json` is the stable integration form. Exit code 0 is healthy, 1 reports a failed or warned check, and 2 means the doctor itself could not complete. The report is schema version 1; safe detail fields are allowlisted and raw child output is not a repair input.\n\n## Safe escalation\n\nKeep the endpoint disabled while configuration is invalid. Do not solve a hostname error by disabling TLS, widening the bind address, putting a token in a URL, using a Quick Tunnel for production, or forcing another process's ownership. Capture only the mode, check id, status, safe summary, and redacted repair code. Provider-specific actions belong in the Cloudflare appendix; generic client or project problems belong with the project owner."
  },
  {
    "id": "cloudflared",
    "title": "Cloudflare named-tunnel appendix",
    "body": "Cloudflared is the first supported tunnel adapter. This appendix covers a locally managed named Cloudflare Tunnel whose credential file and stable hostname are provisioned by the operator outside Kanmer. Kanmer starts an already installed executable, forwards one exact loopback MCP origin, and reports bounded health; it does not create provider resources.\n\n## Supported named-tunnel mode\n\nProvide an absolute `cloudflared` executable, a UUID-shaped tunnel id, a protected credentials-file reference, and one exact public hostname. The generated route maps that hostname to the loopback origin and ends with a terminal not-found rule. Cloudflared preserves the public request path, so a public `/mcp` request reaches the authenticated loopback `/mcp` endpoint without embedding `/mcp` in the provider's `service` URL. The child runs without shell interpolation and without automatic updates. Readiness and metrics remain local and bounded. The credentials file is validated before start and is never copied into project data or diagnostics.\n\n## Operator provisioning\n\nInstall the supported executable using the provider's normal distribution channel and verify its version yourself. Create the named tunnel, credential file, and DNS record through the provider's operator workflow. Enter only the references and values in Kanmer's Cloudflare Tunnel fields. Account identifiers, credential JSON, real hostnames, and provider tokens do not belong in a manual, ticket, or proof.\n\n## Start, readiness and replacement\n\nSave the exact fields, create a Kanmer bearer, and run the config and local doctor checks before starting. Kanmer performs an authenticated local initialize and session close before spawning the provider. A provider readiness failure leaves the local diagnostics surface available and reports the provider as failed or degraded. Stop before changing the tunnel id, credential reference, executable, or hostname. Replace an executable or credential through the operator process, validate it, and rerun doctor before restarting.\n\n## Public verification and rollback\n\nPublic doctor mode checks DNS, certificate, redirects, bearer rejection, MCP initialize, fingerprint, tool policy, session close, and local/public consistency. Local readiness is not public verification. If a replacement fails, stop the child, restore the last known-good operator-managed executable or credential reference, and rerun config/local doctor. Do not roll back by weakening TLS or widening the route.\n\n## Boundaries\n\nCloudflare Access does not replace the Kanmer bearer. Kanmer does not automate account or DNS changes, create or delete tunnels, download or update the executable, accept remote-managed tunnel-token mode, use Quick Tunnels as production infrastructure, or host Kanmer in a Worker. A disposable Worker can be an external integration client for a separately recorded acceptance proof; it is never a Kanmer server or proxy."
  },
  {
    "id": "updates",
    "title": "Keeping Kanmer up to date",
    "body": "Kanmer keeps itself current. You do not need to check a website or download\nanything after the first install.\n\n## How it works\n\nKanmer looks for a new version shortly after it starts, and every few hours\nafter that. When it finds one it downloads it in the background and tells you\nwhen it is ready — there is nothing to click to start it.\n\nYou can also check whenever you like: **Help → Check for Updates…**. A manual\ncheck tells you either that a download has started or that you are already up to\ndate. Automatic checks stay silent unless there is something to say, which is\nthe right behaviour for something that runs every few hours.\n\n## Installing it\n\nWhen a version is downloaded you get a banner: *Kanmer x.y.z is ready to\ninstall*, with two buttons.\n\n- **Restart now** closes Kanmer, installs, and reopens.\n- **Later** dismisses the banner. The update installs the next time you quit\n  Kanmer anyway, so \"Later\" means \"not this second\", not \"not at all\".\n\nIf you press Later and then quit, Kanmer asks once more before it installs, with\nthe option to quit without installing.\n\n## Why it asks before restarting\n\nBecause restarting can cost you something, and Kanmer checks what before it\ninterrupts you.\n\nIf you have unsaved changes in a ticket, or if agents are connected to this\nKanmer right now, the confirmation names exactly what is at stake — which ticket\nhas unsaved edits, and how many agent connections will close — before you commit\nto it.\n\n## Agent connections close on update\n\nThis is the one consequence worth understanding.\n\nWhen an agent is connected to a project, the thing serving your board to that\nagent **is the installed Kanmer**. So the update cannot replace Kanmer while\nthose connections are open, and Kanmer closes them itself as part of installing.\n\nYour board is not at risk — it is files on disk, and they are untouched. What\ndrops is the agent's live connection. After the update, **restart the agent**\nand it reconnects; an agent left running holds the old version and keeps reading\nyour board with the old code.\n\nIf Kanmer cannot confirm the connections closed, it refuses to install rather\nthan risk a half-replaced application. The download is kept and it tries again\nnext time. A refused update is a safe update.\n\n## Going back a version\n\nThere is no rollback inside Kanmer. It will not install an older version over a\nnewer one, and there is no menu item for it.\n\nIf you genuinely need to go back, download the older installer from the releases\npage and run it — the same manual route as a first install, SmartScreen warning\nand all. Your board is unaffected either way: it is files in your project, not\nstate inside the app."
  },
  {
    "id": "troubleshooting",
    "title": "Troubleshooting",
    "body": "Things that go wrong, and what they mean.\n\n## A move is refused\n\nThat has its own chapter, because it is the most common thing to be stuck on and\nthere are exactly two causes. See **Why can't I move this?**\n\n## An attachment does not satisfy a gate\n\nIt never will, by design. See **Reference files and scratch**.\n\n## The board is empty\n\nCheck you opened the project you meant to — the one containing the board. Kanmer\nremembers recent projects, and it is easy to reopen a neighbour.\n\nIf the project is new, an empty board is correct: the board's folder is created\nthe first time something is written to it. Make a ticket and it appears.\n\nIf the project is a Git repository and the board was working yesterday, look at\n**Settings → Git**. The board lives on its own branch in a separate working copy\nand Kanmer finds it for you, but if something has happened to that branch —\ndeleted locally, renamed outside the app — that tab is where it will say so.\n\n## A ticket will not open\n\nAlmost always a file that was edited outside Kanmer and is no longer valid — a\nbroken frontmatter block at the top of the ticket's markdown file, usually a\nstray quote or a bad indent. Open the file and look at the block between the\n`---` lines.\n\n## An agent cannot see the board\n\nIn order of likelihood:\n\n1. **The agent was already running when you connected it.** It started before\n   the registration existed. Quit it and start it again in the same project.\n2. **It was never connected for this project.** Registration is per project.\n   **Settings → Connect**, press Connect on your host.\n3. **Kanmer updated while the agent was running.** An update closes agent\n   connections deliberately — see **Keeping Kanmer up to date** — and the agent\n   does not reconnect by itself. Restart it.\n4. **You are using Codex and have not trusted the folder.** Codex only reads a\n   project's configuration for folders you have trusted. Kanmer warns you about\n   this when it can tell.\n\n## An agent is working on the wrong project\n\nRegistration is per project and so is the board it points at. If you have\nseveral projects open, check the agent was started in the folder you think it\nwas — an agent's connection follows the directory it launched in, not the tab\nyou are looking at.\n\n## Automatic sync is not syncing\n\n**Settings → Git**: sync is off until you tick **Automatic sync**, so a board\nthat has never synced usually just has not been asked to.\n\nIf it *was* syncing and stopped, look for an error on that tab and a **Retry**\nbutton where **Sync now** used to be. That means a sync hit a conflict and\npaused. Nothing is lost — your work is committed on the board branch — but it\nwill stay paused until you resolve the conflict and retry. See **Sharing a board\nover Git**.\n\nIf the board branch is being handed off, the timer also stays stopped while the\nopen worktree is on the wrong branch. Kanmer never uses the cached branch for an\nautomatic push; after the exact destination is observed, only the generated\nhandoff pause is cleared. A genuine sync error remains, with **Retry** available.\n\n## Two people edited the same thing\n\nKanmer tells you rather than picking a winner.\n\nOn a document you get **Reload from disk** or **Overwrite anyway**; your typed\ntext survives either way, so reload, look at what arrived, and re-apply your\nchange if it still applies.\n\nOn the ticket's fields you get **Keep mine** or **Take theirs**, and only the\nfields that actually collided are in question — an agent moving a ticket while\nyou retitle it is not a conflict at all.\n\n## Renaming the board branch\n\nFor a non-protected branch, do it from **Settings → Git**, not from the command\nline. Renaming there keeps the board's history and its working copy, and pushes\nthe new name before any cleanup. A custom-to-custom rename retains the old\nremote ref and warns you to update the hosted `KANMER_BOARD_BRANCH` variable\nfirst; delete the old ref only after that variable points at the new branch.\nThis retained-ref rule applies to every custom-to-custom rename. If Settings\nshows a board worktree together with an error while Git is marked unavailable,\nthe project is a failed reconciliation, not a non-Git project: complete the\nhandoff and press **Retry**.\nFor the protected default, Kanmer deliberately refuses this automatic rename.\nAn administrator must push the destination, set the repository Actions variable\n`KANMER_BOARD_BRANCH` to the destination, retarget protection and required\nchecks, remove the old rule, and rename each local board worktree. Only after\nthat handoff is complete should the Kanmer setting be changed; closed projects\nreconcile when they next open against the exact destination.\n\n## I cannot find the setting for stages, or priorities\n\nNeither exists. The six stages are the same on every board and cannot be\nchanged; priority was removed. **Settings, tab by tab** has a short list of the\nother things people go looking for and do not find.\n\n## An update will not install\n\nIf Kanmer cannot confirm that connected agents have disconnected, it refuses to\ninstall rather than risk replacing itself while part of it is in use. Close your\nagents and quit Kanmer; the download is kept and it will install then.\n\n## Something else\n\nThe board is files. If Kanmer is confusing you about what state something is in,\nopen the ticket's folder and read it — that is the actual truth, and there is no\nhidden state anywhere else."
  },
  {
    "id": "glossary",
    "title": "Glossary",
    "body": "One line each, so the rest of the manual can use a word without stopping to\ndefine it.\n\n**Agent** — a coding assistant you work with. Connect one and it reads and\nchanges your board directly, through the same files you do.\n\n**Area** — a part of your work: the API, the GUI, the docs. Colours and clusters\ncards inside each column, and supplies the prefix on ticket ids.\n\n**Attachment** — a file you hand the work: a mockup, a log, a spec. Lives on the\nticket, readable by agents, and never satisfies a gate.\n\n**Board** — your tickets and their settings, stored as files inside the project.\n\n**Board branch** — in a Git project, the branch your board lives on, checked out\nseparately so ticket edits stay out of your code reviews.\n\n**Boundary** — the line between two stages. What a ticket owes is attached to\nboundaries, not to stages.\n\n**Card** — a ticket as it appears on the board.\n\n**Checklist** — the tickable distillation of a plan; one box per step.\n\n**Dispatch** — starting a background agent on one named deliverable for one\nticket, and nothing else.\n\n**Document** — one of the seven pieces of writing a ticket can carry: research,\nfiles, plan, checklist, open questions, post-implementation report, proof.\n\n**Epic** — a group meaning *these ship together*.\n\n**Gate** — the check at a boundary. It asks whether the ticket has what its\nprofile says it owes, and refuses the move if not.\n\n**Governing document** — one of your project's own product or design documents,\nlinked to a ticket so the work is tied to what it is meant to serve.\n\n**Group** — a set of tickets that belong together, holding a shared goal and\nshared context. Two kinds ship: epic and horizon.\n\n**Horizon** — a group meaning *this is what matters now*. A time box rather than\na feature.\n\n**Integration branch** — the branch a project's ordinary pull requests target\nand its ordinary verification proves. Most projects use `main`, which is what\nKanmer assumes when a project says nothing; a project that develops on `dev` and\nreleases from `main` declares that instead, and every worker then gets the right\nbase branch, pull-request target and verification target without being told.\n\n**Delivery state** — how far a change has actually travelled: not integrated,\nintegrated into a branch at an exact commit, included in a release candidate,\nreleased under a branch and tag, deployed, production verified. It is recorded\nseparately from the workflow stage on purpose. A ticket is Done when it has been\naccepted against its integration target — it does not sit waiting for the next\nproduction release — and its inclusion in that release is recorded here\nafterwards. Delivery state never opens a gate: recording a release can never\nstand in for proof.\n\n**Lease** — an agent's renewable hold on a ticket's workspace (its branch and\nworktree). It expires unless renewed, so a dead agent's ticket can be taken\nover — with its work kept — instead of staying taken forever.\n\n**Release channel** — a named stream a project releases on, usually its release\nbranch. Exactly one release owns a channel at a time, held by a renewable lease\nthat behaves like a ticket's: it expires unless renewed, and an expired one is\nreclaimed rather than quietly taken. Names are normalized for Windows, corrupt\nownership fails closed, and a short-lived journal finishes an interrupted\nmulti-file update before another owner can act. A second owner is refused.\n\n**Release attempt** — the durable record of one release on a channel: which\ncommit it was cut from, its candidate identity, the branch and tag it produced,\nthe pull requests and tickets it includes, its artifacts and its verification\nstate. Its identity can never be edited, and a finished attempt is frozen — so a\nfailed release keeps its exact evidence instead of being tidied away. The record\nalso binds the delivery-policy version used when its integration commit was\nresolved; a policy change during collection refuses the mint.\n\n**Candidate identity** — the name a release attempt gives the exact commit it is\nreleasing. It is derived from that commit, so remediating at a different commit\nnecessarily produces a *different* candidate — which is what stops evidence\ngathered for the first one from being read as evidence for the second. A release\nthat finishes clears its channel; one that is replaced records its causal\npredecessor/successor rather than relying on wall-clock timestamps.\n\n**Batch workspace** — a deliberate exception to \"one ticket, one workspace\":\ntwo or more small related tickets that share one branch, worktree and pull\nrequest. The batch is frozen when its first member is taken, no other ticket\ncan join it or use its workspace, each member still gets its own review and\nproof, and cleanup waits until every member is finished.\n\n**Profile** — how much evidence a ticket owes. `fix` is the default; `chore`,\n`feature`, `spike` and `custom` are the others.\n\n**Proof** — evidence from the merged result, gathered after the change lands.\nNot a description of what was built.\n\n**Scratch** — an agent's notepad on a ticket. Working notes, not conclusions,\nand never satisfies a gate.\n\n**Stage** — one of the six fixed columns: Backlog, Preparing, Implementing,\nReview, Verifying, Done.\n\n**Sync** — pulling and pushing the board branch so a shared board stays level\nacross machines and people.\n\n**Taken** — a ticket somebody or something is actively working, stamped with who\nhas it and which branch. Shown as a **⛏** badge on the card.\n\n**Ticket** — a unit of work. A folder on disk, with an id, some fields, and its\ndocuments.\n\n**Verifying** — the stage between merged and done, where the shipped result is\nchecked and the proof is written.\n\n**Worktree** — a second working copy of a repository on a different branch. Used\nfor the board, and by agents so parallel work does not collide."
  }
]);
