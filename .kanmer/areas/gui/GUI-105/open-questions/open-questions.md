# Open questions — GUI-105

No unresolved questions. The existing core inventory and FRD-003 define the required behavior.

## Parked (explicitly deferred)

- Create/rename/delete operations for arbitrary document paths are separate UX work.
