# Files

- `apps/gui/src/main/index.ts` — guard automatic sync with a live board-branch inspection.
- `apps/gui/src/main/syncBranch.ts` and focused GUI tests — pure branch-state decision and regression coverage.
