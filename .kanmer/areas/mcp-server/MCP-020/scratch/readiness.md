2026-08-21 execution readiness check: do not take MCP-020 yet. Its approved plan’s Failure and deviation rules require stopping (or landing only after) [[MCP-022]] if `expected_project` helpers are unavailable; MCP-022 remains Preparing. A source audit confirms no `expected_project`/`expectedProject`, `WRONG_PROJECT`, `failCoded`, `KanmerError`, `DispatchSupervisor`, or shared dispatch-provider registry exists in the current core/MCP/GUI source. Implementing the three remote process-launch tools without that prerequisite would create the prohibited competing project-token/authorization path. Keep MCP-020 Preparing until MCP-022 is shipped, then re-run the plan against its merged API.
