# Open questions — GUI-068

No unresolved product questions. The run uses the earliest reproducible published version pair whose starting build contains GUI-064.

## Parked (explicitly deferred)

- Moving the MCP runtime outside the installation directory is separate architecture work.
