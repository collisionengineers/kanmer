2026-08-22: Built core/MCP artifacts (npm run build exit 0). Pre-build launcher/npm runs exit 1 at 78/80 because generated packages/core/dist was absent; this first failure is preserved. After build, direct launcher, npm run test:scripts, and Git Bash npm run test:scripts each exit 0 with 80/80. PR #145's Node 20/Git Bash literal-glob exit 1 remains the authoritative pre-fix reproduction; local Node 24 cannot reproduce the glob failure.

2026-08-22: Focused rails recorded: npm run typecheck exit 0; npm run test:scripts 80/80 exit 0; git diff --check exit 0. Shared npm run verify exit 1 at mcpb:check because @anthropic-ai/mcpb dist/cli/cli.js is absent; preceding build, core 263/263, GUI 352/352, HTTP 61/61, scripts 80/80, typecheck, protocol smoke 224/224 and headless smoke passed. No dependency or unrelated fix absorbed.
