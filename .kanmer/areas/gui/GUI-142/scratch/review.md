---
kind: review-attestation
pr: "281"
head_sha: "cd4c241ae4b6c2189cfc83da850b001bb9a5316f"
verdict: needs-changes
reviewer: "independent-reviewer-gpt-5.6"
independent: true
plan_hash: "dbf90ad24d76b31b"
ticket_updated: "2026-08-26T11:23:02.709Z"
findings:
  - id: REV-GUI142-001
    severity: blocker
    summary: "The Kanmer CI gate could not find GUI-142 on the remote board."
    disposition: fixed
  - id: REV-GUI142-002
    severity: major
    summary: "The probe did not propagate a non-zero launcher exit status."
    disposition: fixed
  - id: REV-GUI142-003
    severity: major
    summary: "FRD-012 contradicted the PowerShell registration contract."
    disposition: fixed
  - id: REV-GUI142-004
    severity: minor
    summary: "Windows descriptor staleness created non-Windows false positives."
    disposition: fixed
  - id: REV-GUI142-005
    severity: minor
    summary: "The copy fallback was unquoted and staleness only token-matched the command."
    disposition: fixed
  - id: REV-GUI142-006
    severity: major
    summary: "A missing or non-invocable launcher still makes the probe report success."
    disposition: open
  - id: REV-GUI142-007
    severity: minor
    summary: "The production invocation does not explicitly propagate the native launcher exit code."
    disposition: rejected-with-reason
    reason: "A direct Windows execution of the registered command against a launcher exiting 19 made powershell.exe exit non-zero (1). Codex only requires a non-zero process result to detect failure; preserving the exact native exit value is not a ticket requirement."
---

# Independent review

Reviewed PR #281 at `cd4c241ae4b6c2189cfc83da850b001bb9a5316f`, ticket GUI-142 at `2026-08-26T11:23:02.709Z`, and plan version `dbf90ad24d76b31b`.

## Evidence

- The bounded diff remains within GUI-142: the portable Codex descriptor, its probe, Windows-only staleness detection, documentation, and Windows execution regression coverage. The corrected FRD-012 R1e now names the same PowerShell contract generated by the provider.
- GitHub Actions run `32962254946` is green: both required `kanmer-gate` and `verify` checks succeeded. This resolves the prior remote-board visibility failure.
- The five earlier GitHub review threads were examined. Their original issues are respectively fixed by the correction commit or the now-green CI run; their GitHub UI status is immaterial to this superseding Kanmer attestation because every finding has a recorded disposition here.
- I directly executed the registered probe against a temporary missing `%LOCALAPPDATA%\\Kanmer\\bin\\kanmer-mcp.cmd`. PowerShell printed CommandNotFoundException but exited `0`: `exit $LASTEXITCODE` sees no native child status when invocation itself fails. Connect would therefore treat a missing launcher as healthy and mutate project configuration.
- I also directly executed the normal registered command against a test launcher returning `19`; powershell.exe exited non-zero (`1`). That preserves failure detection for the actual production invocation, although it does not preserve the exact native status.

## Findings and dispositions

1. `REV-GUI142-001` — fixed. The board is now synchronized for CI and the required Kanmer gate passes at the reviewed head.
2. `REV-GUI142-002` — fixed. The probe appends `exit $LASTEXITCODE`, and the new Windows subprocess test covers a launcher which starts then returns non-zero.
3. `REV-GUI142-003` — fixed. FRD-012 R1e, the README, example, and contributor instructions agree on the portable PowerShell descriptor.
4. `REV-GUI142-004` — fixed. The descriptor-specific staleness migration is gated to Windows.
5. `REV-GUI142-005` — fixed. The fallback quotes the `-Command` payload and staleness parses the Kanmer table's complete argument array.
6. `REV-GUI142-006` — major, open. Add a failure-safe invocation/probe contract that causes a missing or non-invocable launcher to exit non-zero, and add a Windows execution test for that case. The current `exit $LASTEXITCODE` only covers a native child which was successfully invoked.
7. `REV-GUI142-007` — rejected with reason. The production command already returns non-zero when a launcher returns non-zero; exact native exit-code preservation is not required for Codex to detect process failure.

## Residual risk

The registration repair is unsafe while its pre-write health probe can accept a missing launcher. A fresh independent review is required after the open major finding is implemented and all required checks are green at the new head.

## Decision

Needs changes. No merge performed; GUI-142 remains in Review.
