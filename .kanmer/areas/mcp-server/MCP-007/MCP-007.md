---
id: MCP-007
type: ticket
title: 'Make plugin:check refuse to validate a bundle built inside a worktree'
status: done
area: mcp-server
assignee: claude-code
profile: fix
stageEntered:
  preparing: '2026-08-16T21:47:40.346Z'
  review: '2026-08-16T23:12:56.561Z'
  verifying: '2026-08-16T23:24:41.544Z'
  done: '2026-08-16T23:29:20.353Z'
labels: []
groups:
  - HZN-003
links:
  - SKILL-011
refs:
  - docs/functional/frd/FRD-022-mcp-server-surface.md
commits:
  - aa16afc
  - c6120b9
  - bc8cde1
prs:
  - 'https://github.com/collisionengineers/kanmer/pull/48'
archived: false
created: '2026-08-16T17:07:36.208Z'
updated: '2026-08-16T23:31:17.277Z'
---

## What

Make `scripts/check-plugin-sync.mjs` detect that it is running from a path under
`.worktrees/` and refuse — or loudly warn — instead of returning a meaningless
pass.

## Why

[[SKILL-011]] merged (PR #31) with a plugin bundle that **did not contain the
feature it shipped**, and every guard in the rail said it was fine.

A git worktree has no `node_modules` of its own, so `@kanmer/core` resolves *up*
to the main checkout's workspace symlink. `tsup` therefore bundles **main's**
core, silently ignoring the core just changed in the worktree:

```
$ ls -d .worktrees/skill-011/node_modules
No such file or directory
$ grep -c filter.group .worktrees/skill-011/packages/core/dist/index.js   -> 1
$ grep -c filter.group .worktrees/skill-011/plugins/kanmer/mcp/kanmer-mcp.cjs -> 0
```

Nothing caught it:

- **Tests** run from `src` via vitest and never touch the bundle.
- **`plugin:check`** compares the committed bundle against a fresh build — and
  inside a worktree *both* are built the same wrong way, so it is self-consistent
  and passes. It is only meaningful when the artifact was built where the check
  runs.
- The single visible tell is the embedded path comments switching from
  `../../node_modules` to `../../../../node_modules`, which nobody reads.

It failed the moment it was run at the repo root, after merge. Fixed for that
instance by PR #32, and AGENTS.md §8 gotcha 8 now names the trap — but prose is
the weakest fix available, and every future ticket that touches `@kanmer/core`
from a worktree hits this.

## Approach

- In `check-plugin-sync.mjs`, detect a `.worktrees/` segment in the repo root it
  resolves, or the absence of a local `node_modules` beside the workspace root.
- Prefer **refuse over warn**: a passing check that cannot be true is worse than
  no check, which is exactly how this shipped.
- Say what to do — build at the repo root — rather than only that something is
  wrong.
- Consider the same guard in `plugin:build`, which is where the bad artifact is
  actually produced. Detecting it at build time would stop the artifact ever
  existing; detecting it at check time only stops it being trusted.

## Verification

- [x] `npm run plugin:check` inside a `.worktrees/<id>` worktree fails with a
      message naming the cause and the fix.
- [x] The same command at the repo root is unchanged.
- [ ] ~~Reproduce the original failure: change something in `packages/core` inside
      a worktree, run `plugin:build`, and confirm the guard fires rather than
      producing a bundle built from main's core.~~ **Superseded by the operator's
      answer to Q1 (option c): `plugin:build` stays unguarded.** This bullet
      described a build-time guard the ticket deliberately does not ship.
- [x] `scripts/release.mjs` still passes at the root — the guard must not break
      the release rail it is part of.

## Outcome

Shipped in **PR #48** (squash `bc8cde1`, merged 2026-08-16). `plugin:check`
refuses inside a linked git worktree instead of reporting a pass it cannot
support; `plugin:build` is untouched, so the artifact can still be *produced*
wrong but no longer *validated* wrong.

**What shipped differently from the ticket as written:**

- **Scope narrowed to `plugin:check`.** The Approach's fourth bullet ("consider
  the same guard in `plugin:build`") was put to the operator as Q1 and answered
  **no** — guarding the build would close the only way a ticket branch can
  refresh its bundle. Verification bullet 3 above went with it.
- **The signal is not the one the ticket proposed.** The `.worktrees/`
  path-segment test was rejected in research: it only recognises worktrees this
  repo's own naming convention created. Shipped instead:
  `git rev-parse --git-dir` !== `--git-common-dir` (queried with `cwd = root`,
  both sides `path.resolve`d), with `statSync(root/.git).isFile()` as the
  fallback when `git` is off PATH. Neither is the "absence of `node_modules`"
  alternative, which was only correct under Q1's option (b).
- **No escape hatch**, per Q2, and none is to be added later without asking.

Also: `AGENTS.md` §8 gotcha 8 shrank from six lines of SKILL-011 narrative to two
pointing at the guard (Q3); §6's `plugin:check` row and §10 item 6 note the root
requirement; `FRD-022` R6 records the precondition.

**Follow-ups — none filed.** One candidate: `origin/main` gained
`test:scripts` → `node --test "scripts/*.test.mjs"` (GUI-066) *during* this
ticket, so the "nothing covers `scripts/`" premise the plan rested on expired
mid-flight. `isLinkedWorktree` would now have somewhere to live as a unit test.
Left unfiled because the operator specified a hand-run command log for this
ticket and both branches of the guard, including the fallback, are exercised in
`proof.md`.
