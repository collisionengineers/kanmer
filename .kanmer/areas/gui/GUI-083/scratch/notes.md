Course-correction found during execute: GUI-079 had already merged into origin/main by the time the worktree was created (after research/plan were written against the pre-fetch main checkout). Its actual change: grok's `configPath` moved from `.mcp.json` to `.grok/config.toml` (ADR-0012 — one host, one file; grok no longer shares Claude's `.mcp.json`). This doesn't change the plan's approach (still: derive every configFile configPath + copySkills skillsDir from providers.ts and ignore each with the shared reasoning), it just changes one entry in the concrete list: added `.grok/config.toml` to the ignore rules (not previously in research/plan, which had assumed grok still wrote `.mcp.json`). `.mcp.json` itself stays ignored as before, now correctly attributed to Claude Code's own `claude mcp add -s project` CLI write rather than grok. Verified with `git check-ignore -v` against every real + the marketplace-manifest path before proceeding.
