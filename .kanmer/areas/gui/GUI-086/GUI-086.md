---
id: GUI-086
type: ticket
title: Make kanmerGit.test.ts survive concurrent load and Windows temp cleanup
status: backlog
area: gui
assignee: ''
profile: fix
labels:
  - rail
  - reliability
  - flake
links: []
archived: true
created: '2026-08-16T23:06:46.581Z'
updated: '2026-08-16T23:13:26.967Z'
---

## Archived as a duplicate of [[GUI-085]]

Filed independently, minutes apart, for the same defect —
`apps/gui/src/main/kanmerGit.test.ts` failing non-deterministically.

**[[GUI-085]] is the better ticket and is the one to work.** It carries the
measured per-test timings, which reframe the problem correctly: four of seven
tests already run *longer than the timeout they are given* (4.6–8.7 s against
vitest's 5 s default) and pass only because the timer races the git process. That
is a mis-specified suite, not a flake — a sharper and more actionable diagnosis
than this ticket's.

The cross-agent corroboration originally written here — four agents hitting it on
four unrelated diffs, the concurrent-load aggravation, and the related
`electron.exe` handle problem GUI-070 hit at closeout — has been moved to
GUI-085's scratch so nothing is lost.

Nothing else here is unique. Archived rather than deleted, per the board's
convention.
