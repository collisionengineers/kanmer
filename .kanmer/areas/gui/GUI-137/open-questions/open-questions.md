# Open questions — GUI-137

- [x] Reuse `canonicalProjectPath`; do not invent a second normalization rule.
- [x] Canonicalize at the manager ownership boundary so all IPC callers receive the same behavior.
