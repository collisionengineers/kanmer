# Checklist

- [x] `visual`, `test-output`, `command-log` as shipped defaults
- [x] `proof:visual@staging` parsed into type + flavour + environment
- [x] environment validated against the board's declared list at write time
- [x] only `proof` accepts a type suffix
- [x] visual-without-image warns inside the shared report
- [x] the warning never blocks
- [x] tests: warn, clear-on-image, reject-unknown-environment
