2026-08-24 — DOC-021 merged normally as be15545a90af27f08e2124e7aaf39c4bcc3b51dc. The pre-existing recorded CORE-096 checkout remains untouched because it is dirty with a release-notes change owned by DOC-021. A new clean isolated checkout was created at `.worktrees/core-096-release`, branch `release/v0.3.4`, exact origin/main be15545a90af27f08e2124e7aaf39c4bcc3b51dc; `git status --short` was empty. Next: one exact preparation invocation with KANMER_ROOT bound to the canonical board.

2026-08-24 — First preparation invocation exited 1 before mutation: `release refused: on branch "release/v0.3.4", not main`. This is a clean-process correction, not a source/test failure. The checkout remains unchanged; switch it to the exact merged `main` and retry the preparation phase once, allowing scripts/release.mjs to create its own release branch.

2026-08-24 — Corrected invocation completed the full release verification gate successfully (core 310, GUI 468, MCP HTTP/smoke, scripts 98, typecheck, docs, headless/MCPB, skills/AGENTS/plugin rails). It then exited 1 before versioning because the empty local `release/v0.3.4` branch, mistakenly pre-created during checkout setup, conflicts with the release script’s generated branch. Validation: checkout clean, `git rev-list --count main..release/v0.3.4` = 0, and no matching remote branch. Remove only this redundant local branch and make one corrected retry; no PR/tag/release/publication occurred.

2026-08-24 — Corrected release preparation PASS: generated and pushed `release/v0.3.4`, commit 03eb9f49e46a3d6961054d7e1eb880bc01790f30; opened PR #244. `git diff --check main...HEAD` passed; changed paths are exactly the eight expected generated release artifacts. Full gate passed (core 310, GUI 468, HTTP/scripts/typecheck/docs/MCP/MCPB/skills/AGENTS/plugin rails). Hosted checks are pending. No tag, public release, asset upload, or publication invocation occurred.

2026-08-24 — Hosted PR #244 initial kanmer-gate run 32762464916/job 97544237475 exited 1 due a timing race: it fetched the board before the controller’s Review-stage move and independent review record, reporting Implementing/no scratch-review. This is retained. The source preparation head is unchanged; after independent exact-head review is recorded, rerun the gate and retain all check outcomes.

2026-08-24 — PR #244 hosted verify job 97544237148 (run 32762464916) also failed: core `src/store.test.ts > gives tickets area-based ids and places them in the area's folder` exceeded its 5000ms Vitest timeout; 309/310 tests passed. The same full release gate passed locally in both clean invocations. Preserve this as a transient hosted timeout; do not weaken/change the test in release scope. After independent attestation, rerun the full workflow once for exact-head hosted confirmation.

2026-08-24 — Independent exact-head review PASS and rerun checks PASS; PR #244 normal-protected squash-merged as 102ba3b120cc3065943089d122a6172de8934ece. Ticket is Verifying. Next is exactly one authorized publish invocation from a new clean checkout pinned to that merge; no source modification is permitted.

2026-08-24 — First publish invocation exited 1 before mutation because scripts/release.mjs requires local branch `main`, rejecting detached HEAD despite the exact --release-commit SHA. The fresh clone’s tracked main is the same merge; switch to clean main and retry the one publish phase. No tag, release, package build, or upload occurred.

2026-08-24 — Corrected publish invocation on clean main exited 1 before build/tag/upload because scripts/release.mjs requires GH_TOKEN (or equivalent) explicitly in its process environment. The existing authenticated gh session remains available; bridge its token only into the release process without printing or recording any secret, then retry once. No tag, release, package build, or upload occurred.

2026-08-24 — Publish failure diagnosis: the exact MCP HTTP suite was rerun read-only in the same clean checkout with KANMER_ROOT bound and passed 102/102. The failed release process reached that suite without an inherited KANMER_ROOT (root resolver showed discovery only). No code/config/test change is warranted. Retry the full publish rail once with the active gh token captured first, then both GH_TOKEN and KANMER_ROOT explicitly set immediately before npm launch; preserve the failed full run and this focused PASS.
