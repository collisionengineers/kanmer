# 2026-08-24T16:40Z — CORE-036 fixture disposition

A disposable public GitHub repository was created to exercise the empty-release negative path without touching production. Both hosted runs stopped before packaging/assets because the release-notes unit test expects the production repository URL while a copied checkout correctly resolves its own URL. This is a fixture portability limitation; it is not evidence for the required asset-verifier red path and does not change production CI status.

The temporary draft release and tag were removed. The disposable repository itself remains because the active GitHub token lacks the `delete_repo` scope; it needs manual deletion or a token scope refresh. CORE-036 remains Verifying for the real future production tag-triggered pass and a valid negative release evidence run.
