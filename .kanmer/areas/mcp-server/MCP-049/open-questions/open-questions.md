# Open questions — MCP-049

- [x] Which OpenAI tunnel is canonical? The operator-selected new tunnel represented by the second profile.
- [x] Where does its runtime key come from? The existing dedicated Infisical secret, injected only into the supervised process.
- [x] Does Cloudflare need new product code? No; the production GUI manager already owns the required lifecycle and is merely unconfigured.
- [x] Are additional provider resources required? No; the named tunnel, DNS route, local credential, and executable already exist.

## Parked (explicitly deferred)

None.
