2026-08-22 exact-head supplementary run: `npm run typecheck -w @kanmer/gui` PASS. `npm test -w @kanmer/gui` was started after d79f5f61; it emitted the full suite through the 5 new index.sync tests and then remained idle without Vitest completion, so it was interrupted and is INCONCLUSIVE. This does not replace the prior full GUI PASS (49 files / 421 tests on the implementation commit) or the independent exact-head 38/38 focused PASS and exact-head 10/10 focused PASS.
