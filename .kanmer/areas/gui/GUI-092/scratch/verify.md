Verified merged main `e5070de` (PR #100 merge commit). Evidence: `npm run test:scripts` 59/59; `npm run typecheck` all workspaces; GUI production build; valid no-network `apps/gui` Electron Builder Windows package; updater package 7/7; diff check clean. The first package command from repo root was invalid and rejected; it is explicitly excluded from proof. No production release or installed-client update was claimed; [[GUI-068]] remains the external acceptance follow-up.
