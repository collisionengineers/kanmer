# Checklist

- [x] miner matches the item marker, not the container
- [x] preview shows documents → items → tickets by area
- [x] every source yielding nothing is named with a reason
- [x] 60 tickets created in `done`
- [x] all `profile: custom` with an empty requires map
- [x] all labelled `backfill` with `refs` at the source
- [x] plan content written to each ticket's `plan/`
- [x] ten spot-checked file-for-file, body echoed not just title
- [x] second run creates nothing
- [x] open-ticket count unchanged
