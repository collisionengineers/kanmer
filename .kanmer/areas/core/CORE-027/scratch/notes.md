2026-08-21 — Researched/planned and began implementation in `.worktrees/core-027`. Core browser artifact build succeeds and `scripts/check-browser.mjs` confirms no Node built-ins. Required `npm run build:ui` is blocked in the linked worktree because its shared `node_modules/@kanmer/core` workspace package manifest remains from the main checkout and does not expose `./browser`; esbuild therefore rejects `@kanmer/core/browser`. Core typecheck passes. No commit/PR/review/merge performed; ticket remains Implementing pending a safe isolated dependency refresh or an approved verification approach.
