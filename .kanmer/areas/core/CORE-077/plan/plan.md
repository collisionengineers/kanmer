# Plan

1. Inspect the live board worktree branch immediately before automatic sync.
2. Refuse/pause when the live branch differs from the saved handoff branch, preserving the canonical board root.
3. Add deterministic unit/state regression and run focused GUI, core, scripts, build and diff rails.
