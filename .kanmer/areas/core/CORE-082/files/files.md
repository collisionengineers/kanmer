# CORE-082 files and impact

## In scope

- `packages/core/src/io.ts`: strengthen lock-owner identity and stale recovery; preserve atomic quarantine, retry, cleanup, and fail-closed semantics.
- `packages/core/src/io.test.ts`: append deterministic PID-reuse-safe ownership, malformed stale-record recovery, and active/uncertain lock regressions without deleting or weakening existing assertions.
- `apps/gui/src/main/kanmerGit.ts`: exclude board lock, owner-marker, and stale/quarantine artifacts from board Git synchronization.
- `apps/gui/src/main/kanmerGit.test.ts`: prove operational board artifacts are ignored while normal board documents and existing source cache behavior remain synchronized.
- `plugins/kanmer/mcp/kanmer-mcp.cjs` only if regenerated by the repository's required deterministic plugin build after the core change.

## Verification files

Run focused core IO and GUI Git tests, package/core and GUI typechecks, relevant build/plugin checks, scripts/diff checks as feasible. Record exact exit codes and preserve first failures.

## Explicitly out of scope

CORE-026 source-fetch semantics, CORE-081/083 implementation, external/live Windows PID-reuse claims, unrelated GUI synchronization behavior, board migration redesign, and merging or verification of parent/child PRs.
