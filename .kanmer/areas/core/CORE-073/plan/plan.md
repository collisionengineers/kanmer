# Plan

1. Compare retry result `boardRoot` with the open context root.
2. Refuse mismatches with an actionable paused error, or rebuild all dependent context atomically.
3. Add deterministic regression and run rails.
