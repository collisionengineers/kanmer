opened PR https://github.com/collisionengineers/kanmer/pull/54 (commit `61d058c`, branch `core-023-detect-stale-repo`, worktree `.worktrees/core-023`).

`link_doc` for `docs/architecture/adr/ADR-0013-staleness-by-content-not-version.md` is **deferred to verify**: the file exists only on the branch, and `link_doc` validates the path against the main checkout. Add it once the PR merges.
