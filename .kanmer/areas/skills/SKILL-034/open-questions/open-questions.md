# Open questions

No unresolved questions. Reuse and package the existing canonical writer/body; do not invent a second implementation.

## Parked (explicitly deferred)

- Updating the user's installed cache is delivered through the corrected release, not by mutating cache state during implementation.
