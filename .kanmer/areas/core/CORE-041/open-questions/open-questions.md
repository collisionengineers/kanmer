# Open questions — CORE-041

- [x] Should the correction remain test-only? Yes — the hosted failure is caused by a hardcoded smoke expectation while production canonicalization correctly uses the host drive.
- [x] Is a new governing document required? No — this is a CI/test-rail remediation and does not change a product contract; the ticket retains `docs_todo` for board bookkeeping.
