# Files — DOC-009

| Path | Change |
|---|---|
| `AGENTS.md` | Replace the two obsolete Codex-plugin/.mcp.json map rows with accurate skills-only Codex and Claude/grok MCP-config entries, outside the managed marker block. |
| `plugins/kanmer/.codex-plugin/plugin.json` | Read-only source of truth for the skills-only claim. |
| `plugins/kanmer/mcp/claude.mcp.json` | Read-only source of truth for the remaining plugin MCP configuration. |
