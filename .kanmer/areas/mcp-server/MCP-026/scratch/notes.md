2026-08-21 checkpoint: took `.worktrees/mcp-026` / `mcp-026-bearer-auth`; committed `86b1101` bearer-authorizer foundation. Added opaque SHA-256 verifier generation/parsing with constant-time comparison, generic 401 challenge, protected token-file loader, and fail-closed HTTP CLI source. Built HTTP smoke, stdio smoke 175/175, and protocol smoke 30/30 pass. Lifecycle rotation/revocation, exhaustive file safety tests, and GUI-095 boundary remain before review; no PR yet.

2026-08-21 checkpoint: committed `332777e` protected token lifecycle. Token files are exclusively created, mode-checked on POSIX, lstat/open/fstat checked, bounded, and zeroed after read; CLI starts only from protected `KANMER_HTTP_TOKEN_FILE` (no raw token arg) and generator prints a fingerprint only. Added local-only host rotation/revocation that invalidates sessions. Built HTTP smoke now proves auth-before-session, rotation invalidation, revocation fail-closed, and file load; stdio 175/175, protocol 30/30, discovery 13/13 remain green. Still need exhaustive auth/file tests, canary scans, full root rails, report, PR, and review.

2026-08-21 verification checkpoint: committed `915f736` to extend built HTTP smoke through token-file generation/round-trip, generator no-secret stdout, generic 401 before session lookup, rotation session invalidation, and revocation-before-JSON parsing. `npm run build:server`, HTTP smoke, MCP typecheck, standard stdio smoke 175/175, protocol 30/30, discovery 13/13, and root npm test (core 255/255; GUI 300/300; scripts continuing/previously green) have been exercised. Remaining checklist boxes are intentionally still visible until their exact security claims have direct evidence.

2026-08-21 security hardening: commits `b3e0a43` and `adb3f7b` make verifier digest non-enumerable/immutable, derive stable opaque generation IDs, validate rotation input, and add malformed header/digest-token/POSIX permission/invalid-file tests. Built HTTP smoke passes. Root `npm run typecheck` still reaches the known unrelated `@kanmer/ui src/demo.tsx` TicketDocsInfo mock mismatch; MCP typecheck passes. No PR yet: the remaining unchecked checklist claims need broader redaction/secret-file and platform evidence.

2026-08-21 process-boundary evidence: committed `b3ffd99`; built smoke now launches the real HTTP CLI from a protected token file, asserts authRequired readiness, and checks raw token absence from both token-generator and listener output. The combined file-safety checklist item remains unticked because partial-write cleanup/space-path coverage has not yet been independently induced.
