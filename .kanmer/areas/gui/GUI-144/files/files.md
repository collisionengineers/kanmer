# Files — GUI-144

## Files the change touches

| File | Change | Risk |
| --- | --- | --- |
| `apps/gui/src/main/projectRegistry.ts` (new) | Registry contract mirror (schema 1, name regex, validation), `registryLocation`, `readRegistry`, `observeRegistry` with GUI deps (KanmerStore, kanmerGit probes), serialised writer (`upsert`, `rename`, `remove`, `setPolicy`) | Shape drift vs server module — covered by contract test |
| `apps/gui/src/main/projectRegistry.test.ts` (new) | Unit tests: location precedence, parse/validate matrix, two fixture boards with distinct health, bound matching, serialised concurrent writes, stale-edit guard, contract check against `packages/mcp-server/src/project-registry.ts` via runtime import | Runtime import outside vite root may need `server.fs.allow`; fall back to fixture JSON |
| `apps/gui/src/main/index.ts` | IPC handlers `registryObserve`, `registryAddOpenProject`, `registryRename`, `registryRemove`, `registrySetPolicy`; identity from `ProjectContext` only | Large file; keep handlers near remote ones |
| `apps/gui/src/shared/ipc.ts` | `CH.registry*`, `KanmerApi.registry*`, `RegistryView`/`RegistryEndpointView` types | Mirror in preload |
| `apps/gui/src/preload/index.ts` | Bridge the new channels | `preload/index.test.ts` may enumerate api keys |
| `apps/gui/src/renderer/src/components/ProjectRegistry.tsx` (new) | Settings tab "Projects": overview cards per endpoint (project_id, location, health, sync ahead/behind, policy, controllers/workspaces), selected project highlighted and the only one with add/rename/policy controls; others "Open project" only | UI only |
| `apps/gui/src/renderer/src/components/ProjectRegistry.test.tsx` (new) | Two projects with distinct health; non-selected project exposes no mutation controls | jsdom |
| `apps/gui/src/renderer/src/components/Settings.tsx` | Add `projects` tab entry rendering the section | Tab list literal |
| `apps/gui/src/renderer/src/styles.css` | Reuse `remote-project-grid`/`card`; minimal additions | none |
| `docs/manual/settings.md` | "Projects" tab section | `check:manual` |
| `docs/manual/connect.md` | Replace "tracked separately (GUI-144)" sentence | `check:manual` |
| `apps/gui/src/renderer/src/manual/chapters.generated.ts` | Regenerated by `npm run build:manual` | must be committed |

## Ripple effects

- `preload/index.test.ts` and any `KanmerApi` exhaustive stubs in renderer tests (grep `kanmer =` in tests) may need the new members.
- `AGENTS.md` §8 gotcha 16 says writer helpers are for the GUI; note the GUI mirrors the contract (no server import).
- `npm run verify:docs` if docs link structure changes.

## Out of scope

- Any change under `packages/core` or `packages/mcp-server` (CORE-115 lane active; server d.ts export is a parked question).
- Spawning MCP servers per endpoint, per-endpoint HTTP/tunnel liveness (parked in MCP-054).
- Cross-project mutations, a global backlog, or moving the registry into `settings.json`.

## Context files

| File | What it tells the implementer |
| --- | --- |
| `packages/mcp-server/src/project-registry.ts` | The authoritative file contract and observation semantics to mirror exactly |
| `packages/mcp-server/src/project-identity.ts` | `LogicalProject`, `LocationFingerprint` (`kanmer-loc-v1`), `redactRemoteOrigin` |
| `apps/gui/src/main/remoteAccess/manager.ts` (`overview`, `enqueue`, `withSettingsLock`) | Cross-project overview + serialised mutation pattern |
| `apps/gui/src/main/index.ts` 130-180, 1027-1090 | `ProjectContext`, `remoteIdentity`, trusted-sender guards, picker flow |
| `apps/gui/src/main/kanmerGit.ts` 135-163, 236-260 | `inspectBoardSync`, `inspectBoardWorktree` (git probes; never throw) |
| `apps/gui/src/renderer/src/components/Settings.tsx` 34-45, 1137-1230 | Tab list and `RemoteSection` card layout / "Open project" convention |
| `apps/gui/src/renderer/src/components/Settings.remote.test.tsx` | Renderer test harness with stubbed `window.kanmer` |
| `apps/gui/src/main/settings.test.ts` | Main-process test harness (electron mock, temp userData) |
| `.worktrees/core-115/packages/core/src/types.ts` 453-470 | Lease field names to read defensively (read-only reference) |
