# Open questions — CORE-059

## Parked (explicitly deferred)

- The GUI cannot update a GitHub repository variable or safely prove that an administrator has retargeted it. Old-ref deletion is therefore explicitly deferred to the administrator after `KANMER_BOARD_BRANCH` is changed; this ticket preserves the ref and documents the handoff.
