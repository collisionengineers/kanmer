```text
Kanmer-first cutoff report
Observed at: 2026-09-05 15:48 BST / 2026-09-05T14:48:00Z (corrected 2026-09-05 ~16:00 BST after read-only review found document overclaims)
Adopted runtime / plugin / skill / managed-policy revisions: NOT RUN — live control plane still 0.4.1 (packaged, %LOCALAPPDATA%\Kanmer\mcp\0.4.1-7432, sha256 3f7af329…). Source ready for 0.4.2 at origin/main 4a1c3a23; no MCPB/plugin/skills adoption has happened yet.
Route: qualified standalone MCP + plugin/skills cut (desktop installer carried forward on Electron 31.7.7, sandbox: false, no new runtime/security claim)

M1 coherent instructions: PASS pending adoption — evidence: DOC-028 merged (bd368549, PR #321), routes work by purpose and names the configured integration branch instead of hardcoding main; DOC-026 merged (37b83b14, PR #326) retires CLOSEOUT_PLAN.md. `get_status.repo` on the current 0.4.1-installed plugin already reports the managed AGENTS.md block as `behind` (stale, informational) against the 0.4.2-shipping content — expected until `kanmer-setup` is re-run after the 0.4.2 plugin is installed. Checks referenced: PR #310-#313 lineage plus this horizon's #321/#326.

M2 one heavy owner: PROCEDURAL — named verifier: Alex (operator), sole merger and merge-authorisation owner for this horizon per HZN-009 context.md "Operating controls". No automated lock exists yet; the `kanmer-verify` skill's evidence-first lookup (MCP-057, e474f317) waits on the bound post-merge `verify` run for the exact merge SHA rather than re-running it, which is the automated half of the control. The enforced heavy-verification permit is deferred to CORE-143 (R2/0.5.0); R1-LEASE is explicitly procedural per AMENDMENTS.md owner decision.

M3 matching evidence: automated + human-reviewed, result PASS on the merged set. Corrected scope: only **MCP-057**'s proof (e474f317, PR #325) exercised the live `PROOF_RECEIPT_REJECTED` demonstration — a non-matching receipt was rejected before the true bound receipt was accepted. CORE-138 (9945b1f2, PR #324), CORE-144 (de5bace9, PR #327) and CORE-145 (58718455, PR #328) proofs carry receipts validated by `assessReceipt`, but did **not** run a rejection demo; do not describe them as having demonstrated `PROOF_RECEIPT_REJECTED`. Every proof's `observed_by` field records **`claude-code verifier (HZN-009)`**, not Alex — provenance in each of these proofs was observed by the verifier subagent via `gh` (reading the hosted run/receipt), not by the human operator; the human-owned control in this horizon is Alex's merge authorisation (M2), a separate mechanism from proof provenance. CORE-129 (410bfd22, PR #329) adds the typed-proof / attempts[] consistency validator itself and is **Done** (verified PASS at the merge SHA; see CORE-129's own proof record and Outcome). CORE-147 (4a1c3a23, PR #330) adds the repository-declared verification contract (`board.yml`'s `delivery.verification`: workflow, required jobs, triggering event) that receipt validation and the verify skill now read instead of assuming Kanmer's own defaults; a repository with no post-integration run recorded at the merge commit still falls back to the designated verifier running the check in a detached worktree, and a consuming repository on an older server keeps the previous default behaviour — both preserved, not new gaps introduced by CORE-147. **CORE-147's own ticket status, read live via `get_item` at the time of this correction: `verifying` (stageEntered.verifying 2026-09-05T15:52:06Z; lease phase `implementing`, taken by claude-code) — verification is in progress, not yet Done. RE-CHECK before the closeout is finalized: re-read `get_item CORE-147` and update this line and the M4 Done roster if it has since reached Done.**

M4 Done separate from deployment: PASS — every R1 ticket recorded here as Done (CORE-129, CORE-138, CORE-140, CORE-144, CORE-145, DOC-026, DOC-028, GUI-152, MCP-057) recorded `delivery_state: integrated` on merge to main; `deploymentTracking` is off board-wide (get_status), and no deployment action (release tag, publish, install) has occurred for any of them. CORE-147 is Verifying, not yet Done — excluded from this Done list until its own status is re-confirmed (see M3 marker). Disposable scenario: NOT RUN — the installed-route mutation drill (get_status → create_item → set_ticket_doc → get_doc_gates → move_item → list_items → archive on a mkdtemp board) is scheduled for the actual 0.4.2 cut (plan step 11), not this preparation step.

M5 actual host and safe promotion: NOT RUN — no MCPB/plugin build, no host install, no rollback drill has been performed in this preparation pass. Deferred to the operator's cut (plan steps 9-12).

Pegasus integration target and current baseline: out of scope for R1 per HZN-009 group body ("Pegasus is out of scope"); no target set here.

Active implementation owners / shared-path owner / verifier: implementer for this ticket's preparation — claude-code (worktree .worktrees/CORE-141, branch CORE-141-release-0.4.2); verifier for the merged HZN-009 tickets' proofs — claude-code verifier (HZN-009), observing provenance via `gh`; named heavy verifier and sole merge-authorisation owner — Alex; lane owners per HZN-009 context.md: lane A (pr.yml, verify.mjs, check-pr.mjs, execute/review skills), lane B (agents-block-body.mjs + mirrors, kanmer-verify/SKILL.md, reconciliation.ts, proof-record.ts), lane C (apps/gui/src/**).

Release-critical subject-project blockers: none identified against Kanmer's own repo. CORE-147 remains in Verifying, not Done, on the live board as of this correction — its code is merged (4a1c3a23) and safe to release on, but its own ticket lifecycle should close (verifying -> done) before or shortly after the 0.4.2 cut so the roster the release notes credit is fully closed out. RE-CHECK marker: see M3.

Unfinished R1 -> R2: CORE-142 (gate-only hosted required check + blocking attestation, needs repository administration); CORE-143 (implemented heavy-verification permit, currently procedural); **CORE-146 (scheduled — group HZN-010, package R2-EVIDENCE, not "unscheduled": reconciliation cannot recommend Done after a squash merge until `commits[]` holds the merge SHA)**; GUI-153 (Focus Board UI-C+D: list view, conflict UI, time-in-stage, packaged qualification); R2-DESKTOP (Electron runtime upgrade + IPC/sandbox re-qualification); R2-EVIDENCE remainder (receipt store, reuse key, provenance adapter beyond MCP-057+CORE-129+CORE-147); the smoke "ready packet is read-only" flake, now tracked as **MCP-058**.

Live-board inventory/audit coverage: complete for this snapshot, with one item still resolving live (CORE-147). get_status.counts as of the first preparation pass: byStage backlog 14, preparing 0, implementing 1 (CORE-141), review 1 (CORE-129), verifying 0, done 381, archived 30, taken 1, total tickets 396. Since that snapshot: CORE-129 reached Done (410bfd22); CORE-147 (#330, 4a1c3a23) is in Verifying, not yet Done — re-read `get_item CORE-147` before finalizing counts. Context.md OP-00 snapshot (2026-09-05 ~03:00 BST) recorded 384 tickets + 30 archived = 414, backlog 10, preparing 1, done 373. Delta since OP-00: 9 of the 10-ticket HZN-009 roster are confirmed Done (CORE-129, CORE-138, CORE-140, CORE-144, CORE-145, DOC-026, DOC-028, GUI-152, MCP-057); CORE-147 is merged code, ticket still Verifying.

Rollback: retained generation 0.4.1-7432 under %LOCALAPPDATA%\Kanmer\mcp\; release ledger attempt main@1 (v0.4.1, 4e94ad80, verification passed) is the last known-good published release with a full asset-digest manifest. Procedure: point host provider registration back at the 0.4.1 generation folder or reinstall Kanmer-Setup-0.4.1.exe from that ledger's asset manifest; confirm get_status.server.version reads 0.4.1; re-run a disposable mutation to confirm the board is still readable. No board migration needed either direction. NOT TESTED in this preparation pass (scheduled at plan step 12, the actual cut).

Resume decision and authority: NOT RUN — this is a preparation report, not a resumption decision. The operator (Alex) makes the actual go/no-go at the real cut per CORE-141's Verification section and this plan; nothing here authorizes running scripts/release.mjs for real, tagging, publishing, or touching the installed host.
```
