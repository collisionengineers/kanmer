# Files

- `apps/gui/src/main/index.ts`: block protected rename when any context has a branch mismatch.
- `apps/gui/src/main/kanmerGit.test.ts` or the existing main integration test surface: prove no automatic rename on mismatch.
- `apps/gui/src/main/kanmerGit.ts` only if the existing state helper needs a bounded adjustment.
- CORE-052 cumulative report/item traceability after merge.
