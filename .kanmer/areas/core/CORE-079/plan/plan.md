# Plan

1. Locate all three failing direct path comparisons in the board-worktree Git tests.
2. Compare filesystem identity rather than Windows spelling, preserving the path contract.
3. Run the focused Git rail, full relevant GUI rail, core/scripts/build/typecheck and diff checks; rerun hosted-equivalent verification.
