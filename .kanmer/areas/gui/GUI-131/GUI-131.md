---
id: GUI-131
type: ticket
title: Repair v0.3.4 packaged Electron entry before release publication
status: preparing
area: gui
assignee: ''
profile: fix
stageEntered:
  preparing: '2026-08-24T18:51:49.600Z'
labels:
  - release
  - packaging
  - regression
groups:
  - HZN-007
links: []
blocks:
  - CORE-096
archived: true
created: '2026-08-24T18:51:14.675Z'
updated: '2026-08-24T18:58:19.588Z'
---

## What
Repair the packaged Electron artifact so Electron Builder includes the configured `out/main/index.js` entry and the updater-package check can produce the required release manifest and NSIS resources.

## Trigger
CORE-096's authorized v0.3.4 publish run pushed tag `v0.3.4` but stopped before any GitHub Release or asset upload. The original report attributed this to a missing `app.asar` main entry.

## Scope
This ticket was limited to diagnosing and repairing a packaging configuration/build inclusion defect, while preserving failed release evidence and avoiding retagging, publication, upload, or release-semantics changes.

## Verification
- packaged app contains the configured Electron main entry;
- updater-package check passes on a clean build;
- a replacement publication plan accounts for the existing v0.3.4 tag without pretending it released.

## Outcome
Archived as a disproven trigger, not completed implementation. GitHub Actions run `32764694871` logged Electron Vite building `out/main/index.js` twice and Electron Builder completing package/NSIS/block-map stages. A clean package at `102ba3b120cc3065943089d122a6172de8934ece` passed the updater-package check and its `app.asar` contains `out/main/index.js`. The actual hosted failure was implicit tag-based publishing without `GH_TOKEN`, which is outside this ticket's packaging-only scope. No source, release, tag, or asset state was changed by GUI-131.
