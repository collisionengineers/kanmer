# Open questions — GUI-110

- [x] Should the demo include provider entries or model values? No. The browser demo cannot launch provider CLIs; the honest shape is an empty `providers` map.
- [x] Should GUI-110 change GUI-075 runtime behavior? No. It only completes the browser fixture required by the shared AppSettings type.
