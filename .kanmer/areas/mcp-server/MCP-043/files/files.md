# Files — MCP-043

## Where the change lands

| Path | Why |
|---|---|
| plugins/kanmer/mcp/kanmer-mcp.cjs | The sole intended source diff: replace the stale committed standalone bundle with the byte-identical output of the canonical plugin:build pipeline after MCP-028. |
| Kanmer ticket documents | Research, plan, checklist, open-questions, and post-implementation report record the bounded artifact work and evidence; these are board metadata, not repository source changes. |

## Context files

| Path | What it tells the implementer |
|---|---|
| scripts/build-plugin.mjs | The supported copy operation from packages/mcp-server/dist/standalone/kanmer-mcp.cjs into the committed plugin location. |
| packages/mcp-server/tsup.standalone.config.ts | The deterministic standalone CJS build configuration and the no-machine-specific-bytes constraint. |
| scripts/check-plugin-sync.mjs | The authoritative plugin byte/tool/manifest/handshake checks and linked-worktree guard. |
| scripts/build-mcpb.mjs and scripts/check-mcpb-sync.mjs | MCPB staging and parity checks against both the fresh standalone server and the committed plugin copy. |
| scripts/verify.mjs | The shared authoritative rail and its inclusion of MCPB/plugin parity checks. |
| docs/architecture/adr/ADR-0016-compiled-workflow.md | The compiled workflow decision and traceability requirement for generated artifacts. |
| docs/functional/frd/FRD-022-mcp-server-surface.md | The shipped MCP surface, deterministic bundle, plugin-check, and MCPB expectations. |
| MCP-028/proof.md and MCP-042/post-implementation-report.md | Provenance: MCP-028 introduced the merged remote-access source changes; MCP-042 documents the prior artifact-only parity pattern and retained hosted evidence. |

## Ripple effects

The plugin bundle is consumed by Claude/plugin packaging and the MCPB parity checker. A regenerated artifact changes only shipped bytes; it must not alter source tool semantics, manifests, GUI behavior, board data, or remote credentials. The normal verify rail and dependent hosted verification consume the refreshed bytes.

## Out of scope

No MCP TypeScript/source changes, checker changes, GUI-075/GUI-110 or GUI-104 behavior, CORE-024 work, documentation-governance changes, provider/tunnel operations, or hand-edited generated bytes.
