# Checklist

- [ ] Refresh the committed plugin artifact from the cumulative source tree.
- [ ] `npm run mcpb:check` passes and the diff contains no source changes.
- [ ] Independent review records the exact commit and merge SHA.
