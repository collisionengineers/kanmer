2026-08-20: ADR-0016 allocated (numbering rail green). Added the ADR and end-state deltas to the ten scoped FRDs; did not edit generated doc-structure. `npm test` core portion completed 250/250; GUI 288/288 rerun separately; scripts 48/48; numbering and diff checks pass. `npm run verify` is not yet available on merged main because CORE-031 is still implementing; recorded as a dependency rather than treating it as a document failure. Target-ticket refs/docs_todo cannot be written until this branch's docs exist on main because MCP validates repo-doc paths against main.
