# Plan — CORE-139: Stop workflow_dispatch from running the full verify rail and fix the shipped skill link and AGENTS block sentence

*The plan. Not the checklist — reasoning establishes bounded work; the checklist distils it into independently observable actions.*

## Objective
A board push never runs the full Windows rail; re-gate dispatches coalesce and skip when nothing is open; the shipped `kanmer-setup` skill and canonical AGENTS block no longer break consuming repositories; a validator spawn failure is reported as such.

## Starting state
- `.github/workflows/pr.yml:30`: `if: github.event.action != 'edited' && (github.event_name == 'pull_request' || github.ref == 'refs/heads/main')` is true on `workflow_dispatch --ref main`. No `concurrency` in `pr.yml` or `board-regate.yml`. Observed 2026-09-03: 53/60 recent runs were dispatches, 25 on one SHA.
- `board-regate.yml` dispatches unconditionally; `regate` (`pr.yml:120-124`) checks for open PRs only after the run is charged.
- `plugins/kanmer/skills/kanmer-setup/SKILL.md:169` links `../../../../docs/manual/greenfield.md`; `scripts/agents-block-body.mjs:60` and `SKILL.md:255` end "…before invoking the stable launcher. Native".
- `scripts/verify-skill-prose.test.mjs`: 19 direct `spawnSync` sites plus `runOn`; none assert `result.error`.
- Evidence: `files/files.md` (this ticket), verified against `origin/main` at `cd5b6b6b`.

## Governing docs
- FRD-023 (agent skills system) R5, skill prose verified against the code it describes: **Meets**; check 21 extends the verification to link portability.
- FRD-013 (setup as reconciliation): **Meets**; the canonical block edit is reconciled by `kanmer-setup` in consuming repos.
- No ADR change.

## Required changes
1. `pr.yml` `verify.if` = `(github.event_name == 'pull_request' && github.event.action != 'edited') || (github.event_name == 'push' && github.ref == 'refs/heads/main')`.
2. `pr.yml` workflow-level `concurrency` keyed by workflow, event name and PR number or ref, `cancel-in-progress` for every event except `push`.
3. `board-regate.yml`: `concurrency` group `board-regate-<ref>` with `cancel-in-progress: true`; `permissions.pull-requests: read`; the dispatch step runs `gh pr list --base main --state open --limit 1 --json number --jq 'length'` and exits 0 without dispatching when it is 0.
4. `kanmer-setup/SKILL.md:169`: unlinked reference to the manual; remove the trailing " Native" from the runtime-supervisor sentence in `scripts/agents-block-body.mjs` and the SKILL.md fenced copy; refresh `AGENTS.md`.
5. `verify-skill-prose.mjs` check 21 "no shipped skill link escapes its skill folder": for every Markdown link target in `plugins/kanmer/skills/**/*.md`, skip scheme, absolute and anchor targets, resolve relative to the file, FAIL when the resolved path leaves the skill folder; print every hit.
6. `verify-skill-prose.test.mjs`: a `spawnValidator(fixture)` helper asserting `result.error === undefined` and `result.stdout.length > 0`, used by `runOn` and every direct site; a check-21 mutation case.
7. `pr-workflow.test.mjs` assertions; AGENTS.md §6 paragraph.

> **Advisory:** `investigate`, `decide`, `choose`, and `determine` usually mean planner work remains. Resolve it before dispatch or use a spike. This is not a gate or regex score.

## Expected files
| Action | Repo-root-relative path | Responsibility |
|---|---|---|
| Modify | `.github/workflows/pr.yml` | trigger, if, concurrency |
| Modify | `.github/workflows/board-regate.yml` | concurrency, permission, guard |
| Modify | `scripts/pr-workflow.test.mjs` | structural assertions |
| Modify | `plugins/kanmer/skills/kanmer-setup/SKILL.md` | link and fenced block copy |
| Modify | `scripts/agents-block-body.mjs` | canonical body |
| Modify | `plugins/kanmer/scripts/agents-block-body.mjs` | generated by `npm run plugin:build` |
| Modify | `AGENTS.md` | block refresh (generated by `scripts/agents-block.mjs`) and §6 prose |
| Modify | `scripts/verify-skill-prose.mjs` | check 21 |
| Modify | `scripts/verify-skill-prose.test.mjs` | spawn assertions and check-21 case |

## Do not modify
- `packages/**` (no core or server change; the MCP bundle bytes must stay identical).
- `kanmer-gate` and `regate` job bodies, `check-pr.mjs`.
- `.worktrees/kanmer` and the `kanmer-board` branch (the operator re-copies the hook).

## Constraints
- `plugin:check` compares the packaged setup runtime byte-for-byte: run `npm run plugin:build` from a checkout with `node_modules`.
- `verify-agents-block.mjs` check 7 pins the SKILL.md fenced block to `BLOCK_BODY`: edit both identically.
- `pr-workflow.test.mjs` matches the existing `regate` `if:` verbatim; do not reformat it.
- Every existing sentence pinned by `verify-skill-prose.mjs` stays.

## Ordered steps
1. Edit `pr.yml` (`verify.if`, concurrency) and `board-regate.yml` (concurrency, permission, guard).
2. Update `scripts/pr-workflow.test.mjs`: new positive and negative assertions; AGENTS.md sentence assertions for the new paragraph.
3. AGENTS.md §6 paragraph: dispatch runs only `regate`; the hook dispatches only when an open PR exists; operator re-copy instruction.
4. Canonical block: remove " Native" in `scripts/agents-block-body.mjs` and the SKILL.md fenced copy; `node scripts/agents-block.mjs .` refreshes AGENTS.md; `node scripts/verify-agents-block.mjs` green.
5. `kanmer-setup/SKILL.md:169` unlinked reference.
6. `verify-skill-prose.mjs` check 21 plus `verify-skill-prose.test.mjs` `spawnValidator` helper and check-21 mutation case. Expected: `npm run verify:skills` PASS on the tree; the mutation case FAILs exactly check 21.
7. `npm run plugin:build && npm run plugin:check` (packaged setup runtime refreshed; bundle bytes unchanged).
8. Full rail `npm run verify` (TMP/TEMP outside the home folder until MCP-056 ships), commit, push, PR with footer `Kanmer: CORE-139`, post-implementation report, move to Review.

## Acceptance checks
- `node --test scripts/pr-workflow.test.mjs` exit 0 with the negative assertion present.
- `npm run verify:skills` exit 0; the check-21 mutation test proves a `](../x.md)` link fails exactly that check.
- `node scripts/verify-agents-block.mjs` exit 0; `grep -c "launcher. Native"` over AGENTS.md, `scripts/agents-block-body.mjs`, `plugins/kanmer/skills/kanmer-setup/SKILL.md` = 0.
- `npm run plugin:check` exit 0; `git diff --stat plugins/kanmer/mcp/kanmer-mcp.cjs` empty.
- `npm run verify` exit 0.
- After merge and the operator re-copy: a board push yields a `pr.yml` run with `verify` skipped; a board push with no open PR dispatches nothing.

## Commands
- Focused: `node --test scripts/pr-workflow.test.mjs`; `node --test scripts/verify-skill-prose.test.mjs`; `npm run verify:skills`; `node scripts/verify-agents-block.mjs`.
- Full: `TMP='C:\kt-tmp' TEMP='C:\kt-tmp' npm run verify` from the ticket worktree.
- Post-merge: `gh run list --workflow pr.yml --event workflow_dispatch --limit 5 --json databaseId` then `gh run view <id> --json jobs` shows `verify` skipped.

## Failure and deviation rules
Stop and report: any failing existing assertion in `pr-workflow.test.mjs` or `verify-skill-prose.mjs` that the plan did not name; any change needed under `packages/**`; a `plugin:check` bundle-byte mismatch; a new dependency.

## Stop condition
PR open with `Kanmer: CORE-139`, post-implementation report written, ticket in Review. Do not merge; do not re-copy the board-branch hook (operator step); do not start another ticket.
