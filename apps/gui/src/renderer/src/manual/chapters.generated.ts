// GENERATED by scripts/build-manual.mjs — do not edit.
//
// Committed deliberately: the renderer CSP forbids fetching anything at
// runtime, and the packaged app does not ship /docs/. Regenerate with
// `npm run build:manual`; `--check` fails when this file is stale.

export interface ManualChapter {
  id: string;
  title: string;
  /** Markdown. */
  body: string;
}

export const MANUAL_CHAPTERS: readonly ManualChapter[] = Object.freeze([
  {
    "id": "getting-started",
    "title": "Getting started",
    "body": "Kanmer is a kanban board that lives in your repository as markdown files, so\nyou and the coding agents you work with are looking at the same thing. There is\nno server and no account: the board is `.kanmer/` on disk.\n\n## The shape of it\n\nA **ticket** is a folder. Beside it live its documents — research, plan,\nchecklist, proof — each in a folder of its own, and any reference files you drop\nin.\n\nA ticket moves through six stages: **Backlog → Preparing → Implementing →\nReview → Verifying → Done**. All six are columns on one board, Backlog first —\nthere is no second place to look. Kanmer did once give Backlog a dedicated list\nview, on the argument that a long queue is something you scan rather than\nsomething you look at all at once; keeping the same tickets in two places cost\nmore than the list was worth, so the list was withdrawn and Backlog came back to\nthe board.\n\n## What a ticket owes\n\nNot the same thing for every ticket. Each has a **profile** that decides which\ndocuments each stage boundary asks for. A `feature` owes research, a plan and\nproof; a `chore` owes a plan and proof; a `spike` may owe only research. Pick\nthe profile when you create the ticket — filing a two-line fix as a `feature`\nmeans owing six documents nobody needs.\n\nA move crosses **one** gated boundary at a time. If a jump is refused even\nthough every document exists, that is why, and the message names the next stage.\n\n## Working with agents\n\nConnect an agent host in **Settings → Connect**. It registers Kanmer's MCP\nserver for that project, so the agent can read and move tickets itself.\n\n**Dispatch** hands a ticket to a background agent for **one deliverable** — map\nthe files, write the plan, run the verification — and it stops when that\ndeliverable exists. The menu shows what each task will produce.\n\n## Sharing a board\n\nIn a Git repository the board lives on its own branch in `.worktrees/kanmer`, so\nboard changes never mix with code changes and a pull request is never noisy with\nticket edits. Turn on automatic sync in **Settings → Git**."
  },
  {
    "id": "troubleshooting",
    "title": "Troubleshooting",
    "body": "## The board is empty, or a ticket will not open\n\nCheck the project you opened is the one holding `.kanmer/`. In a Git repository\nthe board lives in `.worktrees/kanmer` on its own branch — Kanmer finds it for\nyou, but a folder opened by hand may not be it.\n\n## A move is refused\n\nTwo different refusals, and the message distinguishes them.\n\n**\"requires \\<document\\>\"** — the ticket's profile asks for something that is not\nwritten yet. Write it, or change the profile if the work is smaller than the\nprofile assumes.\n\n**\"crosses N document gates\"** — the jump is too big. A move crosses one gated\nboundary at a time; the message names the next stage to move to. Every document\nexisting does not make the jump legal, because the stages are meant to be walked.\n\n## An attachment does not satisfy a gate\n\nIt never will. Files under `reference/` are inputs to the work — a mockup, a\nspec, a log — and `scratch/` is a notepad. Neither can satisfy a requirement.\nPipeline documents go in their own folders.\n\n## An agent cannot see the board\n\nReconnect it in **Settings → Connect**. If the agent runs its own MCP server\nprocess, it holds the version it started with — restart it after updating\nKanmer, or it keeps reading the board with the old code.\n\n## Automatic sync is not syncing\n\nCheck **Settings → Git**: sync is off unless an interval is set. If a sync\nconflicts it pauses and keeps your local work rather than resolving it for you;\nthe error says what happened and the button becomes **Retry**.\n\n## Renaming the board branch\n\nDo it from **Settings → Git**, not from the command line. Renaming there moves\nthe existing branch and pushes it before deleting the old one, so no history is\nstranded. Projects that were closed at the time are migrated the next time they\nopen."
  },
  {
    "id": "profiles",
    "title": "Requirement profiles",
    "body": "# FRD-002 — Requirement profiles\n\n*Full specification: `docs/functional/frd/FRD-002-requirement-profiles.md`.*"
  },
  {
    "id": "stages",
    "title": "The six stages",
    "body": "# FRD-007 — The fixed six-stage board\n\n*Full specification: `docs/functional/frd/FRD-007-fixed-six-stage-board.md`.*"
  },
  {
    "id": "documents",
    "title": "Ticket documents",
    "body": "# FRD-003 — Ticket documents\n\n*Full specification: `docs/functional/frd/FRD-003-ticket-documents.md`.*"
  },
  {
    "id": "groups",
    "title": "Epics and horizons",
    "body": "# FRD-001 — Groups\n\n*Full specification: `docs/functional/frd/FRD-001-groups.md`.*"
  },
  {
    "id": "proof",
    "title": "Proof",
    "body": "# FRD-006 — Typed proof\n\n*Full specification: `docs/functional/frd/FRD-006-typed-proof.md`.*"
  },
  {
    "id": "references",
    "title": "Reference files",
    "body": "# FRD-004 — Reference files\n\n*Full specification: `docs/functional/frd/FRD-004-reference-files.md`.*"
  },
  {
    "id": "dispatch",
    "title": "Dispatching agents",
    "body": "# FRD-010 — Task-scoped dispatch\n\n*Full specification: `docs/functional/frd/FRD-010-task-scoped-dispatch.md`.*"
  },
  {
    "id": "board-sync",
    "title": "Sharing the board over Git",
    "body": "The board can live on its own branch, shared across machines, without ever touching your source branch.\n\n- R1. The board root is `<repo>/.worktrees/kanmer`, checked out on a configurable branch (default `kanmer-board`, global setting); the source checkout remains the project tab. MCP calls receive the board root; agent execution and project config use the source root.\n- R2. First-time setup migrates an existing `.kanmer/` to the branch only after a successful commit; source-branch cleanup is left **staged and visible**, never auto-committed. `.kanmer/` and `.worktrees/` are gitignored at the source; `activity.jsonl` is ignored on the board branch.\n- R3. **Automatic sync** (off by default; whole-minute interval): add scoped to `.kanmer` + the board `.gitignore` → commit if dirty → fetch/rebase if the remote branch exists → push. Never `--force`, never any other branch; a rebase conflict aborts, preserves local commits, and **pauses that project** with a visible error + Retry. \"Sync now\" always available.\n- R4. Non-Git folders keep colocated `.kanmer/` with sync shown unavailable.\n- R5. **Branch rename** should migrate all known projects safely — push the new branch before deleting the old, worktree path unchanged so MCP registrations stay valid. **Not built** (see the gap note below); the end state is specified here so the eventual implementation has a target.\n\n**Acceptance (as-built):** the Phase 9 verification list — real-repo tests for orphan creation, migration byte-preservation, conflict pause, and cross-machine recovery. (Rename is excluded: see R5.)\n\nRelated: docs/plans/kanmer-v2/phase-9 · kanmerGit.ts · FRD-018 (watcher picks up synced changes).\n\n*Full specification: `docs/functional/frd/FRD-020-board-git-worktree-sync.md`.*"
  },
  {
    "id": "shortcuts",
    "title": "Keyboard shortcuts",
    "body": "Generated from the app's own binding table, so it cannot drift from what\nthe keys actually do.\n\n| Keys | Does | Where |\n|---|---|---|\n| `Ctrl+K` | Command palette | Anywhere |\n| `Ctrl+N` | New ticket | Anywhere |\n| `Ctrl+F  /  /` | Focus search | Anywhere |\n| `Ctrl+,` | Settings | Anywhere |\n| `F1` | Manual | Anywhere |\n| `Ctrl+1…3` | Switch view (Board, Standup, Archived) | Anywhere |\n| `Ctrl+Tab` | Next project tab (Shift for previous) | Anywhere |\n| `Escape` | Close the topmost panel, else deselect | Anywhere |\n| `← / →` | Move the selected ticket a stage | Board |"
  }
]);
