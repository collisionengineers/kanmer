2026-08-24 correction: the main checkout is 208 commits behind origin/main, so its root verification failure is historical evidence only. A first current-main focused run in the GUI-127 worktree also resolved @kanmer/core from the stale parent checkout because the worktree lacked its own dependency/build state; 19 resulting assertions named a missing withExclusiveFileLock export and are classified as harness setup failures, not source failures. The same run independently reproduced one afterEach 10-second timeout plus EPERM fixture-removal failure. Install/build the worktree before evaluating the current baseline.
