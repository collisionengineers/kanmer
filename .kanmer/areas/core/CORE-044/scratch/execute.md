2026-08-22 — Implementation progress: exact base b5ae6f36; core schema/resolver, board locking updateBoard, manual public same-origin fetch, cache revalidation/atomic locking, source schema derivation, canonical tool/skill/docs rails, .gitignore, and source tests are implemented. First focused command `npm run test -w @kanmer/core -- --run packages/core/src/sources.test.ts packages/core/src/store.test.ts` exited 1 (Vitest path filter found no files); corrected command `npm run test -w @kanmer/core -- src/sources.test.ts src/store.test.ts` exited 0 (91/91). First MCP source rail failed 1/7 because the new root-304 fixture used an invalid Response 304 body; corrected fixture and rerun passed 11/11. No full rails or PR yet.

2026-08-22 — Rail updates: `npm test` first exited 1 at `check:manual` because generated chapters.generated.ts was stale; `npm run build:manual` corrected it. The next full run reached core 290/290 and GUI 382/382, then MCP HTTP exited 1 with 78/80: unrelated environment-sensitive project-resolution spawnSync ETIMEDOUT and tunnel readiness timeout. The authoritative MCP source cases passed 12/12 within that run; the prior complete source/HTTP rerun passed 80/80. These first failures remain INCONCLUSIVE for the shared environment and are not attributed to CORE-044 source behavior.
