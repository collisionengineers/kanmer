---
id: GUI-084
type: ticket
title: Fix desktop notification wording and styling for doc updates
status: implementing
area: gui
assignee: codex-mcp-client
profile: fix
stageEntered:
  preparing: '2026-08-20T21:36:03.797Z'
taken_at: '2026-08-21T19:21:21.225Z'
branch: gui-084-notification-wording-style
worktree: .worktrees/gui-084
labels:
  - bug
  - ux
  - notifications
groups:
  - HZN-005
  - HZN-007
links: []
refs:
  - docs/functional/frd/FRD-018-live-sync-and-notifications.md
archived: false
created: '2026-08-16T22:43:54.623Z'
updated: '2026-08-21T19:28:19.268Z'
---

## What

Desktop push notifications for ticket document changes (e.g. "research updated") should name the ticket they refer to and use normal title casing, and the notification's grey background should be re-themed to match the app.

## Why

Screenshot attached by the user shows a native toast reading just "research updated" — lower case, generic, and with no indication of which ticket the change belongs to. That's the fallback wording in `flushToasts()` (`apps/gui/src/main/index.ts`), which fires when the batched toast `key` is a document type (e.g. `research`) rather than a ticket id, so the `store.getItem(key)` lookup in the ticket-id branch never resolves and the title falls back to `` `${key} ${event} updated` ``. The intended path (same function) already builds a proper `` `${key} — ${stage}` `` title with the item's own title as the body when `key` is a ticket id — this only needs to also work for doc-level events. Separately, the notification chrome is plain grey and doesn't match Kanmer's theme.

## Approach

- [x] Research phase reconciled: `classifyKanmerPath` now maps format-3 document and scratch paths to the owning ticket; Electron native `Notification` chrome is OS-owned and cannot be portably themed.
- [x] Existing merged-main path resolves the owning ticket and renders `` `${ticketId} — ${stage}` `` with `stageName` and the ticket title as body; no new source change is required.
- [x] Styling contract resolved: retain native OS chrome and document the platform limitation; a Windows-only `toastXml` redesign is not selected.

## Verification

- [x] Merged-main code and classifier tests confirm document events resolve to ticket ids and the native toast title uses normal stage casing; no unsupported interactive OS capture is claimed.
- [x] Native styling constraint verified from the Electron surface and FRD-018: OS-owned chrome cannot be portably themed; no visual pass is claimed.

## Outcome

Resolved by merged-main evidence reconciliation. Existing commit 360392777d41e453dcd2edbaa9ee251dab38bce1 is reachable from d9379d32; no source changes or new PR were needed. Native OS chrome remains explicitly accepted under FRD-018, and proof/closeout remain post-review work.
