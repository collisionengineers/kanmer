# Where the change lands

| Path | Why |
|---|---|
| `scripts/agents-block.mjs` | `BLOCK_BODY` — the canonical literal. |
| `plugins/kanmer/skills/kanmer-setup/SKILL.md` | The hand-duplicated fenced copy; must match byte for byte. |
| `AGENTS.md` (this repo) | Regenerated by running the script — the repo eats its own output. |

## Context files

| Path | What it tells the implementer |
|---|---|
| `scripts/verify-agents-block.mjs:146-154` | The byte-identity assertion: it checks the skill *contains* the script's literal, so any drift fails. This is the safety net for the hand duplication. |
| `docs/architecture/adr/ADR-0009-...md` | Layer 3: the block orients, it does not gate. Prose restating rules goes stale — this block is the evidence. |
| `packages/core/src/profiles.ts` `DEFAULT_PROFILES` | Why a single pipeline cannot be stated: there are five, and which applies is per ticket. |
| `packages/core/src/gates.ts` `collapsesPipeline` | The one-gated-boundary rule an agent will otherwise meet as an error. |
