---
id: DOC-007
type: ticket
title: Rewrite the in-app manual as a real user guide instead of FRD stubs
status: done
area: docs
assignee: claude-code
profile: feature
stageEntered:
  preparing: '2026-08-16T21:51:46.492Z'
  review: '2026-08-16T23:22:16.258Z'
  verifying: '2026-08-16T23:25:11.646Z'
  done: '2026-08-16T23:31:39.345Z'
taken_at: '2026-08-16T22:43:52.530Z'
branch: doc-007-manual-user-guide
worktree: .worktrees/doc-007
labels:
  - manual
  - user-facing
groups:
  - HZN-003
links:
  - GUI-017
refs:
  - docs/functional/frd/FRD-024-in-app-manual.md
commits:
  - '5819157'
  - 19244f62d05ddf64ff7aa52ea4cf34342798013f
prs:
  - 'https://github.com/collisionengineers/kanmer/pull/49'
archived: false
created: '2026-08-16T14:24:23.643Z'
updated: '2026-08-16T23:33:11.791Z'
---

## What

**8 of the manual's 12 chapters are stubs** — an FRD's H1 and a pointer line,
~100 characters each. Replace them with written user-facing chapters, and stop
the generator deriving user documentation from internal specs.

## Why

Run the generated chapters and the state is unambiguous:

| Chapter | Body |
|---|---|
| getting-started | 1799 |
| troubleshooting | 1772 |
| board-sync | 1761 |
| shortcuts | 717 |
| profiles, stages, documents, groups, proof, references, backlog, dispatch | **82–116 chars each** |

Every stub is literally this:

```
# FRD-002 — Requirement profiles

*Full specification: `docs/functional/frd/FRD-002-requirement-profiles.md`.*
```

Three things are wrong, in order of severity:

1. **The chapters are empty.** A user opening Help → Manual → Requirement
   profiles learns nothing at all.
2. **It exposes our internal document scheme.** PRD/FRD/ADR ids are a
   contributor concern. The manual may *explain* that Kanmer's process uses
   governing docs — that is a product concept — but it must never print
   `FRD-002` as a chapter title or send a user to a path that is not in the
   installed app.
3. **The pointer line is a dead end.** `docs/functional/frd/…` does not ship in
   the package (`scripts/build-manual.mjs:5-8` says so), so the one line each
   stub does contain points at a file the reader cannot open.

How it got here: `build-manual.mjs:41-47` takes each FRD's "lead prose" —
everything above the first `## ` — but these FRDs go straight from H1 to their
first section, so the lead is just the heading. The `if (!body) throw` guard at
`:66` was meant to catch exactly this and did not fire, because the H1 strip at
`:46` left a non-empty string. The guard is testing the wrong thing.

[[GUI-017]] built the manual mechanism; this ticket is the content it needs.

## Approach

- Write the 8 chapters by hand into `docs/manual/`, in the user's vocabulary:
  what the thing is, when to use it, what it looks like in the app. No
  requirement ids, no repo paths, no "full specification" footer.
- Drop the `FROM_FRD` derivation from `build-manual.mjs`. An FRD is written for
  an implementer; a manual assembled from FRDs is the wrong shape however much
  prose they have. The script's own comment at `:22-27` already argues this — it
  just stopped one step short.
- Replace the emptiness guard with one that cannot be satisfied by a heading:
  minimum prose length after stripping headings, and no `FRD-`/`ADR-`/`PRD-`
  token in any chapter body or title.
- Keep the two generated chapters that are genuinely generated — shortcuts (from
  the binding table) — and keep `--check`.
- Amend FRD-024 to say the manual is hand-written; it currently permits the
  derivation.

## Verification

- [x] Every chapter body is substantive prose; the shortest is well past the
      current 82 characters. — **shortest authored chapter is 2462 chars, 30×**
- [x] No chapter title or body contains `FRD-`, `ADR-`, `PRD-`, or a
      `docs/…` repo path — asserted in `manual.test.ts`, so it cannot regress.
      **Also asserted in the build, and extended to requirement lines and
      body-level H1s.**
- [x] `npm run check:manual` passes and the committed artifact is current.
      **And it is now actually invoked — it was reached by nothing before.**
- [x] Each chapter answers its own title for someone who has never seen the
      repo. *(Mechanised half verified — rendering, cross-references, no stray
      H1. The judgement half is the author's, who was also the reviewer; said so
      in the proof rather than claimed otherwise.)*

## Outcome

**Shipped in PR #49**, squash-merged as `19244f6`.

**19 chapters**, replacing 12 of which only 3 were fit to ship:
`getting-started` (What Kanmer is) · `install` · `connect` · `first-ticket` ·
`stages` · `profiles` · `gates` · `documents` · `references` · `proof` ·
`groups` · `dispatch` · `board-sync` · `sync` · `settings` · `shortcuts`
*(generated)* · `updates` · `troubleshooting` · `glossary`.

All twelve pre-existing chapter ids survive, so nothing that deep-links into the
manual later lands on the wrong chapter.

### Shipped differently than planned

**19 chapters, not the planned 20.** [[GUI-070]] merged mid-ticket and withdrew
the separate Backlog view — the tab, the table, multi-select and bulk actions.
The `backlog` chapter was written, then deleted; Backlog is a stage like any
other now and the `stages` chapter covers it, including why the list view went.
Shipping a chapter about a view that no longer exists would have been the same
defect this ticket removes. `groups` was corrected for the same reason: bulk
"Add to group…" was the only group-assignment UI in the app and went with the
table, so that chapter now states the gap plainly.

**The guard has six rules, not the two the ticket named.** Added beyond scope: a
requirement-line ban (`R1.`, `AC2.`) because the worst chapter was not a stub
but 1761 characters of requirement list — length was never the measure — and a
body-level `# ` heading ban, which makes the original bug unrepresentable rather
than merely unlikely. The `docs/…` path ban is scoped to `docs/` specifically so
the legitimate `.kanmer/` paths in getting-started still pass; verified
explicitly.

**Four FRD claims were found stale** while fact-checking against shipped code,
and would each have put a bug in the manual: board-branch rename is built
(FRD-020 R5 says "Not built"); notifications default on and live in Appearance;
dispatch never creates a worktree; and Backlog.

### Follow-ups filed

- [[DOC-008]] — README is stale in three user-visible ways and now contradicts
  the manual. Parked deliberately by the operator during this ticket.
- [[GUI-087]] — `friendlyGateError` is dead code, so users see `set_ticket_doc`
  in gate refusal banners. Found while writing the gates chapter, which is
  written to be honest about the current behaviour; revisit that section when
  GUI-087 lands.
