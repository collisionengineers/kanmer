Stopped before PR/review: clean standalone checkout ran `npm ci` then `npm run verify`; the rail exited 1 at its mandated first command `npm test`. Fresh checkout has no tracked or generated `packages/core/dist` (`git ls-files packages/core/dist` empty; `Test-Path` false), while GUI stages/dispatch tests import `@kanmer/core` through that dist entry. The approved rail builds third, so do not reorder or inject a prebuild without an explicit plan decision. Resume on `core-031-shared-verify-steps` in `.worktrees/core-031` after that disposition.
