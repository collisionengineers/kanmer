---
id: MCP-009
type: ticket
title: Bring plugin install to parity across all five providers
status: done
area: mcp-server
order: 1050
assignee: claude-code
profile: feature
stageEntered:
  preparing: '2026-08-16T21:51:12.360Z'
  review: '2026-08-16T22:51:36.099Z'
  verifying: '2026-08-16T22:56:14.567Z'
  done: '2026-08-16T23:05:39.743Z'
labels: []
groups:
  - HZN-003
links: []
refs:
  - docs/functional/frd/FRD-012-connect.md
  - docs/architecture/adr/ADR-0009-skills-are-not-the-contract.md
commits:
  - b3a70ac
  - 9d316e7
  - c81063e
prs:
  - 'https://github.com/collisionengineers/kanmer/pull/44'
archived: false
created: '2026-08-16T18:25:18.699Z'
updated: '2026-08-16T23:19:36.286Z'
---

## What

Easy install across every supported provider, with the per-provider variance
that actually requires — Claude and Codex at minimum, then opencode,
Antigravity and grok CLI.

**Scope narrowed by operator decision (Q3): this ticket shipped the DOCUMENTS
only.** The code corrections its research turned up are filed as [[MCP-013]],
[[MCP-014]], [[MCP-015]] and [[GUI-079]].

## Why

A marketplace already exists, and the answer to "do we have one" is *partly*:

- `.claude-plugin/marketplace.json` — Claude, one plugin, `./plugins/kanmer`
- `.agents/plugins/marketplace.json` — the agents format, with an install policy
- `plugins/kanmer/.claude-plugin/plugin.json` and
  `plugins/kanmer/.codex-plugin/plugin.json` — two plugin manifests

So this is parity and coverage work, not greenfield. What is unclear is whether
each provider's install path is **tested**, and whether the manifests agree with
each other and with `providers.ts`.

## The failure mode to guard against, named correctly

ADR-0009 records that it once carried a wrong premise — that two hosts received
no skills at all — and attributes it to provider research having "gone stale in
weeks". **That framing is wrong and should not guide this ticket.** Provider
capability gaps do not go stale: opencode and Antigravity had supported skills
for a long time, and no host is going to *remove* skill support. The mistake was
assuming a capability was absent without checking it.

So the instruction here is not "re-verify because things change". It is:

- **Never conclude a provider lacks a capability from the absence of evidence.**
  Check for the capability, and record what you found and where you looked.
- Treat every "provider X can't do Y" claim in this repo as unverified until
  someone has looked. [[GUI-073]] is one such claim already caught — Antigravity
  labelled register-only when it has project-level support — and it is unlikely
  to be the only one.

## How to check: the binary, not only the docs

Documentation lags and omits. **The installed CLI is the ground truth**, so every
capability claim is checked against the tool itself as well as its docs:

- `claude --help`, `codex --help`, `opencode --help`, and the grok and
  Antigravity equivalents — then the relevant subcommand's own help
  (`claude mcp --help`, `claude plugin --help`, and so on). Subcommands that
  exist and are undocumented are common; the reverse happens too.
- Where a config file is the interface rather than a flag, read a real one the
  tool has written rather than a documented example.
- Record the command run and its output in the research document. A capability
  claim with no command behind it is the same assumption this ticket exists to
  stop, wearing a citation.

Note that the agent doing this work may not have every CLI installed. That is a
finding to report, not a reason to fall back on assumption.

## Also in scope: fix the source of the bad instruction

**ADR-0009's staleness clause is amended by this ticket.** It currently tells
future readers to re-verify provider specs "at implementation time" because
"skill-ecosystem facts go stale in weeks" — the wrong lesson, in the document
with the most authority, which is how it propagated into the first draft of this
ticket. Replace it with the absence-of-evidence rule and the check-the-binary
method above. Amending a merged ADR's reasoning is a real decision, so it is
called out here rather than done quietly.

## Approach

- Establish what parity means: same skills, same MCP registration, same install
  entry point — and where a provider genuinely cannot match, say so with the
  evidence, not the assumption.
- Audit the existing "cannot" claims across `providers.ts`, the manifests and
  FRD-012's install matrix, and check each one by the method above.
- Note the convergence already recorded in ADR-0009: one project-scoped write to
  `.agents/skills/` serves both opencode and Antigravity.
- Keep FRD-012's install matrix as the single description, corrected to match.

## Verification

- [ ] Each of the five providers installs from a documented path, tested rather
      than asserted. — **Tested** (all five install; `research` Findings 1-4, 9),
      but correcting the *shipped* paths is [[MCP-013]] / [[MCP-014]] /
      [[MCP-015]] after the split.
- [x] Every capability claim cites **both** the doc and the command run against
      the real CLI, with its output.
- [ ] The manifests agree with each other and with `providers.ts`. — The
      disagreements are documented (`research` Finding 6); reconciling them is
      [[MCP-011]] and [[MCP-013]].
- [x] FRD-012's install matrix matches reality.
- [x] ADR-0009's staleness clause is replaced with the absence-of-evidence rule
      and the check-the-binary method.
- [x] Any CLI that could not be checked is named as unchecked, not assumed. —
      None were unchecked; all five binaries were present. The Antigravity *IDE*
      is recorded as **parked**, explicitly distinguished from unchecked.

The two unticked boxes are code, deliberately out of scope after the operator's
split. Both are named in FRD-012's `Related:` open-work list so they are findable
from the document, not only from the board.

## Outcome

**Shipped** in PR [#44](https://github.com/collisionengineers/kanmer/pull/44),
squash-merged as `c81063e` on 2026-08-16. Docs only — two files, 24 insertions,
8 deletions, nothing outside `docs/`.

- **ADR-0009** — the staleness clause is gone. In its place, a named
  `### Method: how a provider capability claim is established`: establish against
  the installed binary, never infer from the absence of evidence, record the
  command and its output, prefer the host's own resolved-configuration dump, read
  a config file the tool itself wrote, a probe needs a positive control, an
  unchecked CLI is a finding. It states explicitly that the original failure was
  **not** decay. The Context paragraph and Consequences gained the matching
  caveats; the **Decision is untouched**.
- **The sharper rule the ticket did not anticipate** — *a positive control is
  necessary but not sufficient: verify the mechanism you are actually testing,
  not a proxy for it.* Shipped with the worked example that produced it.
- **FRD-012** — R2 is now a per-host matrix with an owning ticket on every
  divergence; R4's shared-directory clause widened to match; AC2 restated with
  the workspace-binding precondition and a call-the-tool check; R5 replaced by a
  pointer to the ADR clause, since paraphrasing it is how the wrong lesson spread.

**A conclusion was overturned before it shipped.** The research concluded that
`.agents/skills/` does *not* serve Antigravity and drafted an ADR clause saying
so. An adjudication (ten runs, positive controls throughout, corroborated by the
probe MCP server's process log) established [[GUI-073]] was right and that
conclusion false. **The clause was not shipped.** ADR-0009's convergence claim
holds, gains grok as a third host, and carries one caveat: `agy` reads
`.agents/skills/` only in a **workspace-bound** session, and Kanmer binds nothing
today, so the write is correct and currently **inert**.

### Handed on, with the evidence, in `research`'s addendum

- [[MCP-013]] — marketplace root + packaging; the swallowed non-zero exit at
  `connect.ts:152`; the two marketplace names; the two `${…}_ROOT` variables;
  the unchosen `--scope user` default.
- [[MCP-014]] — grok to the plugin path; `.grok/skills` is a redundant write.
- [[MCP-015]] — Antigravity workspace binding (`--add-dir` / `--project` /
  `--new-project`) and `dispatch: true` with `["-p", prompt]`. Also inherits the
  three tested **non**-gates: trust, a git root, and project existence.
- [[GUI-079]] — the `.mcp.json` collision. [[MCP-011]] — the frozen `0.1.0`.
- [[GUI-073]] owns *saying* Antigravity is not register-only; not edited here.

### Reported, not filed

`apps/gui/src/main/kanmerGit.test.ts` is a pre-existing Windows `EPERM` flake
(temp git-worktree `rmSync` in `afterEach`), failing a different subset each run.
Proven pre-existing by running it at `2f06713`, the commit *before* the merge,
where it also fails. Filing is outside this ticket's docs-only scope.
