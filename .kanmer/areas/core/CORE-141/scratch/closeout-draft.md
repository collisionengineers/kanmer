```text
Kanmer-first cutoff report
Observed at: 2026-09-05 15:48 BST / 2026-09-05T14:48:00Z
Adopted runtime / plugin / skill / managed-policy revisions: NOT RUN — live control plane still 0.4.1 (packaged, %LOCALAPPDATA%\Kanmer\mcp\0.4.1-7432, sha256 3f7af329…). Source ready for 0.4.2 at origin/main 410bfd22; no MCPB/plugin/skills adoption has happened yet.
Route: qualified standalone MCP + plugin/skills cut (desktop installer carried forward on Electron 31.7.7, sandbox: false, no new runtime/security claim)

M1 coherent instructions: PASS pending adoption — evidence: DOC-028 merged (bd368549, PR #321), routes work by purpose and names the configured integration branch instead of hardcoding main; DOC-026 merged (37b83b14, PR #326) retires CLOSEOUT_PLAN.md. `get_status.repo` on the current 0.4.1-installed plugin already reports the managed AGENTS.md block as `behind` (stale, informational) against the 0.4.2-shipping content — expected until `kanmer-setup` is re-run after the 0.4.2 plugin is installed. Checks referenced: PR #310-#313 lineage plus this horizon's #321/#326.

M2 one heavy owner: PROCEDURAL — named verifier: Alex (operator), sole merger for this horizon per HZN-009 context.md "Operating controls". No automated lock exists yet; the `kanmer-verify` skill's evidence-first lookup (MCP-057, e474f317) waits on the bound post-merge `verify` run for the exact merge SHA rather than re-running it, which is the automated half of the control. The enforced heavy-verification permit is deferred to CORE-143 (R2/0.5.0); R1-LEASE is explicitly procedural per AMENDMENTS.md owner decision.

M3 matching evidence: automated + human-reviewed, result PASS on the merged set — receipts validated end-to-end: CORE-138 (9945b1f2, PR #324), CORE-144 (de5bace9, PR #327), CORE-145 (58718455, PR #328) and MCP-057 (e474f317, PR #325) proofs each recorded `PROOF_RECEIPT_REJECTED` correctly rejecting a non-matching receipt before accepting the true bound receipt, observed by the human reviewer (Alex) at review/verify time, not re-derived here. CORE-129 (410bfd22, PR #329) adds the typed-proof / attempts[] consistency validator itself; its own ticket is merged to main but still shows status `review` on the live board (proof/closeout not yet run against it) — provenance for that gap is human-observed, not machine-confirmed at this snapshot.

M4 Done separate from deployment: PASS — every R1 ticket that reached Done (CORE-138, CORE-140, CORE-144, CORE-145, DOC-026, DOC-028, GUI-152, MCP-057) recorded `delivery_state: integrated` on merge to main; `deploymentTracking` is off board-wide (get_status), and no deployment action (release tag, publish, install) has occurred for any of them. Disposable scenario: NOT RUN — the installed-route mutation drill (get_status → create_item → set_ticket_doc → get_doc_gates → move_item → list_items → archive on a mkdtemp board) is scheduled for the actual 0.4.2 cut (plan step 11), not this preparation step.

M5 actual host and safe promotion: NOT RUN — no MCPB/plugin build, no host install, no rollback drill has been performed in this preparation pass. Deferred to the operator's cut (plan steps 9-12).

Pegasus integration target and current baseline: out of scope for R1 per HZN-009 group body ("Pegasus is out of scope"); no target set here.

Active implementation owners / shared-path owner / verifier: implementer for this ticket's preparation — claude-code (worktree .worktrees/CORE-141, branch CORE-141-release-0.4.2); named heavy verifier and sole merger — Alex; lane owners per HZN-009 context.md: lane A (pr.yml, verify.mjs, check-pr.mjs, execute/review skills), lane B (agents-block-body.mjs + mirrors, kanmer-verify/SKILL.md, reconciliation.ts, proof-record.ts), lane C (apps/gui/src/**).

Release-critical subject-project blockers: none identified against Kanmer's own repo. CORE-129 remains in Review, not Done, on the live board — its code is merged and safe to release on, but its own ticket lifecycle should close (review -> verifying -> done) before or shortly after the 0.4.2 cut so the roster the release notes credit is fully closed out.

Unfinished R1 -> R2: CORE-142 (gate-only hosted required check + blocking attestation, needs repository administration); CORE-143 (implemented heavy-verification permit, currently procedural); GUI-153 (Focus Board UI-C+D: list view, conflict UI, time-in-stage, packaged qualification); R2-DESKTOP (Electron runtime upgrade + IPC/sandbox re-qualification); R2-EVIDENCE remainder (receipt store, reuse key, provenance adapter beyond MCP-057+CORE-129); the smoke "ready packet is read-only" flake (tracked, not fixed).

Live-board inventory/audit coverage: complete for this snapshot. get_status.counts now: byStage backlog 14, preparing 0 (this ticket has since moved to implementing via take_ticket), implementing 1 (CORE-141), review 1 (CORE-129), verifying 0, done 381, archived 30, taken 1, total tickets 396. Context.md OP-00 snapshot (2026-09-05 ~03:00 BST) recorded 384 tickets + 30 archived = 414, backlog 10, preparing 1, done 373. Delta since OP-00: +9 merged HZN-009 tickets reached Done (CORE-138, CORE-140, CORE-144, CORE-145, DOC-026, DOC-028, GUI-152, MCP-057, plus CORE-129 code-merged-but-Review) net done 373 -> 381 (+8 confirmed Done, CORE-129 pending); backlog 10 -> 14 (net +4, unrelated new backlog capture not itemised here); preparing 1 -> 0 net (CORE-129 left preparing for review; CORE-141 entered and left preparing for implementing in this session); review 0 -> 1 (CORE-129); implementing 0 -> 1 (CORE-141, this ticket's own preparation work).

Rollback: retained generation 0.4.1-7432 under %LOCALAPPDATA%\Kanmer\mcp\; release ledger attempt main@1 (v0.4.1, 4e94ad80, verification passed) is the last known-good published release with a full asset-digest manifest. Procedure: point host provider registration back at the 0.4.1 generation folder or reinstall Kanmer-Setup-0.4.1.exe from that ledger's asset manifest; confirm get_status.server.version reads 0.4.1; re-run a disposable mutation to confirm the board is still readable. No board migration needed either direction. NOT TESTED in this preparation pass (scheduled at plan step 12, the actual cut).

Resume decision and authority: NOT RUN — this is a preparation report, not a resumption decision. The operator (Alex) makes the actual go/no-go at the real cut per CORE-141's Verification section and this plan; nothing here authorizes running scripts/release.mjs for real, tagging, publishing, or touching the installed host.
```
