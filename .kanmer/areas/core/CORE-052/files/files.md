# Files

- `apps/gui/src/main/` or the existing branch-sync implementation: refresh destination equality and paused/error preservation.
- `.github/workflows/` or the existing workflow source: document and validate `KANMER_BOARD_BRANCH` handoff.
- `apps/gui/src/renderer/` manual/troubleshooting source: reconcile rename guidance.
- Tests covering branch refresh, workflow handoff, state preservation, and manual text.
- Generated/manual artefacts only where the existing build pipeline requires them.
