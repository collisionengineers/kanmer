# Files — MCP-049

## Committed files

| Path | Why |
|---|---|
| `docs/manual/connect.md` | Documents the stable launcher, complete wrapper environment, native runtime supervision, and the separate GUI lifecycle accurately. |
| `apps/gui/src/renderer/src/manual/chapters.generated.ts` | Generated in-app mirror of the corrected manual. |
| `scripts/agents-block-body.mjs` | Canonical source for the new native-runtime command convention. |
| `plugins/kanmer/skills/kanmer-setup/SKILL.md` | Installed-skill fenced mirror required by managed-block verification. |
| `AGENTS.md` | Repository generated instance of the canonical managed instructions. |
| `plugins/kanmer/scripts/agents-block-body.mjs` | Packaged setup-runtime copy generated by `npm run plugin:build`; required to keep the shipped setup helper synchronized. |

## Operational state (never committed)

- The existing OpenAI profile/runtime remains in the user profile with environment-only credential references.
- The installed GUI owns the existing Cloudflare runtime and its OS-protected bearer.
- No provider resource, credential, tunnel identifier, generated bearer, or machine-specific project value belongs in the repository.

## Production callers and boundaries

- The stable installed launcher is called by the operator-private wrapper; the wrapper exports both project cwd and board-branch convention.
- Native `tunnel-client runtimes connect/status/stop/remove` owns the long-lived OpenAI runtime.
- The GUI's OpenAI surface separately owns `init/doctor/run`; it does not manage native aliases.
- The GUI remote-access manager remains the existing Cloudflare caller; no parallel provider implementation was added.
