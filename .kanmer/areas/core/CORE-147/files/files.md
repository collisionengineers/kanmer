# Files — CORE-147

Base: `origin/main` @ `410bfd22`. Branch `CORE-147-verification-contract`,
worktree `.worktrees/CORE-147`.

## Core (contract + resolver + assessment)

| File | Change |
|---|---|
| `packages/core/src/types.ts` | `VerificationContractSchema` (zod, camelCase) inside `DeliveryConfigSchema.verification`; `VerificationContract` interface + `DEFAULT_VERIFICATION_CONTRACT`; `DeliveryPolicy.verification`; `ReconciliationEvidence.verification?` (additive). |
| `packages/core/src/board.ts` | `resolveDelivery` resolves `verification`; new `deliveryVerificationSource(board)` mirroring `deliveryPolicySource`. |
| `packages/core/src/proof-receipts.ts` | `assessReceipt(receipt, { mergedSha, contract? })` reads the contract; positive-integer `run_id`/`attempt`, non-empty `provider`/`repo`; new `assessReceiptSet(receipts, { mergedSha, contract })` reporting `incomplete` job coverage. |
| `packages/core/src/reconciliation.ts` | `receiptAssessmentRejections` uses `assessReceiptSet` with `evidence.verification ?? DEFAULT_VERIFICATION_CONTRACT`; `stableEvidence` copies the new field. |
| `packages/core/src/release.ts` | comment only — `deliveryPolicyVersion` deliberately keeps its four fields (no digest change). |

## MCP server

| File | Change |
|---|---|
| `packages/mcp-server/src/index.ts` | `get_status.delivery` exposes `verification` (via the resolved policy) and `verificationSource`. |
| `packages/mcp-server/src/reconciliation.ts` | `collectReconciliationEvidence` assembles `verification` from `resolveDelivery(board)`. |

## Tests

| File | Change |
|---|---|
| `packages/core/src/proof-receipts.test.ts` | board-declared contract acceptance/rejection, incomplete job set, `run_id: 0`/`"abc"`, `attempt: 0`, `provider`/`repo`. |
| `packages/core/src/delivery.test.ts` | `resolveDelivery` default + declared `verification`, `deliveryVerificationSource`, policy-version stability. |
| `packages/core/src/reconciliation.test.ts` | contract-bound `PROOF_RECEIPT_REJECTED`. |
| `packages/mcp-server/src/reconciliation.test.mjs` | fallback proof (`receipts: []` under a `ci.yml`/`["build","test"]` contract → no `PROOF_RECEIPT_*`, same recommendation) and the rejected `pr.yml`/`verify` receipt under that contract. |
| `packages/mcp-server/src/smoke.mjs` | `get_status.delivery.verification` shape. |

## Prose / generated

`plugins/kanmer/skills/kanmer-verify/SKILL.md` (steps 3–5, coverage rule, fallback),
`plugins/kanmer/skills/kanmer-setup/SKILL.md` (declaring the contract),
`docs/manual/proof.md`, `docs/functional/frd/FRD-006-typed-proof.md`,
`docs/functional/frd/FRD-031-configurable-delivery-and-release-state.md`,
`AGENTS.md` §4 board.yml example,
`apps/gui/src/renderer/src/manual/chapters.generated.ts` (generated by `build:manual`),
`plugins/kanmer/mcp/kanmer-mcp.cjs` (generated by `plugin:build`).

## Deliberately untouched

`scripts/verify.mjs`, `scripts/agents-block-body.mjs`, `.github/workflows/pr.yml`,
`apps/gui/src/**` hand-written sources (Settings does not render `delivery`),
`.kanmer/board.yml` (Kanmer keeps the default contract — FRD-031),
`defaultBoardConfig()`, the tool roster (stays 41).
