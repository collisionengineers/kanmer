# Proof — GUI-088

PR [#74](https://github.com/collisionengineers/kanmer/pull/74) merged to `main` as `eba5631db6f5465937637c0d95e089aae26d6dfe`.

Merged-main verification: `npm test -w @kanmer/gui -- connect.test.ts` (22 passed), `npm run typecheck -w @kanmer/gui` passed, and `git diff --check` is clean.

Every provider now receives the idempotent AGENTS.md managed block before marketplace/copy-skills installation diverges.
