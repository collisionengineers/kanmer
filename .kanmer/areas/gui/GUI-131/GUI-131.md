---
id: GUI-131
type: ticket
title: Repair v0.3.4 packaged Electron entry before release publication
status: preparing
area: gui
assignee: ''
profile: fix
stageEntered:
  preparing: '2026-08-24T18:51:49.600Z'
labels:
  - release
  - packaging
  - regression
groups:
  - HZN-007
links: []
blocks:
  - CORE-096
  - CORE-098
archived: false
created: '2026-08-24T18:51:14.675Z'
updated: '2026-08-24T21:24:23.590Z'
---

## What

Repair the packaged Electron artifact so Electron Builder includes the configured `out/main/index.js` entry and the updater-package check can produce the required release manifest and NSIS resources.

## Evidence and history

The first trigger was CORE-096's v0.3.4 publisher failure. GUI-131 was archived after a clean package at the v0.3.4 release commit passed and its app.asar contained the main entry; that evidence remains valid and must not be erased.

CORE-098 then reproduced the same symptom in the canonical local publisher context: the v0.3.5 publisher completed all verification rails and pushed its immutable tag, then Electron Builder reported that `app.asar` lacked `out/main/index.js`; follow-on updater-package checks failed because packaging was incomplete. No v0.3.5 GitHub Release or asset exists.

## Reopened scope

Diagnose the difference between the earlier passing clean package and the release-script publisher context at the v0.3.5 merged release commit. Implement only a verified packaging/build-inclusion remediation if the defect is source-owned.

- Preserve both failed release records and never retag, manually upload, or repair v0.3.4/v0.3.5.
- Do not alter release publishing semantics, credentials, workflows, or the dirty root checkout.
- A future successor release requires its own ticket and review after this defect is proven fixed.

## Verification

- A clean package in the canonical release-script context contains `out/main/index.js` in app.asar.
- The updater-package check completes with its required artifacts.
- The exact failing and passing contexts, commands, exit codes, and no-release-state boundaries are retained.
- No source fix is claimed until the discrepancy is reproduced and resolved.

## Outcome
