# Post-implementation report — CORE-139

## Files changed

| Path | Change |
|---|---|
| `.github/workflows/pr.yml` | `verify.if` → `(github.event_name == 'pull_request' && github.event.action != 'edited') \|\| (github.event_name == 'push' && github.ref == 'refs/heads/main')`; workflow-level `concurrency` (group = workflow + event + PR number or ref; `cancel-in-progress` unless `push`) |
| `.github/workflows/board-regate.yml` | `concurrency` group `board-regate-<ref>` with cancel-in-progress; `pull-requests: read`; dispatch step exits when `gh pr list --base main --state open` is empty |
| `scripts/pr-workflow.test.mjs` | new positive/negative assertions on `verify.if`, both concurrency blocks, the upstream guard, three AGENTS.md sentences |
| `plugins/kanmer/skills/kanmer-setup/SKILL.md` | greenfield step no longer links into the Kanmer tree; dangling `Native` removed from the fenced canonical block |
| `scripts/agents-block-body.mjs`, `plugins/kanmer/scripts/agents-block-body.mjs` | dangling `Native` removed; packaged copy regenerated by `npm run plugin:build` |
| `AGENTS.md` | managed block refreshed (`node scripts/agents-block.mjs .`); §6 paragraph on dispatch/regate/concurrency/operator re-copy |
| `scripts/verify-skill-prose.mjs` | check 21 "no shipped skill link escapes its skill folder" |
| `scripts/verify-skill-prose.test.mjs` | `spawnValidator` helper (asserts no spawn error, non-empty stdout) used by every validator spawn; check-21 mutation case |

## Commands and exit codes

| Command | cwd | Exit | Result |
|---|---|---|---|
| `node scripts/agents-block.mjs .` | `.worktrees/core-139` | 0 | AGENTS.md refreshed |
| `node scripts/verify-agents-block.mjs` | `.worktrees/core-139` | 0 | 31/31 checks passed |
| `node --test scripts/pr-workflow.test.mjs` | `.worktrees/core-139` | 0 | 1/1 pass |
| `node scripts/verify-skill-prose.mjs` | `.worktrees/core-139` | 0 | ALL CHECKS PASSED; check 21: 0 hits |
| `grep -c "launcher. Native" AGENTS.md scripts/agents-block-body.mjs plugins/kanmer/skills/kanmer-setup/SKILL.md` | `.worktrees/core-139` | — | 0, 0, 0 |
| `node --test scripts/verify-skill-prose.test.mjs` | `.worktrees/core-139` | 1 (direct), 0 (in rail) | direct run before any build failed to load (`packages/core/dist/index.js` absent in the fresh worktree); inside `npm run verify` → `npm test` → `test:scripts` the file passed, incl. `skill-link validator rejects a link that escapes its skill folder` ✔ |
| `npm run plugin:build && npm run plugin:check` | `.worktrees/core-139` | 0 | plugin-sync OK — 41 tools match, bundle bytes match; `plugins/kanmer/mcp/kanmer-mcp.cjs` unchanged, packaged `agents-block-body.mjs` refreshed |
| `TMP='C:\kt-tmp' TEMP='C:\kt-tmp' npm run verify` | `.worktrees/core-139` | 0 | PASS, 13 steps, ending `plugin-sync OK`; started 2026-09-03T19:04Z (`C:\kt-tmp\core139\verify1.log`) |

## Deviations from the plan

- Two AGENTS.md assertions in `pr-workflow.test.mjs` use `\s+` where the plan implied a literal space, because the sentences wrap across lines.
- The staleness row for the board-regate copy (plan "Out of scope") is deferred to 0.4.2 as planned; the operator re-copy is documented in AGENTS.md §6 and the workflow header.

## Operator step after merge

Re-copy the workflow onto the board branch (the copy is not refreshed automatically):

```
cp .github/workflows/board-regate.yml .worktrees/kanmer/.github/workflows/
git -C .worktrees/kanmer add .github && git -C .worktrees/kanmer commit -m "ci: board push re-gate (CORE-139)"
git -C .worktrees/kanmer push origin kanmer-board
```

## PR

https://github.com/collisionengineers/kanmer/pull/314 — head `bb773f142e263890ad6ed78981f8024d092bfab5` (rebased onto c25e4590 after GUI-149 merged; same diff)
