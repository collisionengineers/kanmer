## 2026-08-27 — execution notes

- Worktree `.worktrees/core-121`, branch `core-121-bootstrap-ownership` from `origin/main` `ea8a6408`. Packet versions: plan `db3f451125275a35`, checklist `9acb739525338053`, files `c9aa48874213d0ab`.
- Deviation 1 (recorded, not a redesign): the attestation parser lives in a new core module `packages/core/src/review-attestation.ts` (exported from `index.ts`) instead of inside `store.ts`, so `check-pr.mjs` can adopt the same function in CORE-123. Tests live in a new `packages/core/src/claims.test.ts` rather than appended to `store.test.ts`.
- Deviation 2 (design correction found by the existing test suite): a hard "reason required for every backward move" broke two existing store tests that move `done → backlog` and `implementing → preparing` with no reason — which is exactly what the Electron GUI does on a drag. The GUI constructs `new KanmerStore(root)` with the default actor `"gui"`; that actor is the human at the board, so its backward moves now carry the implicit reason `operator: moved on the board`. MCP clients (which name themselves) must supply `reason`. This keeps the plan's compatibility constraint (v0.3.12 GUI behaviour unchanged) without weakening the agent-side rule. Ticket Verification box 2 ("backward move without reason is refused") holds for every non-GUI actor and is tested with actor `test-actor`.
- First focused run: `npx vitest run src/claims.test.ts src/store.test.ts src/frontmatter.test.ts` → 5 failed / 107 passed (the two GUI-style backward moves above, one test of mine creating a standalone `plan` item which format 3 refuses, and two not yet inspected). Retained as the first failure; fixes applied, rerun follows.

- `npm run verify` attempt 1 (cwd `.worktrees/core-121`): exit 1. Core 347/347, GUI 486/486, MCP smoke and plugin rails green; MCP `test:http` failed one case — `src/http.test.mjs:65 "project resolution fails before binding and leaves no listener"` with `AssertionError: spawnSync C:\Program Files\nodejs\node.exe ETIMEDOUT` (a 2 s child-process spawn timeout while the GUI suite was also running). Not related to CORE-121 code paths; retained as the first failure. Line endings of three files patched via Python were normalised back to LF and the bundle rebuilt before the rerun.

- `npm run verify` attempt 2: exit 1. Core 347/347, GUI 486/486, MCP HTTP 107/107, smoke/protocol/docs/plugin green; scripts 118/120 — `scripts/antigravity-plugin-config.test.mjs` "the quote-free launcher still reaches the shim when LOCALAPPDATA contains spaces" and "the shipped installer shim restores the provider cwd before MCP launch" fail with `EBUSY: resource busy or locked, rmdir …\Kanmer Test Space\Kanmer\bin`. `npm run test:scripts` alone reproduces it. The same file run on the untouched `origin/main` checkout (`ea8a6408`, `C:\Users\Alex\Documents\GitHub\kanmer`) fails the same two tests in ~150 ms, so the cause is this Git-Bash session's `cmd.exe` spawning environment, not CORE-121. Recorded as INCONCLUSIVE locally; the hosted `verify` job is authoritative for that rail.

- Hand-off: PR https://github.com/collisionengineers/kanmer/pull/287 at head `a79f125c95cad5e1d93ac393a84bb89a7ac5ccc3`; ticket moved implementing → review. Ticket stays taken (`claude-code`, `core-121-bootstrap-ownership`, `.worktrees/core-121`) for review/verify/closeout. Author does not review or merge.
