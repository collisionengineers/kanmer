2026-08-24 — Execution packet obtained from the current origin/main bundled MCP because the installed packaged v0.3.3 server does not expose get_execution_packet. Packet was ready for GUI-129; worktree .worktrees/gui-129 at origin/main 9a75bd690a80bf070bb8ddc372b3a95fa03ec789, branch gui-129-windows-settings-rename-retry.

Baseline: npm test -w @kanmer/gui -- --run src/main/settings.test.ts (cwd .worktrees/gui-129) exited 0: 1 file, 5 tests passed. This is a clean focused baseline; it does not reproduce the intermittent external Windows lock.

2026-08-24 — First GUI typecheck attempt: npm run typecheck -w @kanmer/gui (cwd .worktrees/gui-129) exited 1 before GUI-129 code was typechecked. It resolved stale parent workspace links and reported pre-existing current-main/core contract mismatches: missing @kanmer/core exports dispatchDeliverableProven and withExclusiveFileLock, missing DispatchSupervisorOptions.verifyDeliverable, and an antigravity DispatchProviderId mismatch. This failure is retained. Next action is npm ci inside only the ticket worktree to restore its own current-main workspace links before rerunning.

2026-08-24 — After npm ci, the first repeated focused test attempt exited 1 before collecting tests: Vite could not resolve the unbuilt local @kanmer/core package entry. This is a clean-worktree build prerequisite, not a GUI-129 assertion failure. Next action: npm run build:core in the ticket worktree, then rerun the exact focused command.

2026-08-24 — Full GUI suite attempt: npm test -w @kanmer/gui (cwd .worktrees/gui-129) was started after the focused/typecheck passes. Every emitted suite was green, including settings.test.ts 11/11 and index.sync.test.ts 11/11, but the Vitest process did not terminate after its last output and was interrupted with Ctrl+C; resulting exit was 1. This is retained as INCONCLUSIVE, not a pass, and no assertion was changed to address it.

2026-08-24 — Verification correction: earlier bare npm commands from the nested worktree are setup-contaminated because npm can resolve the parent checkout. They are retained only as failed/setup observations and are not evidence for GUI-129. All authoritative reruns below use npm --prefix with the absolute .worktrees/gui-129 path, including ci --ignore-scripts, core build, tests, typecheck, and GUI build. Commit 49807c28 predates this correction and is not yet presented as verified.

2026-08-24 — Authoritative normal-checkout verification at commit 49807c28 was run with npm --prefix against C:\\Users\\Alex\\AppData\\Local\\Temp\\kanmer-gui129-49807c28-verify. Core passed 310/310. GUI emitted all suites green through settings.test.ts 11/11, then index.sync.test.ts had one failure: “reconciles provider registrations and retains native reconnect state when a closed project reopens” failed because its hook timed out at the suite's 10,000 ms limit. The runner then did not terminate; after two additional no-output waits it was interrupted, exit 1. This is FAIL/INCONCLUSIVE for the full rail, not a pass. The failure is outside GUI-129's two-file scope and no assertion was weakened; next action is an isolated rerun of that named suite before PR creation.
